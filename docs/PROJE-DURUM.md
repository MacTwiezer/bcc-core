# BCC-Core — Proje Durumu

> Bu dosya, yeni bir sohbete başlarken bağlam olarak yapıştırılır.
> Her özellik bittiğinde güncellenir ve commit'lenir.

---

## 1. Proje Nedir

Airtable'ın birebir kopyası (arayüz + işlev) + Airtable'ın çözemediği eksikler.
BCC şirketi için iç araç. Geliştiren: Yiğit Aslantaş.

**Airtable'da olmayan, bizde olması gerekenler:**
- KVKK ekip izolasyonu (TY / GULF / ATP ekipleri birbirinin verisini göremez)
- Slack'e otomatik duyuru
- Zengin metin editörü (Word gibi punto/link)
- Editör sayısı sınırsız (Airtable free 5 editörle sınırlı)
- Türkçe karakter desteği

**Pusula:** "Bu, Airtable taklidi mi, yoksa Airtable'ın çözemediği bir eksik mi?"

---

## 2. Ortam

| | |
|---|---|
| Proje yolu | `C:\xampp\htdocs\bcc-core` |
| Web sunucusu | Apache (XAMPP), DocumentRoot = `public/` |
| Adres | `http://localhost/` |
| PHP | **7.3.33** — `C:\php73\php.exe` (typed properties / 7.4+ özellik YOK) |
| DB | MariaDB 10.4 (XAMPP MySQL), `127.0.0.1:3306`, root, şifresiz, `bcc_core`, utf8mb4 |
| Erişim katmanı | **mysqli** (PDO tamamen kaldırıldı) |
| Git repo | `github.com/MacTwiezer/bcc-core` — her özellik ayrı commit |
| Claude Code | `cd C:\xampp\htdocs\bcc-core` → `claude` |

**Apache'ye PHP 7.3 nasıl bağlandı** (bir daha yaşanmasın diye):
- `httpd-xampp.conf`: PHP 8.2 satırları `#` ile kapatıldı, altına eklendi:
  ```
  LoadModule php7_module "C:/php73/php7apache2_4.dll"
  PHPIniDir "C:/php73"
  ```
- `C:\php73\php.ini`: `extension_dir = "C:\php73\ext"` (TAM YOL), eklentiler kısa ad (`extension=mysqli`)
- `C:\php73\ext` DLL'leri **7.3.33 için derlenmiş** olmalı (thread-safe, VC15)
- `httpd.conf`: `DocumentRoot "C:/xampp/htdocs/bcc-core/public"`

**Başlatma:** XAMPP Control Panel → Apache Start + MySQL Start.

---

## 3. Kalıcı Kurallar

1. **Partials** — tekrar eden HTML/mantık tek yerde (`src/partials/`)
2. **htdocs** — proje Apache'nin göreceği yerde
3. **Apache** — `php -S` KULLANILMAZ
4. **mysqli** — PDO yok, `bcc_query` / `bcc_fetch_all` / `bcc_fetch_one` / `bcc_fetch_column` / `bcc_execute` / `bcc_last_insert_id` / `bcc_begin_transaction` / `bcc_commit` / `bcc_rollback`
5. **Kod tekrarı yok** — aynı şey iki yerde yazılmaz, ortak fonksiyon/partial'a alınır
6. **Özelliğini yazmadığım şeyi ekleme** — belirtilmemiş butonlar sadece görünüm olarak durur

**Güvenlik değişmezleri:**
- Tüm çıktı `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')`
- Tüm sorgular prepared statement
- Tüm formlar CSRF'li
- Her veri erişiminde `require_team_access()` / `require_role()`
- `team_id` her zaman satırdan alınır, URL'den değil
- `audit_log`'a kayıt
- SQL'e gömülen tablo/kolon adları whitelist'ten (prepared statement ile bağlanamazlar)
- `json_encode(..., JSON_UNESCAPED_UNICODE)` — Türkçe için

**Sır yönetimi (kalıcı kural, 2026-08-06'dan itibaren):** Hiçbir gizli/hassas bilgi (SMTP/mail şifreleri, DB şifreleri, API anahtarları, token'lar, kimlik bilgileri, kişisel veri) koda YAZILMAZ ve git'e GİRMEZ — her zaman `.gitignore`'lu bir `*.local.php` dosyasında tutulur (örnek: `config/database.local.php`, `config/mail.local.php`, `config/mail_record_send.local.php`), yanında şifresiz bir `.example` şablonu bulunur. Her commit'ten önce `git status` + `git diff --cached` ile sır sızıntısı kontrol edilir. Yanlışlıkla commit'lenmiş bir sır fark edilirse dosyayı silmek YETMEZ (git geçmişinde kalır) — ayrıca ele alınması gerekir.

**Admin/rol karışıklığı (kalıcı uyarı, 2026-08-06'dan itibaren):** `users.is_admin` (platform Admin paneline erişim bayrağı) ile `team_members.role='owner'` (takım/rol sistemi) birbirinden TAMAMEN FARKLI kavramlardır, karıştırılmamalıdır. `is_admin=1` kişi Admin panelinde "Admin" rozeti görür, bu onun takım rolüyle ilgisi yoktur.

---

## 4. Mimari

> **[2026-07-30 denetimi]** Bu bölüm uzun süredir güncellenmemiş, aşağıdaki sayılar/
> liste artık gerçek durumu yansıtıyor (canlı DB + dosya sistemi karşılaştırılarak
> doğrulandı). Eskiden "13 tablo"/"7 alan tipi" yazıyordu ve `record_links` (artık
> yok) listede, `user_starred_bases`/`user_favorite_views`/`slack_routing_rules`
> (gerçekten var) listede DEĞİLDİ — düzeltildi.

**EAV (Entity-Attribute-Value)** — 15 tablo, hepsi InnoDB + utf8mb4:

`teams`, `users`, `team_members`, `bases`, `user_starred_bases`, `tables_meta`,
`fields`, `records`, `cell_values`, `attachments`, `views`, `user_favorite_views`,
`slack_webhooks`, `slack_routing_rules`, `audit_log`

- `cell_values`: `value_text` / `value_number` / `value_date` / `value_json`
- Ekipler: TY, GULF, ATP
- Roller: owner / editor / commenter / viewer
- Alan tipleri (10): `single_line_text`, `long_text`, `number`, `checkbox`, `date`,
  `single_select`, `multiple_select`, `time`, `user`, `attachment`

**Dosya haritası** (2026-07-30 itibarıyla, yine de tam bir manifest değil — yalnızca
yönelim için; kesin liste her zaman `ls`/`Glob`):
```
config/database.php        mysqli bağlantısı + yardımcılar + config/database.local.php override
src/
  bootstrap.php            error_handler → csrf → auth → audit → schema → slack → validation yükler
  error_handler.php         genel uncaught-exception yakalayıcı (sade hata sayfası/JSON)
  auth.php                 current_user, require_login, require_team_access,
                           require_role, attempt_login, bcc_user_initial
  schema.php               find_base/table/field_or_404, bcc_find_field,
                           parse_grid_* (sort/filter/hidden/group/row_height),
                           filter_condition_sql, bcc_reorder_sibling,
                           normalize_cell_value, cell_display_text
  api_bootstrap.php        public/api/*.php ortak önyükleyici (json_fail, api_require_*)
  validation.php           bcc_is_valid_email / bcc_is_valid_password
  slack.php  xlsx_writer.php  csrf.php  audit.php
  partials/                header.php, top_nav.php, footer.php, flash.php,
                           account_menu.php, home_shell_top.php, home_shell_bottom.php,
                           notifications_panel.php, field_type_wizard_fields.php
public/
  login.php register.php logout.php terms.php privacy.php index.php diag.php
  dashboard.php starred.php workspaces.php account.php    Airtable Home ailesi (paylaşılan kabuk)
  bases.php base.php base_tables.php table_fields.php slack_settings.php
  grid.php                 Airtable Data ekranı (en büyük dosya)
  interface.php            Duyuru (Interface / yayınlanmış görünüm)
  api/                     28 uç nokta (cell_update, record_add, view_*,
                           table_import_csv, table_clear_data, star_base,
                           attachment_*, account_*, base_delete/restore, ...)
  admin/                   index, create_user, create_team, assign_team,
                           export_users_xlsx, export_team_members_xlsx
  assets/                  32 dosya — style.css, login.css, home.css, theme.css,
                           grid-shell.css, interface.css, grid.js + ~14 grid-*.js,
                           account-menu.js, home.js, interface.js, admin.js,
                           dismissable-panel.js, share-popover.js, theme-init.js,
                           logo.png, favicon.svg
scripts/                   create_admin, test_isolation, _isolation_case,
                           _verify_phase4_*
```

---

## 5. Biten İşler

**Faz 0-4 (çekirdek)**
- Şema, bağlantı, tanı sayfası (`diag.php`)
- Kimlik: login/logout/dashboard, roller, ilk admin, admin paneli, CSRF
- **KVKK ekip izolasyonu — 6/6 testle kanıtlı** (`scripts/test_isolation.php`)
- Base/tablo/alan yönetimi, 7 alan tipi
- Grid + hücre düzenleme (AJAX)
- Arama, sıralama (3 slot), filtreleme (5 kural, VE/VEYA) — **19/19 test**

**Airtable arayüz kopyası**
- `login.php` — ortada beyaz kart, BCC logo, "Hoş geldiniz", beyaz arka plan
- `register.php` — aynı tasarım; kayıt `is_active=0` + ekipsiz → admin onaylar (KVKK)
- `dashboard.php` — Airtable Home (üst bar, sol panel, base kartları)
- `grid.php` — Airtable Data ekranı (üst bar, sol dikey şerit, tablo sekmeleri, araç şeridi)
- `base.php` — `dashboard.php` → `grid.php` arasındaki `base_tables.php` ara adımını atlayan köprü sayfası (base'in ilk tablosuna yönlendirir, boşsa `base_tables.php`'ye düşer)
- Tablo sekmeleri barı (`grid.php`) — ortak `bcc_list_base_tables()` (base.php ile paylaşılır), "+ Add table" artık `$canEdit`'e bağlı, uzun tablo adında ellipsis
- Sol üst logo/Base adı (`grid.php`) — küp ikonu zaten dashboard linkiydi; Base adı artık yönlendirmesiz `<div>`, yanıltıcı hover kaldırıldı
- Sekme seçenekleri menüsü (`grid.php`) — her sekmede `<details name="gs-table-tab-menu">` ok, Import/Clear data pasif buton, `grid-table-tabs.js` dışarı tık/Escape ile kapatır
- "All tables" arama/geçiş menüsü (`grid.php`) — $siblingTables tek kaynaktan, TR-locale arama, aktif tabloda tik; grid-table-tabs.js'e toggle-tabanlı "tek menü açık" + açılınca arama kutusuna odak eklendi
- `Ctrl+J`/`⌘+J` kısayolu (`grid-table-tabs.js`) — "All tables" panelini aç/kapat, hücre düzenlerken pasif; koyu tooltip + platforma göre Ctrl J/⌘ J rozeti
- Görünüm bilgi popover'ı (`grid.php`) — `.gs-view-trigger` saf CSS hover (JS'siz, ~300ms gecikme); "Created by" atlandı, `views.created_by` kolonu yok (uydurma veri yok) — migration için YAPILACAKLAR-UI.md maddesi açıldı
- Görünüm seçenekleri dropdown'ı (`grid.php`) — `.gs-view-trigger`'a kardeş `<details name="gs-table-tab-menu">` (ok tetikleyici), tüm öğeler pasif; "Rename view" bağlanacağı yer tek satır yorumla işaretli; kapatma mevcut grid-table-tabs.js mekanizmasıyla
- Görünüm adını satır içi düzenleme (`grid.php`+`grid-table-tabs.js`+`public/api/view_rename.php`) — `bcc_get_or_create_default_view()` (views tablosu, yarış-güvenli get-or-create, DDL yok) her table_id için tek satır garanti eder; dblclick yalnızca editor'de render edilir (`data-view-id`), sunucu da `require_role('editor')` ile ayrıca reddeder; Escape/blur yarışı bayrakla çözüldü, 9/9 uçtan uca doğrulandı (geçici script silindi)
- Dashboard görünüm değiştirici — liste/kart (`dashboard.php`+`home.js`) — önceden var olan işlevsiz `.home-icon-btn` çifti gerçek `data-view-mode-btn` tetikleyicisine bağlandı; `localStorage` tek doğrulama noktası `<head>`'teki senkron script (FOUC yok), `.view-mode-list`/`html.home-view-list` aynı kurala akar; "Çalışma alanı" kolonu $teams'ten (yeni sorgu yok), 8/8 uçtan uca doğrulandı (geçici script silindi)
- Dashboard tarih filtresi (`dashboard.php`+`src/audit.php`+`base.php`) — "son açılma" ne `bases`'te ne `audit_log`'da vardı, `log_base_open()` ile audit_log'a `base.open` yazılmaya başlandı (aynı kullanıcı+base 5 dk içinde tekrar açılırsa güncellenir, F5 satır biriktirmez); işlevsiz eski `.home-filter` div/li stub'ı gerçek `<details>`+whitelist'e (`timeframe`) bağlandı; index eksikliği (~167 satırda maliyetsiz) YAPILACAKLAR-UI.md'ye not düşüldü; 13/13 uçtan uca doğrulandı (geçici script silindi)
- Profil dropdown'ı (`src/partials/account_menu.php`, dashboard.php+grid.php ile paylaşılır) — mevcut avatar/toggle/logout iskeletine Account/Manage groups/Notification/Language/Appearance/Contact sales/Upgrade/Tell a friend/Integrations/Builder hub/Trash pasif öğeleri + Business/Beta rozetleri eklendi; tek gerçek işlev "Log out" (adı "Çıkış"tan değiştirildi) korundu; ikinci menü/kapatma mekanizması açılmadı, `max-height`+scroll eklendi, 9/9 uçtan uca doğrulandı (geçici script silindi)
- Kayıt ekleme — (a) yuvarlak buton, (b) tablo tabanı + satırı, (c) Shift+Enter (`grid.php`+`grid.js`+`public/api/record_add.php`) — üçü de TEK uç noktayı çağırır; `bcc_render_grid_data_row()` grid.php'den `src/schema.php`'ye taşınıp AJAX yanıtıyla paylaşıldı; sort/group aktifken `after_record_id` gönderilmez (sona ekler), filtre/sort/group aktifken tek toast (`.ok` sınıfı yeniden kullanıldı, ikinci bildirim sistemi yok); araya ekleme `position` kaydırmasıyla, istek kilidi ile, viewer'da hem buton gizli hem `require_role('editor')` sunucuda; 13/13 + 9/9 uçtan uca doğrulandı (geçici scriptler silindi)
- Sütun dondurma (`grid.php`+`grid-freeze-columns.js`+`public/api/view_config_update.php`) — `views.config` JSON tek kaynak (localStorage yok, DDL yok); `bcc_get_frozen_column_count()` savunmacı okuma (NULL/bozuk/beklenmedik tip → sessizce 1), read-modify-write diğer config anahtarlarını EZMEZ; satır no kolonu (`.grid-rownum`) zaten sticky olduğundan dokunulmadı, yeni dondurma yalnızca EK kolonlara uygulanır (frozen_column_count=1 = eski davranışla birebir aynı); alt sınır 1, üst sınır `bcc_max_frozen_columns()` (görünür kolonların yarısı, hem istemci hem sunucuda); viewer'da tutamaç yok + sunucuda `require_role('editor')`; mousemove `requestAnimationFrame`'e alındı, pencere dışında bırakma `mouseleave`+`buttons===0` ile temizleniyor; kayıt ekleme ile `window.BCC_reapplyFreeze` üzerinden entegre; 28/28 uçtan uca doğrulandı (geçici script silindi)

**PDO → mysqli geçişi (8 faz)**
- 21 dosya, ~113 sorgu, 2 transaction
- PDO projede tek satır kalmadı (kod + yorum + dokümantasyon)
- Regresyonlar: **6/6 izolasyon, 8/8 sıralama, 19/19 filtre** (hepsi güncel, bu oturumda çalıştırılıp doğrulandı).

**Kod tekrarı temizliği**
- `flash.php` partial (7 dosya)
- `bcc_find_field()` — KVKK sorgusu tek yerde
- `teams.php` → `terms.php`, partials'a geçti
- `account_menu.php` + `account-menu.js` + `bcc_user_initial()`
- `bcc_reorder_sibling()` — sıra değiştirme mantığı
- Bildirim paneli DOM'u + veri hazırlama `src/partials/notifications_panel.php`'ye çıkarıldı (home_shell_top.php + grid.php paylaşıyor, parametrik tetikleyici görünümü)
- Sütun ekleme sihirbazının DOM iskeleti `src/partials/field_type_wizard_fields.php`'ye çıkarıldı (table_fields.php + grid.php'nin "+" popup'ı paylaşıyor)
- 7 ayrı dismiss-panel implementasyonu (home.js×4, grid-view-manage.js, grid-row-detail.js) tek `bcc_bindDismissable()`'a indirildi (`assets/dismissable-panel.js`); grid-table-tabs.js'in grup mantığı örnek alındı, dokunulmadı
- Freeze-columns/resize-columns'un mousedown/mousemove(rAF)/mouseup iskeleti `bcc_bindColumnDrag()`'a indirildi (`assets/grid-column-drag.js`), iki dosya da ortak fonksiyonu çağırıyor

**Grid araçları**
- **Hide fields** — Airtable tarzı toggle, birincil alan gizlenemez, "Find a field", Hide all/Show all
- **Group** — backend çok seviyeli (en fazla 3): parser (`parse_grid_group_rules`), SQL (JOIN+ORDER BY seviye başına), tek geçişte ağaç segmentasyonu, hiyerarşik `data-group-path` ile aç/kapa (iç içe gruplar dahil), `(Empty)` grubu, Collapse/Expand all — hepsi tamam. Panel arayüzü (dropdown, "Add subgroup" linki, seviye silme, durum taşıma) HENÜZ YOK — bkz. Kalan İşler #1; şu an ikinci/üçüncü seviye yalnızca URL'e `group_field_2`/`group_field_3` elle yazılarak kurulabiliyor
- **Row height** — Short/Medium/Tall/Extra Tall + Wrap headers
- **Görünüm listesi paneli (hamburger) — hata düzeltmesi** — `.gs-view-drawer` `.gs-view-toolbar`'ın kardeşiydi, konumlandırma referansı (`position:relative`) toolbar'daydı; drawer toolbar'ın içine taşındı, aç/kapat artık çalışıyor

**Not:** Beş aracın durumu (hidden_fields, sort_*, filter_*, group_*, row_height/wrap_headers) URL'de taşınır ve birbirini korur.

- `audit_log` index migration'ı (`migrations/002_audit_log_entity_index.sql`, `idx_audit_log_entity` = entity_type+entity_id+action) canlı DB'ye uygulandı; ayrıca "sıralama 7/8" kaydı hiç doğrulanmamıştı — kök neden index'le ilgisiz çıktı (`_verify_phase4_sort_search.php`'nin `row-h-*` sınıfını hesaba katmayan katı string eşleşmesiydi), test düzeltildi, 8/8.
- `views.created_by` migration'ı (`migrations/003_views_created_by.sql`, FK → users.id ON DELETE SET NULL) canlı DB'ye uygulandı; `bcc_get_or_create_default_view()` yeni satırda `created_by`'ı oturumdaki kullanıcıyla dolduruyor, eski satırlarda NULL kalıyor; görünüm bilgi popover'ına (`grid.php`) "Created by" satırı yalnızca isim mevcutken (uydurma veri yok) eklendi; 6/6+8/8+19/19 regresyon + manuel oluşturma testiyle doğrulandı.
- Kaydedilebilir görünümler — tek view'ın state'ini kaydetme (`public/api/view_save_state.php`, DDL yok, `views.config['grid_state']`) — görünüm seçenekleri menüsüne "Save view" (yalnızca editor+, `grid.js`); sort/filter/group/hidden/row height/wrap headers durumu, grid.php'nin URL'i parse ettiği AYNI `parse_grid_*` fonksiyonlarıyla sunucuda yeniden doğrulanıp `bcc_grid_state_to_array()` ile kanonik forma çevrilir (istemciden gelen ham state'e güvenilmez); `frozen_column_count` EZİLMEZ (read-modify-write). grid.php artık `table_id` dışında hiçbir GET parametresi olmayan "çıplak" isteklerde kayıtlı `grid_state`'e 302 redirect yapıyor (`bcc_grid_state_is_empty()`) — `parse_grid_*` fonksiyonlarına dokunulmadı, redirect sonrası istek normal yoldan işleniyor. 6/6+8/8+19/19 regresyon + uçtan uca kaydet→redirect→uygulanma doğrulandı (geçici script silindi).
- Arama iyileştirmesi (`grid-toolbar.js`, mevcut event listener'ın üzerine inşa edildi) — satırlar artık `display:none` ile GİZLENMİYOR, eşleşen metin her hücrede `<mark>` ile sarı vurgulanıyor (kullanıcı verisi ham innerHTML birleştirmeyle değil, yalnızca createTextNode/createElement ile enjekte edilir — XSS riski yok); arama kutusunun yanına Airtable tarzı "N / M" sayaç + yukarı/aşağı ok butonları eklendi (aktif eşleşme turuncu, `scrollIntoView`); "grup başlıkları satır gizlenince de görünür kalıyor" kusuru KALKTI (artık hiçbir satır hiç gizlenmiyor) — not silindi. `/browse` ile gerçek tarayıcıda doğrulandı (vurgu, sayaç, ok navigasyonu, temizlenince tüm satırlar/vurgular geri dönüyor); 6/6+8/8+19/19 regresyon yeşil.
- Yeni alan tipleri: **Saat** (`time`, date'in birebir kopyası — DDL yok, `value_text`'e sıfır dolgulu "HH:MM") ve **User** (`user`, DDL yok, `value_number`'a `users.id`) — DAİMA mevcut merkezi mekanizmaya eklendi, paralel sistem yok: `BCC_FIELD_TYPES`/`BCC_FIELD_VALUE_COLUMN`/`BCC_FIELD_TYPE_BADGE`/`BCC_FILTER_OPERATORS`/`BCC_GROUP_DIR_LABELS` haritalarına birer satır, `normalize_cell_value()`/`cell_raw_value()`/`cell_display_text()`/`filter_condition_sql()`/`buildInput()` (grid.js) switch'lerine birer `case`. User'ın id≠görünen-ad farkı için tek yeni parça: `bcc_team_users_by_id($teamId)` (KVKK — yalnızca o takımın aktif üyeleri) id→ad haritası, `cell_display_text()`'e opsiyonel 3. parametre olarak 3 çağrı yerine (grid.php satır render, grup başlığı, `cell_update.php`) + `normalize_cell_value()`'a whitelist olarak (`select_choices_from_options`'ın 'user' karşılığı — gönderilen id o takımın üyesi değilse reddedilir) eklendi; filtre paneli 'user' için serbest metin yerine takım üyelerinden `<select>`'e döner (`grid-filter.js`, `window.BCC_TEAM_MEMBERS`). `table_fields.php`/sort paneli değişmedi (zaten jenerik). 9 fonksiyonel doğrulama (KVKK izolasyonu dahil) + `/browse` ile gerçek tarayıcıda alan oluşturma/hücre düzenleme/filtre/grup uçtan uca doğrulandı, konsol/PHP notice temiz; 6/6+8/8+19/19 regresyon yeşil.
- Sütun ekleme akışı (`table_fields.php`, "Yeni Alan" formu — mevcut `create_field` uç noktası TEKRAR YAZILMADI, üzerine inşa edildi) — tek adımlı form iki adıma bölündü: önce 9 tipin tamamını `$GLOBALS['BCC_FIELD_TYPES']`'tan (elle tekrar yazılmadan) buton grid'i olarak gösteren tip adımı, tip seçilince görünen ve isim odaklanan detay adımı ("Tip değiştir" ile geri dönülür); seçenekler kutusu yalnızca `BCC_SELECT_FIELD_TYPES`'taki tipler için görünür (yeni JS global, elle tekrar yazılmadan). Aynı `<form>`, aynı POST alanları — sunucu tarafı hiç değişmedi. `/browse` ile uçtan uca doğrulandı (tip seç → detay göründü/odaklandı → gönderildi → DB'de doğru `field_type`), konsol temiz; 6/6+8/8+19/19 regresyon yeşil.
- Add subgroup — Group paneli çok seviyeli oldu (`grid.php`+`grid-group.js`, backend zaten 3 seviyeye kadar hazırdı, yalnızca panel HTML/JS eklendi): her seviye kendi alan+yön `<select>`'i + "✕" kaldırma linkiyle (kalan seviyeler sunucuda 1'den yeniden numaralanır, JS gerekmez) ayrı bir satır; "+ Add subgroup" DOM'da zaten basılı ama gizli bir sonraki satırı açar (yeni form elemanı yok); panel etiketi Sort'un "(N)" deseniyle "Group (N)" oldu. Yol boyunca bulunan bug düzeltildi: `$groupState` (diğer panellerin gizli input'larına gruplama durumunu taşıyan değişken) yalnızca 1. seviyeyi koruyordu — `$sortState` ile aynı desene çevrilip TÜM seviyeler artık korunuyor (önceden 2./3. seviye başka bir paneli değiştirince sessizce kayboluyordu). `views.config['grid_state']` (Kaydedilebilir görünümler) zaten tüm seviyeleri yazıyordu, dokunulmadı. `/browse` + doğrudan HTTP testleriyle uçtan uca doğrulandı (2 seviyeli grup, iç içe başlıklar, seviye kaldırma/yeniden numaralama, groupState fix, konsol/PHP notice temiz); 6/6+8/8+19/19 regresyon yeşil.
- Color — tekli/çoklu seçim seçeneklerine renk (seçeneğe göre, koşula göre DEĞİL — Filter altyapısına dokunulmadı). DDL yok: `fields.options` zaten JSON, renkler paralel bir `colors` haritası olarak eklendi (`select_choice_colors_from_options`), `choices` listesi düz string kalmaya devam ediyor (`normalize_cell_value`/`filter_condition_sql`/sort/group hiç değişmedi). Sabit 10 renklik whitelist (`BCC_CHOICE_COLORS`, serbest hex değil — attribute injection riski yok). Yeni alan oluştururken renk otomatik sırayla paletten atanır (`bcc_resolved_choice_color_key`, DB'ye yazılmaz, yalnızca render-zamanı geri dönüş); "Alanı Düzenle"de her seçeneğin yanına renk swatch'ları eklendi (`table_fields.php`, indeks tabanlı `colors[i]`, seçenek metnini array key yapmaktan kaçınıldı). Hücreler artık düz metin değil renkli "chip" (`bcc_render_choice_chips`). Kritik entegrasyon noktası: `cell_update.php`'nin AJAX yanıtına `display_chips` eklendi ve `grid.js`'in `saveCell()`'i DOM ile (textContent, innerHTML string birleştirme yok) chip'i yeniden çiziyor — bu olmasaydı bir hücreyi düzenledikten sonra chip düz metne dönerdi. `/browse` + fonksiyonel testlerle uçtan uca doğrulandı (auto-renk, hücre düzenleme sonrası chip kalıcılığı, renk seçici kaydetme); 6/6+8/8+19/19 regresyon yeşil.
- Slack otomasyonu — Faz 1 (webhook altyapısı + tek kanal gönderimi, tablo-özel — takım-geneli yok). `slack_webhooks` şeması zaten schema.sql'de vardı, canlı DB'de doğrulandı, **DDL gerekmedi**. Yeni `src/slack.php`: `bcc_notify_slack_new_record($tableId, $recordId)` — tabloya bağlı aktif webhook varsa tablo adı + birincil alan değeri + grid linkiyle Slack'e `curl` POST eder; TAMAMEN try/catch içinde, hiçbir hata (webhook yok, ağ hatası, curl eksik) kayıt ekleme akışını engellemez, `webhook_url` hiçbir log/response'a yazılmaz (yalnızca başarı/başarısız `audit_log`'a). Hem AJAX yolu (`record_add.php`) hem JS'siz form fallback'i (`grid.php`'nin kendi `create_record` action'ı) AYNI fonksiyonu çağırır — ikinci bir tetikleme mekanizması yok. Yeni `slack_settings.php` (`table_fields.php` deseninde, CSRF+`require_role('editor')`) — kaydedilen URL bir daha asla tam gösterilmez (yalnızca son 4 karakter maskeli), boş bırakılırsa mevcut URL korunur. **Bulgu:** Apache'de curl şu an ÇALIŞMIYOR (kök neden bulundu: `C:\php73` Apache'nin PATH'inde değil, DLL bağımlılığı çözülemiyor — kullanıcıya PATH+Apache restart talimatı verildi, kodun kendisi bunu gerektirmez, düzeltilince otomatik çalışır). `/browse` + doğrudan HTTP testleriyle uçtan uca doğrulandı (her iki tetikleme yolu, URL maskeleme, sızıntı yok, başarısız gönderimde kayıt yine de eklendi); regresyon paketleri kod tamamlanınca temiz geçti, sonraki tekrar denemeleri ortamdaki (bu değişiklikle ilgisiz, önceden var olan) MySQL kararsızlığı yüzünden yarıda kaldı.
- Slack otomasyonu — Faz 2 (curl doğrulaması + "(başlık boş)" kusuru + takım-geneli webhook). curl artık aktif (PATH düzeltmesi sonrası) — `bcc_notify_slack_new_record()` gerçek HTTP endpoint'i (`record_add.php`) üzerinden uçtan uca doğrulandı (`audit_log`'da `slack.notify_sent`, `webhook_url` sızmadı). **Yeni bulgu:** `curl.cainfo` boş/`cacert.pem` yok olduğu için HTTPS hedeflere (gerçek Slack webhook'ları dahil) istek `SSL certificate problem` (curl errno 60) ile başarısız oluyor — production kodunda SSL doğrulaması KAPATILMADI (güvensiz olurdu), kullanıcıya ayrıca soruldu, kod tarafında yapılacak bir şey yok. "(başlık boş)" → "(başlıksız kayıt)" yapıldı (kayıtlar boş oluşturulup hücreler sonradan dolduğu için hâlâ sık görülecek — bu, mesaj metninin kendisi, davranış değişikliği değil). Takım-geneli webhook: DDL YOK (`slack_webhooks.table_id` zaten nullable) — `bcc_find_slack_webhook($tableId, $teamId)` tablo-özel (varsa) takım-genelinden (table_id NULL) ÖNCELİKLİ şekilde çözer; `slack_settings.php` aynı sayfada iki bağımsız kart (tablo-özel + takım-geneli) gösterecek şekilde genişletildi, HTML tekrarı olmadan (`bcc_render_slack_scope_card()`). Fonksiyonel testlerle doğrulandı (öncelik sırası, tablo-özel silinince takım-geneline düşme, başka takımın webhook'unun sızmaması — KVKK); `/browse` ile ayar ekranı da doğrulandı; 6/6+8/8+19/19 regresyon yeşil.
- Kod tekrarı — grid panel state input'ları: Sort/Filter/Group/Hide-fields panellerinde ayrı ayrı yazılan "diğer panellerin durumunu gizli input'a taşı" `foreach+htmlspecialchars` deseni (4 panelde toplam 11 tekrarlı blok) tek `bcc_render_grid_state_hidden_inputs($state)` fonksiyonuna (`src/schema.php`) çıkarıldı — davranış BİREBİR AYNI kaldı, yalnızca kod organizasyonu değişti (Row height paneli zaten link-tabanlı/DRY olduğu için dokunulmadı). Sort/filter/group/hidden_fields/row_height/wrap_headers'ı AYNI ANDA aktif ederek 4 panelin form'unda doğru alan adı+değerin (ör. `group-form` içinde `sort_field_1=70`, `hidden_fields=71`) eksiksiz taşındığı doğrulandı; 6/6+8/8+19/19 regresyon yeşil.
- Zengin metin editörü (F6 — kalın/italik/font büyüklüğü/link gömme, TAM kapsam). **DDL yok** (`value_text` yeterli). 3. parti WYSIWYG YOK — `document.execCommand('bold'|'italic'|'createLink')` kullanıldı (tarayıcı çıktısı zaten whitelist'le birebir örtüşüyor); font büyüklüğü `execCommand('fontSize')`'ın eskimiş `<font>` çıktısı anında `<span style="font-size:Npx">`'e çevrildi (sabit 7 boyutluk liste). **Güvenlik modeli:** proje genelinin "her zaman render'da htmlspecialchars" kuralının TEK istisnası — `long_text` YAZMA anında `bcc_sanitize_rich_text()` (kendi yazdığım, `DOMDocument` tabanlı whitelist yeniden inşası, regex YOK) ile temizlenip DB'ye güvenli HTML olarak yazılıyor, render'da bu alan için AYRICA kaçırma uygulanmıyor (diğer TÜM alan tipleri eski davranışı koruyor). `script`/`style`/`img`/`on*` event handler'lar dahil whitelist dışı HER ŞEY etiketiyle düşüyor (iç metin kalıyor), yalnızca `strong/b/em/i/br/a[href, http(s) şema zorunlu]/span[font-size: 10-32px sabit liste]` izinli; `target="_blank" rel="noopener noreferrer"` sunucu tarafından ZORLA eklenir, istemciden asla alınmaz. Düzenleme hücre içinde değil popover'da (mevcut Filter/Group panel CSS deseniyle) — araç çubuğu tıklamaları contenteditable'ın blur'unu tetiklediği için "blur=kaydet" deseni burada işlemiyor, açık Kaydet/İptal butonlarıyla çalışıyor; global Shift+Enter "kayıt ekle" kısayolu `richtext-editable`'ı da (textarea gibi) hariç tutacak şekilde güncellendi. Arama vurgulama (`grid-toolbar.js`) `long_text` hücrelerinin `.innerHTML`'ini ARTIK yeniden yazmıyor (biçimlendirme kaybolmasın diye) — satır hâlâ eşleşme sayısına dahil, yalnızca `<mark>` sarmalanmıyor. `/browse` ile uçtan uca doğrulandı: kalın+link kaydetme (sunucu `target`/`rel` eklediğini kanıtladı), `<script>`/`<img onerror>` XSS denemesi tamamen düşürüldü (render'da hiçbir kod çalışmadı, konsol temiz), arama sırasında biçimlendirme korundu; ayrıca 11 sanitizer birim testi (10/11 geçti — bilinen kapsam dışı sınır: tarayıcı DIŞINDAN çıplak `<`/`>` gönderilirse libxml o kısmı düşürebilir, GÜVENLİK sorunu değil, gerçek yol zaten kaçırılmış gönderir); 6/6+8/8+19/19 regresyon yeşil.

- Global arama kutusu (dashboard üst bar, Ctrl+K/Cmd+K ile odaklan, her tuşta canlı filtre). F3 (Duyuru Interface — GEREKSINIMLER.md) ile İLGİSİZ, çünkü F3 hiç yazılmadı (bkz. "Bilerek yapılmayanlar"); bu iş dashboard.php'deki dekoratif, sarılmamış `.home-search` input'unu canlı hale getirdi. **Client-side, sunucuya istek YOK**: `.home-base-grid` zaten kullanıcının TÜM base'lerini (`WHERE b.team_id IN (...)` ile sunucu tarafında KVKK-izole edilmiş, sayfalama yok) sayfa yüklenirken DOM'a basıyor, arama yalnızca bu veriden türetiliyor — debounce/yeni endpoint/yeni KVKK yüzeyi gerekmedi. **Airtable tarzı popover'a taşındı** (ilk sürüm sayfaya gömülü bir bloktu, ekran görüntüsüyle karşılaştırınca yetersiz bulundu): `<details id="home-search">` (mevcut `#home-filter` ile aynı aç/kapa + dışarı-tık deseni) — kapalıyken `<summary>` yalnızca eski görünümü taklit eden tıklanabilir bir tetikleyici, açılınca `position:fixed` bir overlay (arka plan kararır) + üst-ortada sabit bir panel (üstte gerçek input, altında sonuç listesi, en altta "Press Ctrl K any time to search" ipucu). Sonuç satırları PHP'de İKİNCİ BİR DÖNGÜ/SORGU olmadan, `.home-base-card`'ların DOM'dan `cloneNode` ile türetilmiş ikon/ad/tarihinden JS'de inşa edilir; her satıra sabit "Base" tip etiketi eklendi. Sayfadaki asıl `#home-base-grid` artık aramadan ETKİLENMİYOR (yalnızca popover'ın kendi listesi süzülüyor) — canlı-filtre ALGORİTMASI (substring eşleşme, hidden toggle, boş durum sayacı) DEĞİŞMEDİ, yalnızca hedef elemanlar değişti. **Bulunan gerçek bug:** `.home-search-result{display:flex}` (author CSS) tarayıcının varsayılan `[hidden]{display:none}` (UA CSS) kuralını her zaman ezer — origin önceliği specificity eşitliğini geçersiz kılar — bu yüzden `hidden` tek başına satırı gizlemiyordu; `.home-search-result[hidden]{display:none}` eklenerek düzeltildi. `/browse` ile uçtan uca doğrulandı (Ctrl+K → dimmed overlay + ortada panel, kısmi/sıfır eşleşme popover İÇİNDE, Escape ile kapanma, kapanınca sayfa/kartlar hiç değişmemiş, konsol temiz); 6/6 izolasyon regresyonu yeşil.

- Dashboard liste görünümü (`.view-mode-list`) kolon başlıkları — "Name / Last opened / Workspace" (gri, ince, tıklanamaz) satırı `#home-base-grid`'in ilk çocuğu olarak eklendi (`.home-list-header`, `aria-hidden="true"`), varsayılan `display:none`, yalnızca `html.home-view-list`/`.view-mode-list` altında görünür (kart moduna hiç sızmaz, mevcut çift-seçici FOUC deseniyle aynı). Kolon genişlikleri kart satırıyla BİREBİR aynı sabit değerleri tekrarlar (ikon 42px, ad 240px, tarih 160px, workspace flex:1, aynı 0.85rem/1.5rem boşluklar) — yeni bir hizalama mekanizması icat edilmedi. `/browse` ile `getBoundingClientRect()` karşılaştırmasıyla piksel piksel doğrulandı (üç kolonun da x/genişlik değerleri başlık↔satır arasında birebir eşleşiyor), kart moduna geçince başlık `display:none` kalıyor, konsol temiz; 6/6 regresyon yeşil (sorgu katmanı değişmedi). Sonradan "kolonlar alt alta diziliyor / kart modunda kayıyor" hata bildirimi geldi — 3 ayrı taze `/browse` profiliyle (temiz disk cache) tekrar test edildi, HİÇBİRİNDE tekrarlanmadı (computed style: header `display:flex`, `.home-list-header-info` `display:flex`, `flex-basis:240px`; kart modunda `display:none`, kart `left` konumu değişmemiş) — kod diskteki haliyle doğruydu. Kök neden: tarayıcı önbelleğinde eski `home.css` (projede zaten iki kez yaşanmış, dokümante edilmiş bir gotcha). Kalıcı çözüm: `dashboard.php`'de CSS linki artık `home.css?v=<?php echo filemtime(...); ?>` ile mtime-cache-busted — her düzenlemede otomatik yeni URL, bir daha Ctrl+Shift+R'a gerek kalmayacak.

- Base kartına hover ile yıldız (favori) + ⋯ (dekoratif) ikonları. **DDL var, onaylandı**: yeni `user_starred_bases` tablosu (`migrations/004_user_starred_bases.sql` + `schema.sql`) — `UNIQUE(user_id, base_id)`, iki yönlü `ON DELETE CASCADE` (mevcut şemadaki HER many-to-many ilişkiyle — `team_members`, `views` — aynı desen; `audit_log`'u bu amaçla kullanmak araştırıldı ama uygunsuz bulundu, write-only geçmiş tablosu). Yeni uçnokta `public/api/star_base.php` — CSRF + `require_team_access()` (base'in `team_id`'si DB'den okunur, `require_role('editor')` DEĞİL: yıldızlama kullanıcı tercihi, içerik değişikliği değil, viewer da yıldızlayabilir); toggle = tek satır INSERT/DELETE (UNIQUE constraint çakışmayı engeller). `.home-base-card` artık `position:relative` + sağ üstte `.home-base-card-actions` (yıldız hover'da belirir, `aria-pressed="true"` iken hover olmasa da görünür kalır; ⋯ yalnızca hover'da, tıklanınca hiçbir şey yapmaz — GEREKSINIMLER.md'de tanımlı değil, bkz. "Bilerek yapılmayanlar"). Sol panel "Starred" bölümü sunucu tarafında team-scoped bir sorgu ile dolduruluyor (`user_starred_bases` + `bases.team_id IN (...)` JOIN — takımdan ayrılan ama base'i silinmemiş kullanıcı için CASCADE devreye girmez, bu JOIN onu her seferinde yeniden süzer, tıpkı ana base listesinin zaten yaptığı gibi); yıldız tıklanınca sidebar DOM'da anında güncellenir (ad/href karttan okunur, ikinci sorgu YOK — yeni öğe listenin SONUNA eklenir, alfabetik sıra yalnızca sayfa yenilenince sunucudan gelir, kabul edilebilir küçük fark). `/browse` ile uçtan uca doğrulandı: hover'da ikonlar görünür, yıldıza tıklayınca sarı dolar + sidebar'a eklenir + sayfa yenilenince kalıcı, tekrar tıklayınca söner + sidebar'dan kalkar, ⋯ tıklanınca base'e navigasyon TETİKLEMİYOR (kartın `<a>` içinde olmasına rağmen), liste modunda da ikonlar doğru konumda, konsol temiz; 6/6 izolasyon regresyonu yeşil.

- **Starred sayfası (`public/starred.php`)** — sol paneldeki "Starred" linki `href="#"` idi, tıklanınca giden bir sayfa yoktu; artık var. **Kod tekrarı yok**: dashboard.php (Home) ile starred.php AYNI iki fonksiyonu çağırır (`src/schema.php`: `bcc_render_home_base_card()`, `bcc_render_home_base_grid()` — kart/grid HTML'i tek yerde), AYNI kabuğu paylaşır (`src/partials/home_shell_top.php` + `home_shell_bottom.php` — üst bar/arama popover/sol panel artık iki dosyada AYRI AYRI yazılı değil). starred.php'nin sorgusu `user_starred_bases INNER JOIN bases ... WHERE b.team_id IN (...)` (dashboard.php'nin ana sorgusuyla aynı KVKK ilkesi: takımdan ayrılan kullanıcı için sonuç kendiliğinden düşer) — sol panelin Starred alt-listesi İLE ana grid AYNI sorgu sonucundan beslenir, ikinci bir sorgu yok. Tarih filtresi (Home'a özgü) bu sayfada YOK, liste/kart view-toggle VAR (home.js zaten `#home-base-grid` id'sine bağlı, sayfa-agnostik, değişiklik gerekmedi). **Yıldız ikonu artık hover'a bağımlı değil**: `.home-base-star-btn[aria-pressed="true"]{opacity:1}` (önceki işte zaten böyle yazılmıştı, bu turda regresyon YOK olduğu doğrulandı) — konumu da SABİT (`position:absolute`, meatball'la aynı flex satırda `gap`, opacity geçişi layout'u etkilemiyor, hover'da kayma yok). **Ek düzeltme**: starred.php'de bir kartı yıldızdan çıkarmak artık o kartı sayfadan da kaldırıyor (son kart kaldırılıyorsa sunucudaki boş-durum mesajını JS'de tekrar üretmek yerine `window.location.reload()` — tek doğruluk kaynağı sunucu kalır). `/browse` ile uçtan uca doğrulandı: `/starred.php` aynı kart tasarımıyla yalnızca yıldızlı base'i listeliyor, "Starred" nav item `is-active`, hover olmadan yıldız görünür + ⋯ görünmez (opacity geçişi ekran görüntüsüyle kanıtlandı), starred.php'de unstar → kart kalkıyor + boş mesaj doğru çıkıyor, konsol temiz; 6/6 izolasyon regresyonu yeşil.

- Grid ekranı sol Görünüm paneli — Airtable'da tablo sekmelerinin altında SOL TARAFTA duran kalıcı dikey panel ("+ Create new...", "Find a view", view listesi) hiç yoktu; `#gs-view-panel-toggle`'ın açtığı `.gs-view-drawer` VARDI ama yüzen bir dropdown'du (`position:absolute`, dışarı tıklayınca kapanan), toolbar'ın içinde gömülüydü. Artık `.gs-view-drawer` `grid.php`'de yeni bir `.gs-body-row` (flex row) içinde `<main class="gs-main">`'in SOLUNDA normal-akış bir sütun — Home sayfasının `#home-sidebar-toggle`/`.is-collapsed` deseniyle AYNI (width→0, dışarı-tık-kapatma YOK, kalıcı panelde anlamsız). İçerik (Create new/Find a view/tek "Grid view" satırı) DEĞİŞMEDİ, yalnızca konumlandırma. Hâlâ dekoratif: "+Create new..." tıklanamaz, arama kutusu filtrelemiyor, listede her zaman tek satır (`bcc_get_or_create_default_view()` hâlâ tabloya özel TEK view garantiliyor — bkz. Kalan İşler #1, çoklu view desteği bu işin kapsamı DIŞINDA). `/browse` ile uçtan uca doğrulandı: panel varsayılan açık (view listesi + Grid view görünür), toggle'a tıklayınca `width:0`'a daralıyor (grid tam genişliğe yayılıyor), tekrar tıklayınca geri açılıyor, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- Grid satırları: seçim checkbox'ı (hover) + genişlet paneli. **Editor-only** (görünür değilse — `$canEdit=false` — satır numarası eskisi gibi düz metin kalır; grid.js zaten yalnızca editor'e yükleniyordu, bu iş aynı sınırı izledi). `.grid-rownum` artık sayı/checkbox/expand-ikonu üçünü barındırıyor — Starred kartındaki yıldızla AYNI opacity/display deseni: hover'da sayı gizlenir+checkbox görünür, `.is-row-selected` (işaretliyken) hover'dan bağımsız checkbox'ı göstermeye devam eder. Başlık checkbox'ı (`#grid-rownum-selectall`) her zaman görünür, tüm satırları seç/kaldır + indeterminate durumu. Seçimin bir AKSİYONU YOK (istenen kapsam buydu, yalnızca UI). **Genişlet paneli** (`grid-detail-overlay`) ortada modal: sol tarafta satırın TÜM alanları (yalnızca görünür sütunlar değil — `bcc_render_grid_data_row()` artık her `<tr>`'a TÜM alanların `{id,name,field_type,options,raw}` JSON'unu `data-fields` olarak gömüyor, panel açılışında ikinci bir sorgu/AJAX YOK), sağda işlevsiz "All comments" bloğu (GEREKSINIMLER.md'de yok, kapsam dışı). Üstte ▲▼ (önceki/sonraki kayıt, DOM sırasına göre), X (kapat), sol üstte birincil alan başlık. **cell_update.php YENİDEN YAZILMADI** — grid.js'deki `saveCell()` iki katmana ayrıldı (`postCellValue`, `applyCellResultToTd`), `window.BCC_GRID` ile dışa açıldı; panel GÖRÜNÜR alanlar için gerçek `<td>`'yi bulup AYNI fonksiyonları çağırıyor (kaydedince grid'deki hücre de senkron güncelleniyor), GİZLİ alanlar için (satırda `<td>`'si olmayan) doğrudan `postCellValue(recordId, fieldId, value)` — ikinci bir endpoint yok. `buildInput`/`getChoices` de aynı nedenle `<td>`'ye bağımlılıktan kurtarıldı (artık düz bir `choices` dizisi alıyor) — hem gridin kendi hücre-düzenleme akışı HEM panel aynı fonksiyonu kullanıyor. `long_text` panelde zengin metin araç çubuğu YOK (kapsam dışı bırakıldı) — ham HTML, DOM'a hiç eklenmeyen bir `<div>`'e yazılıp `.textContent` ile düz metne çevrilip düz `<textarea>`'da düzenleniyor; kaydedilince yine `bcc_sanitize_rich_text()`'ten geçiyor. **Tıklama çakışması yok**: `.grid-rownum` hiçbir zaman `.editable` sınıfı almadı (grid.js'in hücre-tıkla-düzenle mantığı yalnızca `td.editable`'a bakıyor) — checkbox/expand ekleme bu yüzden yapısal olarak sıfır çakışma; ikisine de savunma amaçlı `stopPropagation()` eklendi (Starred'daki yıldız/⋯ ile aynı desen). `/browse` ile uçtan uca doğrulandı (ayrı ekran görüntüleri): hover'da checkbox+expand beliriyor, işaretleyince hover'dan bağımsız kalıyor, başlık checkbox'ı tümünü seçip kaldırıyor, genişlet paneli açılıp alan düzenlenince hem panel hem alttaki grid hücresi hem DB (doğrudan sorguyla doğrulandı) güncelleniyor, kapatınca temiz kapanıyor, mevcut grid içi hücre-tıkla-düzenle akışı (refactor sonrası) hâlâ çalışıyor, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- **F3/F4 — Duyuru (Interface / yayınlanmış görünüm), salt-okunur** (`public/interface.php`). F4 onayı: "(salt-okunur)" F4'ün BAŞLIĞINDA — herkes için aynı ekran, editor/viewer ayrımı YOK, bu sayfadan hiçbir mutasyon (`cell_update.php` vb.) çağrılmıyor. grid.php'deki ölü "Launch" `<button>`'ı artık gerçek bir `<a href="/interface.php?base_id=...">` — "Interfaces" sekmesi (topbar tab) hâlâ dekoratif, Launch'tan AYRI, ona bağlanmadı. **KVKK**: `base.php` ile BİREBİR AYNI zincir — `find_base_or_404($baseId)` → `require_team_access($base['team_id'])`. **Kod tekrarı yok**: sol nav `bcc_list_base_tables()` (zaten grid.php'nin table-tabs'ında kullanılıyordu) ile dolduruluyor; kayıt/hücre sorguları grid.php'nin AYNI iki-aşamalı deseni (`fields` → `records` → bulk `cell_values` IN (...) → `cellsByRecord`) ama read-only sadeleştirilmiş hâliyle `src/schema.php`'de yeni `bcc_interface_fetch_records()` + `bcc_fetch_cells_by_record()` fonksiyonlarında; `public/interface.php` (ilk yükleme) VE `public/api/interface_search.php` (arama) AYNI `bcc_interface_fetch_records()`'u çağırıyor — paralel sorgu YOK, arama yalnızca fonksiyona bir `$searchTerm` parametresi ekliyor (WHERE'e `EXISTS` koşulu). "Özet" alanı sabit bir kural: tablodaki İLK `long_text` tipli alan (yoksa özet satırı boş kalır) — `bcc_interface_summary_field()`; view/field ayarı gibi yeni bir DB kavramı EKLENMEDİ. "Last Update" `records.updated_at`'e DEĞİL `MAX(cell_values.updated_at)`'e dayanıyor (kayıt satırı hücre düzenlenince bump olmuyor, `cell_values.updated_at` oluyor) — bu aynı zamanda "en yeni en üstte" sıralamasının doğru çalışmasını sağlıyor. **Arama sunucu tarafında** (200ms debounce, `interface_search.php`): listede görünen özet KIRPILMIŞ önizleme olduğu için client-side filtre tam içerikte eşleşmeyi garanti edemezdi — sunucu birincil+özet alanının TAM `value_text`'inde `LIKE` arıyor, yanıt yalnızca eşleşen `record_id` listesi (HTML değil, ikinci bir render yolu yok), istemci zaten basılı satırları gösterip/gizliyor; eşzamanlı istekler için basit bir "yalnızca en son isteğin sonucu uygulanır" yarış-koruması var. Detay paneli TÜM diğer alanları (`{label,value,is_rich}`) satır başına `data-detail-fields` JSON'u olarak gömüyor (grid'in satır-genişletme özelliğindeki `data-fields` deseniyle aynı fikir) — tıklanınca ikinci bir sorgu YOK; `long_text` alanı `is_rich:true` ile işaretlenip `.innerHTML` ile (sunucuda zaten `bcc_sanitize_rich_text()`'ten geçmiş, güvenli) basılıyor, diğerleri `.textContent`. `/browse` ile uçtan uca doğrulandı (ayrı ekran görüntüleri): sarı nav + aktif tablo, kayıt listesi (başlık+özet+tarih), arama eşleşme ("Yig" → "Yiğit" bulundu — utf8mb4_unicode_ci koleksiyonu ğ/g'yi eşdeğer sayıyor, beklenen DB davranışı) VE eşleşme-yok ("No records found"), kayda tıklayınca detay paneli (başlık+Last Update+tüm alanlar), Launch butonu gerçekten buraya götürüyor, konsol temiz; 6/6 izolasyon regresyonu yeşil (bu iş `find_base_or_404`/`require_team_access` dışında sorgu katmanına dokunmadı, 8/8 ve 19/19 ilgisiz).

- Home sayfası (kart+liste) ince ayarlar — Airtable referansıyla karşılaştırma. (1) "Opened anytime" filtre butonu: border/arka plan kutusu kaldırıldı, düz metin+ok. (2) Liste modunda WORKSPACE kolonu başlığı KALDI ama hücre değeri artık HİÇ basılmıyor (`bcc_render_home_base_card()` — `$workspaceName` parametresi geriye dönük uyumluluk için duruyor ama kullanılmıyor). (3) Liste modunda kalıcı (hover'sız) yıldız göstergesi artık sağ uçta kendi "kolonunda" değil, "Last opened" hücresinin İÇİNDE tarihin hemen önünde (`.home-base-meta-star`, yalnızca liste modu + `.is-starred` iken görünür) — actions overlay'in yıldızı liste modunda kalıcılığını kaybetti (`opacity:0` varsayılan, yalnızca hover'da `1`), kart modunda DEĞİŞMEDİ (hâlâ kalıcı). (4-6) "..." menüsü GERÇEK bir menüye dönüştü — Open (sağa açılan alt menü: Interface → `interface.php?base_id=...`, önceki işte kurulan sayfa TEKRAR YAZILMADI) + Duplicate (disabled, yalnızca görünüm); `<details>`+dışarı-tık+Escape deseni (grid-table-tabs.js ile AYNI — birden fazla kart, `toggle` olayı diğerlerini kapatır); kart modunda zaten var olan meatball butonuna bağlandı, ayrıca eklenmedi. **Bulunan gerçek bug:** eski (bir önceki işten kalma) `.home-base-more-btn` click handler'ı `e.preventDefault()` çağırıyordu — o zaman buton bir `<button>`'dı (dekoratifti), şimdi bir `<summary>` — preventDefault native `<details>` açılmasını TAMAMEN ENGELLİYORDU; kaldırıldı (kartın navigasyonunu engelleme işini zaten yeni kod `stopPropagation()` ile, preventDefault OLMADAN yapıyor). **İkinci bug:** `.home-base-card:hover{transform:translateY(-1px)}` (mevcut, dokunulmadı) `position:fixed` torunlar için YENİ bir containing block oluşturuyor — menü paneli viewport'a göre değil dönüştürülmüş karta göre "fixed" oluyor, konum hesaplaması tamamen yanlış çıkıyordu; düzeltme: panel açılışta `document.body`'ye taşınıyor (alt menüsüyle BİRLİKTE, `appendChild` tüm alt ağacı taşır), kapanışta menünün içine geri dönüyor — dışarı-tık kontrolü hem `menu.contains()` hem `panel.contains()`'a bakacak şekilde güncellendi (panel artık DOM'da menünün içinde değil). "Interface" gerçek bir `<a>` DEĞİL — kart zaten bir `<a>`, iç içe `<a>` HTML ayrıştırıcısı tarafından otomatik kapatılıp yapıyı bozardı (bu session'daki grid satır-genişletme/Starred kararlarıyla AYNI ilke) — `<button data-nav-href>` + `window.location.href`. `/browse` ile uçtan uca doğrulandı: filtre butonu düz metin, liste modunda WORKSPACE boş, yıldız "Last opened" içinde (hover'sız kalıcı, hover'da sağ üstte interaktif çift geri geliyor), kart VE liste modunda "..." menüsü doğru konumda açılıyor (liste modunun `overflow:hidden`'ından kaçtığı doğrulandı), Open > Interface tıklanınca gerçekten `interface.php`'ye gidiyor, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- Sol panel + hesap menüsü sadeleştirme (3 ayrı iş, tek turda). **(1)** `src/partials/home_shell_top.php`'den "Templates and apps"/"Marketplace" (ikisi de `href="#"` dekoratif) ve artık boş kalan `.home-sidenav-secondary` nav'ı + öncesindeki divider kaldırıldı. **(2)** `src/partials/account_menu.php` sadeleşti — Manage groups/Notification preferences/Language preferences/Appearance/Contact sales/Upgrade/Tell a friend/Integrations/Builder hub (hepsi zaten işlevsizdi) kaldırıldı; yalnızca **Account, Trash (ikisi de PASİF, onaylanmış karar), Log out (gerçek işlevli, değişmedi)** kaldı. Bu partial dashboard.php ("home" önekiyle) VE grid.php ("gs" önekiyle) tarafından paylaşılıyor — tek dosya değişikliği `/browse` ile HER İKİ sayfada da doğrulandı (ayrı ayrı ekran görüntüsü). **(3) Workspaces sayfası** (`public/workspaces.php`, YENİ) — sol paneldeki dekoratif "Workspaces" linki artık gerçek bir sayfaya gidiyor. **Yeni DDL YOK**: mevcut `teams`/`team_members` üzerine kurulu (araştırma sonucu: GEREKSINIMLER.md'de "workspace" hiç geçmiyor, `team` zaten KVKK-izole edilmiş + rollü-collaborators'lı [`team_members.role`] container — Airtable'ın workspace'iyle birebir aynı rolde, ayrı bir varlık gerekmiyor). Sayfa: kullanıcının üyesi olduğu takımları kart olarak listeler (ad + base sayısı, `bases` tablosunda `GROUP BY team_id`), tıklanınca (`?team_id=X`, yalnızca `$teams` listesindeki bir id kabul edilir + ayrıca `require_team_access()` — savunma amaçlı ikinci kontrol) o takımın collaborators'ı (`team_members JOIN users`, ad/e-posta/rol) + Create/Share/Settings (disabled, onaylanmış karar — yetkilendirme kapsam dışı) gösterir. Sayfa dashboard.php/starred.php ile AYNI kabuğu (`home_shell_top.php`/`_bottom.php`) paylaşıyor — `$homeActiveNav` artık `'workspaces'` değerini de destekliyor. `/browse` ile uçtan uca doğrulandı: sidebar'da iki link yok, hesap menüsü üç öğeye indi (her iki sayfada), Workspaces sayfası "TY" kartını + 2 collaborator'ı (Editor/Owner rolleriyle) doğru gösteriyor, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- Bildirim paneli (zil ikonu) — **(A) seçeneği, onaylanmış karar: DDL yok** (yeni `notifications` tablosu/fan-out INSERT YOK), `audit_log` salt-okunur gösteriliyor. **Tek kolon migration** (`migrations/005_users_last_seen_notifications.sql`, önce gösterildi, onaylandı, sonra çalıştırıldı + `schema.sql`'e eklendi): `users.last_seen_notifications_at DATETIME NULL` — "Unread" = bu zamandan SONRAKİ audit_log satırları, "Read" = ÖNCEKİLER, "Mark all as read" bu tek kolonu `NOW()`'a çeker (NULL = panel hiç açılmamış, her şey unread). `current_user()` (src/auth.php) SELECT listesine bu kolon eklendi. Whitelist (`$GLOBALS['BCC_NOTIFICATION_ACTIONS']`, src/audit.php): `record.create`, `view.rename`, `slack.notify_sent`, `slack.notify_failed`, `team_member.assign`, `team_member.role_change` (**[DÜZELTME]** bu madde önceden `team_member.role_change`'i listeye almamıştı — kod (`src/audit.php`) zaten 6'sını da içeriyor) — `user.login` (canlı DB'nin %66'sı) ve `cell.update` (%7'si) KASITLI DIŞARIDA (onaylanan whitelist). **Kod tekrarı yok**: `bcc_fetch_notifications()` takım id'lerini `current_user_team_ids()`'ten (src/auth.php, zaten vardı) alıyor, ikinci bir "kullanıcının takımları" sorgusu YAZILMADI; `bcc_notification_message()` action→cümle çevirimi tek yerde. **KVKK**: sorgu `team_id IN (current_user_team_ids())` ile filtreli — kullanıcı yalnızca KENDİ takımlarının whitelist'teki olaylarını görüyor. Panel `src/partials/home_shell_top.php`'de (dashboard/starred/workspaces'in AYNI paylaşılan kabuğu) — grid.php'nin KENDİ (paylaşılmayan) zil ikonu bu turun kapsamı DIŞINDA bırakıldı (ayrı bir iş). UI: `<details>`+dışarı-tık+Escape deseni (`#home-filter` ile aynı) — Unread/Read sekmesi + arama TAMAMEN client-side (veri zaten DOM'da gömülü, Ctrl+K aramasıyla AYNI gerekçeyle debounce/AJAX gerekmedi), yalnızca "Mark all as read" gerçek bir AJAX çağrısı (`public/api/notifications_mark_read.php`, CSRF + `is_logged_in()` — `require_team_access()` burada anlamsız, yalnızca oturumdaki kullanıcının KENDİ satırı güncelleniyor). `/browse` ile uçtan uca doğrulandı: rozet sayısı (9+) doğru, popover açılıyor, Read sekmesi başta boş ("Sonuç yok"), Mark all as read tıklanınca rozet kayboluyor + tüm satırlar Read'e taşınıyor + DB'de kalıcı (doğrudan sorguyla doğrulandı) + sayfa yenilenince de kalıcı, arama "Slack" ile 3/21 satıra doğru filtreliyor, workspaces.php'de de (paylaşılan kabuk) çalıştığı doğrulandı, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- Arayüz Türkçeleştirme + küçük temizlikler (tek tur, ~15 dosya). Kalan İngilizce arayüz metinleri (nav öğeleri, panel başlıkları/butonları, rol adları, boş durum mesajları, tooltip'ler) Türkçeye çevrildi — davranış/DOM yapısı/sınıf adları DEĞİŞMEDİ, yalnızca görünen metin. Rol adları için yeni `$GLOBALS['BCC_ROLE_LABELS']` haritası (`src/auth.php`) eklendi — DB'deki İngilizce rol değerleri (`BCC_ROLE_RANK`/`require_role()`) DOKUNULMADI, yalnızca ekranda gösterilen etiket bu haritadan okunuyor (`admin/index.php`, `admin/assign_team.php`, `bases.php`, `workspaces.php`). Regresyon test scriptlerindeki metin bağımlı assertion'lar ("Sort (1)"/"Filter (1)") yeni Türkçe metinlere güncellendi. **Sütun genişliği (resize) özelliği kaldırıldı** (kod tekrarı/karmaşıklık kaygısıyla, onaylanmış karar) — `grid-resize-columns.js` silindi, `bcc_get_column_widths()` (`src/schema.php`) ve `column_widths` config anahtarı (`view_config_update.php`, artık yalnızca `frozen_column_count`'u günceller) kaldırıldı, grid.php'deki `.grid-resizable-th`/`.grid-resize-handle` DOM'u ve `style.css`'teki karşılık gelen ölü kurallar temizlendi; sütun dondurma (freeze) özelliğine DOKUNULMADI, hâlâ çalışıyor. Logo `bcc-logo.svg` → `logo.png` (login/register/dashboard/grid rail — tüm kullanım yerleri). Dashboard üst barındaki işlevsiz "Yardım" linki ve sol paneldeki dekoratif "Shared" nav öğesi kaldırıldı (GEREKSINIMLER.md'de tanımlı değildi, "Bilerek yapılmayanlar" listesindeki diğer dekoratif öğelerle aynı gerekçe). `C:/php73/php.exe -l` ile tüm değişen PHP dosyaları lint edildi, temiz; 6/6+8/8+19/19 regresyon yeşil.

- **Kaydedilebilir görünümler (çoklu view) — TAMAMLANDI.** Büyük kısmı (migration `006_views_description_position.sql` + `007_user_favorite_views.sql`, canlı DB'ye zaten uygulanmış; `view_id` URL routing `bcc_find_view()`/`bcc_get_or_create_default_view()` ile; `bcc_list_table_views()`; uç noktalar `view_rename.php`/`view_delete.php`/`view_duplicate.php`/`view_reorder.php`/`view_description_update.php`/`view_favorite_toggle.php`/`view_export_csv.php`; sol panelde arama/favori/yukarı-aşağı taşıma) önceki bir turda yazılıp commit'lenmişti ama bu dosyaya hiç işlenmemiş — bu turda keşfedilip doğrulandı. **Bu turda eklenen tek gerçek eksik**: "+ Yeni oluştur..." butonu DOM'da vardı ama tıklanınca hiçbir şey yapmıyordu (uç nokta yoktu). Yeni `public/api/view_create.php` — `view_duplicate.php` ile AYNI ekleme deseni (tablonun EN SONUNA, `position` = mevcut MAX + 1, CSRF + `require_role('editor')`), yalnızca kaynak view'ı kopyalamak yerine `config=NULL` boş bir view oluşturuyor; ad `"Görünüm " . (mevcut view sayısı + 1)` (Airtable'ın "Grid 2" deseninin Türkçesi, uniqueness zorunlu değil — `view_duplicate.php`'nin " copy" ekiyle aynı gevşek kural). `grid-view-manage.js`'e `view_duplicate.php` ile AYNI post→redirect deseninde bir click handler eklendi (ikinci bir AJAX mekanizması yok). Buton grid.php'de `$canEdit`'e bağlı hale getirildi (önceden viewer'a da görünüyordu — diğer tüm mutasyon işlemleri, ör. Rename/Delete/Duplicate, zaten bu desendeydi, gözden kaçmış bir tutarsızlıktı). Geçici bir uçtan uca doğrulama scripti (`test_isolation.php` deseninde, editor+viewer ayrı kullanıcılarla) 14/14 geçti (buton editor'de görünüyor + viewer'da gizli, yeni view oluşuyor + `view_id` ile yükleniyor + drawer listesine ekleniyor, geçersiz `table_id` 404, viewer sunucu tarafında da 403 ile reddediliyor) — script silindi. 6/6+8/8+19/19 regresyon yeşil.

- **Hesap Özeti sayfası (`public/account.php`, YENİ)** — hesap menüsündeki önceden işlevsiz "Hesap" öğesi artık gerçek bir sayfaya bağlı (`src/partials/account_menu.php`, `<button>` → `<a href="/account.php">`, hem dashboard hem grid'de). Airtable'ın "Account Overview" ekranı referans alındı, Türkçeleştirildi: büyük avatar (`bcc_user_initial()`), Ad Soyad/E-posta satırları "Düzenle" ile inline düzenlenebilir (Airtable'ın "Edit name" deseni — tıklanınca görünüm satırı gizlenip input+Kaydet/İptal açılır), "Şifreyi güncelle" (mevcut+yeni+tekrar), ekip/rol listesi SALT-OKUNUR (değiştirme admin panelinin işi, kapsam dışı). **ÖNEMLİ GÜVENLİK KARARI: require_role() KULLANILMAZ** — ad/e-posta/şifre değişikliği tamamen ROLDEN BAĞIMSIZ (viewer dahil herkes kendi hesabını düzenler), yalnızca `require_login()` + oturumdaki `$user['id']` (istekten bir kullanıcı id'si asla kabul edilmez); rol yalnızca TAKIM verisine erişimi kısıtlar, kişinin kendi hesabına değil. Üç yeni uç nokta (`view_rename.php` güvenlik deseniyle AYNI, yalnızca require_role satırı YOK): `account_update_name.php` (boş/150 karakter üstü red), `account_update_email.php` (`filter_var` + uygulama katmanında `email != own id` benzersizlik kontrolü — `register.php` ile AYNI desen, DB'deki `uq_users_email` UNIQUE zaten son savunma hattı), `account_update_password.php` (`password_verify()` ile mevcut şifre doğrulanmadan yeni şifre KAYDEDİLMEZ, `current_user()`'ın SELECT listesinde `password_hash` olmadığı için ayrı bir sorguyla okunuyor, min 8 karakter + tekrar eşleşmesi, ne mevcut ne yeni şifre hiçbir log'a yazılmaz — yalnızca "değişti" olayı). Üçü de `audit_log`'a `user.account_updated` yazıyor (`field` ayırt edici, e-posta için `old_email`/`new_email` de). **E-posta bildirimi kasıtlı YOK** (onaylanan karar) — projede mail()/SMTP/PHPMailer altyapısı hiç yok, kurulum kapsamı büyütürdü. Ad/E-posta satırları TEK genel `account-page.js` davranışını paylaşıyor (`data-account-field`/`data-account-endpoint`, ikinci bir kopya yazılmadı) — şifre ayrı akış (3 alan, gösterilecek bir "değer" yok). **Bulunan gerçek bug (bu projede DAHA ÖNCE de yaşanmış, bkz. Home arama popover'ı notu):** `.account-row-form`/`.account-row-display`'in `display:flex` kuralı, tarayıcının `[hidden]{display:none}` UA kuralını origin-önceliğiyle her zaman eziyordu — formlar `hidden` olsa bile hep görünürdü; `[hidden]` için ayrıca `display:none` eklenerek düzeltildi. `/browse` ile uçtan uca doğrulandı (ayrı ekran görüntüleri): ad değiştirme (DB'de kalıcı), e-posta değiştirme (yeni e-postayla giriş yapılabiliyor, DB'de kalıcı), e-posta benzersizlik reddi ("Bu e-posta zaten başka bir hesapta kayıtlı."), yanlış mevcut şifre reddi ("Mevcut şifre yanlış."), doğru akışta şifre değişikliği (DB'de `password_verify()` ile doğrulandı) — `audit_log`'da üç ayrı `user.account_updated` satırı, şifre değerleri hiçbirinde sızmıyor; 6/6+8/8+19/19 regresyon yeşil.

- Grid üst bar (`gs-topbar`) Airtable referansıyla birebir hizalandı — **kök neden bulundu**: kullanıcı "Automations/Interfaces/Forms sekmeleri, Share butonu, ↺ ikonu kaldırıldı ve tamamlandı" diye önceden bir rapor hatırlıyordu, ama `git log`/`git diff` taraması bu deponun TÜM geçmişinde böyle bir değişikliğin hiç yapılmadığını gösterdi — üstelik `docs/PROJE-DURUM.md`'nin kendisi ("Bilerek yapılmayanlar", eski hali) TAM TERSİNİ, bu öğelerin bilinçli olarak dekoratif BIRAKILDIĞINI kayıtlı tutuyordu. "Test/Test Base farklı bir partial kullanıyor olabilir" ihtimali de araştırıldı ve elendi: `grid.php` TÜM tablolar için tek ve aynı şablon, tabloya özel ayrı bir header YOK. Yani önceki "tamamlandı" raporu bu depoda hiç karşılığı olmayan bir iddiaydı — düzeltme gerçek bir kaldırma işiydi, bir "bug" değil. **Yapılan:** `<nav class="gs-topbar-tabs">` (Data/Automations/Interfaces/Forms) ve "Geçmiş" (↺) + "Share" butonları grid.php'den tamamen kaldırıldı — `gs-topbar` artık yalnızca sol tarafta base adı, sağ tarafta Launch (`justify-content:space-between` ile). `grid-shell.css`'teki karşılık gelen ölü kurallar (`.gs-topbar-tabs`, `.gs-topbar-tab` + `:hover`/`.is-active`, `.gs-icon-btn-dark` + `:hover`) temizlendi; `.gs-btn-primary`/`.gs-btn-ghost` başka yerlerde (görünüm açıklaması Kaydet, Launch) kullanıldığı için DOKUNULMADI. Sol alt köşedeki ikon kümesi (#4, ayrıca sorulan) araştırıldı: dashboard/starred/workspaces/account.php VE grid.php'nin sol rail'i zaten birbirleriyle TUTARLI (yalnızca bildirim zili + hesap avatarı) — bir "yardım (?)" veya "gstack benzeri" ikon hiçbir zaman var olmadı, bu turun kapsamı dışında bırakıldı (onaylandı, bkz. Kalan İşler değil çünkü iş açılmadı). `interface.php` (Duyuru) kasıtlı olarak bambaşka, minimal bir kabuk (nav rail/hesap menüsü/zil YOK) — bu bir tutarsızlık değil, "salt-okunur yayınlanmış görünüm" tasarımının parçası. `/browse` ile `table_id=8` (Test), `table_id=9` (Test Base) ve `interface.php?base_id=15` (Duyuru) karşılaştırmalı doğrulandı, üçü de tutarlı; 6/6+8/8+19/19 regresyon yeşil.

- **Notlar alanına native emoji paneli (Win+. / Cmd+Ctrl+Space) — araştırıldı, KOD DEĞİŞİKLİĞİ GEREKMEDİ.** Kontrol edilen üç olası engel: (1) `grid.js`'deki `.richtext-editable` keydown handler'ı yalnızca Escape'i yakalıyor, başka hiçbir tuşu/kombinasyonu engellemiyor — zaten Win+./Cmd+Ctrl+Space işletim sistemi seviyesinde işlenir, sayfa JS'i bunu hiç görmez. (2) `bcc_sanitize_rich_text()` doğrudan emoji içeren girdilerle test edildi (😀🎉🚀👍) — hiçbiri silinmedi, metin düğümleri yalnızca `htmlspecialchars` ile kaçırılıyor, byte/karakter aralığı filtreleyen bir kod yok. (3) DB round-trip: `cell_values.value_text` zaten `utf8mb4`, bağlantı charset'i (`config/database.php`, `$DB_CHARSET`) zaten `utf8mb4` — klasik "MySQL utf8 (3 byte) emoji'yi keser" tuzağına hiç düşülmemiş. `/browse` ile TAM uçtan uca doğrulandı (gerçek bir Notlar hücresine `document.execCommand('insertText', ...)` ile emoji eklendi — native panelin ürettiği input event'iyle AYNI mekanizma —, Kaydet'e tıklandı, sayfa yenilendi, emoji hem DB'de hem ekranda `<b>`/`<a>` biçimlendirmesiyle birlikte sapasağlam duruyordu); test verisi orijinal haline geri döndürüldü.

- **Sütun ekleme sihirbazının 2. adımı — çıplak/stilsiz görünme bugı düzeltildi.** Kök neden: bu partial (`field_type_wizard_fields.php`) `table_fields.php` VE `grid.php`'nin "+" popup'ı tarafından paylaşılıyor; `table_fields.php` sarmalayıcı `<form>`'una `class="stacked"` veriyordu (2. adımın input/textarea/butonunu stilleyen TEK yer, `style.css`'te `form.stacked input/textarea/button`), `grid.php`'ninki bu class'tan YOKSUN'du — 1. adım (`.field-type-grid`/`.field-type-option`, class'tan bağımsız stillenmiş) her iki sayfada da düzgün görünürken 2. adım yalnızca grid.php'de çıplak kalıyordu. **Düzeltme:** `grid.php`'deki `<form id="new-field-form">`'a da `class="stacked"` eklendi (CSS'e YENİ bir şey yazılmadı, yalnızca table_fields.php'nin zaten kullandığı sınıf uygulandı). **Bu düzeltme ikinci, daha ince bir bugı AÇIĞA ÇIKARDI** (muhtemelen table_fields.php'de de ÖNCEDEN vardı, fark edilmemiş): `form.stacked label { display:block }` kuralı, "Seçenekler" satırının (`#new-field-options-row`, yalnızca single/multiple select tipinde görünmesi gereken) `hidden` attribute'unu tarayıcının `[hidden]{display:none}` UA kuralına karşı origin-önceliğiyle eziyordu — bu projede DAHA ÖNCE İKİ KEZ yaşanan AYNI CSS tuzağı (`.home-search-result`, `.account-row-form`). Düzeltme: `form.stacked [hidden] { display: none; }` (style.css, genel — yalnızca bu id'ye özel değil, `.stacked` içindeki her `hidden` elemanı gelecekte de korur). **Yetki kontrolü (ayrıca istenen) zaten doğruydu, değişiklik gerekmedi:** grid.php'deki "+" tetikleyicisi (`<th class="grid-add-field-th">`) zaten `$canEdit`'e bağlıydı, `public/api/field_create.php` zaten `require_role($table['team_id'], 'editor')` çağırıyordu — ikisi de doğrulandı, dokunulmadı. `/browse` ile hem editor (2. adım artık düzgün stillenmiş, Seçenekler satırı tek satır metin/uzun metin gibi tiplerde gizli + tekli/çoklu seçimde görünür, `table_fields.php`'de de aynı düzeltme doğrulandı) hem viewer (`"+"` DOM'da hiç yok, `document.querySelector('.grid-add-field-th')` null) ayrı ayrı doğrulandı; 6/6+8/8+19/19 regresyon yeşil.

- **Sütun ekleme sihirbazı 1. adımda REGRESYON düzeltildi** (bir önceki turun `class="stacked"` eklemesinin yan etkisi). Kök neden: `form.stacked button { background:#1d1d1f; color:#fff; ... }` (2 tip seçici + 1 class → özgüllük 0,1,2) `.field-type-option` (yalnızca 1 class → 0,1,0) ve `.link-btn`'ü (aynı şekilde 0,1,0) eziyordu — 1. adımdaki tip seçim butonları ("Tek satır metin" vb.) SİYAH, "Tip değiştir" linki de siyah bir buton gibi görünüyordu. **Düzeltme:** `form.stacked button:not(.field-type-option):not(.link-btn)` (style.css) — kapsam CSS sınıfı YERİNE `:not()` ile daraltıldı (yeni bir `.field-wizard-step2` sınıfı icat edilmedi, mevcut `.field-type-option`/`.link-btn` sınıfları referans alındı). Tip seçim butonları eski beyaz/açık-gri + ince kenarlık + hover'da gri stiline geri döndü, "Tip değiştir" tekrar mavi altı çizili link oldu, "Alan Oluştur" (gerçek submit butonu) siyah kalmaya devam etti. `/browse` ile doğrulandı, 6/6+8/8+19/19 regresyon yeşil.

- **Duyuru (Interface/F3-F4) arayüzü Airtable referansına hizalandı.** (1) Seçili tablo sekmesi (`.if-nav-item.is-active`) düz beyaz kutucuk (`background:#fff`) yerine sarı zeminin üzerinde hafif koyu-amber yarı saydam vurguya (`rgba(74,59,0,0.14)`) çevrildi — Airtable'daki "düz liste, kutucuk değil" görünümüyle eşleşiyor. (2) Kullanıcının hatırladığı "profil+bildirim zaten hazır, bu ekrana kopyala" talimatı — `git log`'da yine hiç karşılığı yoktu (aynı desen, bkz. Grid üst bar notu). Şimdi gerçekten eklendi: mevcut `notifications_panel.php` VE `account_menu.php` partial'ları `if` önekiyle require edildi (ikinci bir kopya YAZILMADI), yeni bir "Paylaş" butonu (dekoratif, projedeki diğer "Bilerek yapılmayanlar" butonlarıyla AYNI desen) eklendi — sol nav'ın alt satırında Paylaş/zil/avatar. Bunun için `interface.php` artık `home.css`'i de yüklüyor (`.home-notif-*` sınıfları oradan geliyor, style.css/grid-shell.css'in home.css'i YANINDA yüklediği desenle AYNI) + `home.js`/`dismissable-panel.js`/`account-menu.js` script'leri eklendi (home.js'deki TÜM diğer bloklar null-check'li olduğu için bu sayfada sessizce no-op kalıyor, yalnızca `#home-notif` bloğu aktif oluyor). Yeni CSS: `.if-account`/`.if-avatar`/`.if-account-menu`/... (`.gs-account`/`.gs-avatar` deseninin `if` öneki birebir kopyası, koyu-üzerine-amber yerine amber zemine uyarlanmış). **Bulunan gerçek bug (uygulama sırasında, doğrudan test edilerek yakalandı):** panel'ler `is-open`+`display:block` olmasına rağmen ekranda hiç görünmüyordu — `.if-nav { overflow-y:auto }` (tablo listesi kayabilsin diye) `position:absolute` açılan panelleri kırpıyordu, projede DAHA ÖNCE yaşanan `.grid-wrap`/`.grid-add-field-panel` ile AYNI ders. Düzeltme: kaydırma `.if-nav`'ın kendisinden yeni bir `.if-nav-scroll` sarmalayıcısına taşındı (yalnızca tablo listesini kapsar), `.if-nav`'ın kendisi artık overflow'suz — panel/menü artık `.if-nav`'ın dışına (sağına) taşabiliyor. (3) Daralt/genişlet (« / ») — hiç yoktu, eklendi: `.if-nav.is-collapsed` (64px, yalnızca ikonlar: geri-git oku, dekoratif klasör ikonu, Paylaş/zil/genişlet/avatar dikey sıralı) — `interface.js`'e dashboard'ın `#home-sidebar-toggle` deseniyle AYNI fikirde (class toggle) küçük bir handler eklendi. `/browse` ile genişletilmiş hâl, daraltılmış hâl, genişlet-geri-dönüş, bildirim paneli VE hesap menüsü panelinin artık tam görünür açıldığı ayrı ayrı doğrulandı, konsol temiz; 6/6+8/8+19/19 regresyon yeşil.

- **Kod kalitesi temizliği (devir öncesi).** Kapsamlı bir denetim raporunun (partials tekrarı, PDO/`php -S` taraması, kod fazlalığı, ölü kod, yorum kalitesi, isimlendirme tutarlılığı) önceliklendirilen 7 maddesi uygulandı:
  1. `public/curl_test.php` silindi — auth'suz, herkese açık teşhis scripti (Slack curl hata ayıklamasından kalma).
  2. **Yeni `src/api_bootstrap.php`** — `public/api/`'deki **18 dosyanın** birebir aynı kopyaladığı `json_fail()` fonksiyonu + POST/login/CSRF kontrol boilerplate'i tek yerde toplandı (`api_require_post()`/`api_require_login()`/`api_require_csrf()`). 17 dosya üçünü de çağırıyor, `interface_search.php` (salt-okunur GET uç noktası) yalnızca `api_require_login()` çağırıyor — davranış (durum kodu, mesaj, sıra) BİREBİR AYNI kaldı. `view_export_csv.php` bilinçli olarak dokunulmadı (JSON değil CSV döner, zaten `json_fail()` kullanmıyordu).
  3. `public/assets/account-menu.js`'deki elle yazılmış dışarı-tık-kapatma kodu kaldırıldı, `window.bcc_bindDismissable()` (dismissable-panel.js) ile değiştirildi — dismissable-panel.js'in "6 kopya artık bunu çağırıyor" notundaki unutulmuş 7. kopya artık merkezi mekanizmayı kullanıyor. Bu arada gerçek bir sıralama bug'ı bulundu ve düzeltildi: `src/partials/home_shell_bottom.php` `account-menu.js`'i `dismissable-panel.js`'den ÖNCE yüklüyordu (dashboard/starred/workspaces/account.php'de `bcc_bindDismissable is not a function` hatasına yol açardı) — script sırası düzeltildi. Yan etki (kabul edilebilir): menü artık Escape ile de kapanıyor (projedeki her diğer panelle tutarlı).
  4. Ölü kod temizliği: `src/schema.php`'deki çağrılmayan `find_field_or_404()` silindi; `style.css`'teki `.login-box`/`.page-wide`/`.grid-add-record`, `grid-shell.css`'teki `.grid-actions-col`, `home.css`'teki `.home-sidenav-divider` (hiçbiri hiçbir `.php`/`.js`'de referans edilmiyordu) silindi; PDO→mysqli geçişinin Faz 2/3'üne özel 4 tek-kullanımlık script (`scripts/_seed_phase2_test.php`, `_cleanup_phase2_test.php`, `_seed_phase3_test.php`, `_cleanup_phase3_test.php`) silindi, dosya haritasından çıkarıldı.
  5. **Yeni `src/validation.php`** — e-posta format kontrolü (`bcc_is_valid_email()`) ve şifre uzunluk kuralı (`bcc_is_valid_password()`) 4'er ayrı dosyada (register.php, admin/create_user.php, scripts/create_admin.php, api/account_update_*.php) tekrarlanıyordu, tek yere çıkarıldı; `src/bootstrap.php`'ye eklendi (tüm `public/*.php`'ye otomatik gelir), `scripts/create_admin.php` bootstrap.php kullanmadığı için ayrıca require ediyor.
  6. `docs/PROJE-DURUM.md:260,265`'teki hatalı `YAPILACAKLAR.md` referansı `YAPILACAKLAR-UI.md` olarak düzeltildi (dosya bu adla hiç var olmamıştı).
  7. Bu bölümün üstünde yeni "9. Devir/Deploy Notları" bölümü eklendi (`bootstrap.php`'nin `secure=false` session cookie ayarı, canlıya HTTPS ile çıkarken `true` yapılmalı).

  Denetimin kendisi ayrıca `docs/PROJE-DURUM.md`'nin TÜM "Biten İşler" listesini (40 madde) git log ile çapraz doğruladı — hiçbiri işaretlenmedi, hepsinin gerçek karşılığı var (önceki oturumlarda yakalanan "yapıldı deniyor ama git'te yok" durumları zaten düzeltilmişti). KVKK `team_id` erişim kontrolü de ayrıca (CRITICAL öncelikle) tek tek tarandı, atlayan bir yol bulunamadı.

  `/browse` ile 2 ve 3. maddeler özellikle test edildi (davranış değişmemeli talimatı gereği): hesap güncelleme (ad/e-posta) `api_bootstrap.php` üzerinden uçtan uca doğrulandı (DB'de kalıcı), `view_rename.php` doğrudan fetch ile doğrulandı, hesap menüsü dışarı-tık-kapatma davranışı `dashboard.php`/`grid.php`/`interface.php`'nin ÜÇÜNDE de ayrı ayrı doğrulandı (konsol hatası yok, Escape ile kapanma da çalışıyor), `register.php`/`admin/create_user.php` yeni `bcc_is_valid_email()`/`bcc_is_valid_password()` ile doğru mesajları veriyor. 6/6+8/8+19/19 regresyon her madde sonrası yeşil.

- **Trash (Çöp kutusu) özelliği** — Airtable'ın workspace trash'ının karşılığı, profil avatarına tıklayınca çıkan menüden erişiliyor. **DDL var** (`migrations/008_bases_soft_delete.sql`, canlı DB'ye uygulandı + `schema.sql`'e yansıtıldı): `bases`'e `deleted_at`/`deleted_by` (`ON DELETE SET NULL`, mevcut `created_by`/`uploaded_by`/`audit_log.user_id` ile AYNI desen) + `idx_bases_deleted_at`. Base silme artık gerçek `DELETE` DEĞİL — `public/api/base_delete.php` (yalnızca **owner** rolü, Airtable'da da sadece Owner silip geri yükleyebiliyor) `deleted_at=NOW()` yazıyor; `find_base_or_404()`/`find_table_or_404()` (src/schema.php) merkezi olarak `deleted_at IS NULL` filtresi eklendi — TÜM base/tablo erişimi (grid.php, interface.php, API'ler dahil) otomatik olarak silinmiş base'leri görmez, ikinci bir kontrol noktası yazılmadı. `public/api/trash_list.php` — kullanıcının ekiplerindeki silinmiş base'leri (`bcc_home_relative_date()` ile "X gün önce" formatında) + `public/api/base_restore.php` (yine owner-only, `deleted_at=NULL` yazar) sağlıyor. Avatar menüsündeki Trash modalı (`account_menu.php` + `account-menu.js`) Airtable ekran görüntüsüyle birebir: avatar + "Bir base sildiniz: X" + bağıl zaman + "Geri yükle" linki.
- **Koyu/Açık/Sistem tema** — theme toggle, account.php'den kaldırılıp hesap menüsü içine (geri oku ile açılan "Görünüm" alt-paneli, 2 seçenek + seçili olanda tik — Airtable ekran görüntüsüyle birebir) taşındı. Tek kaynak `localStorage['bcc_theme']`; **TEK kopya** `public/assets/theme-init.js` — her sayfanın `<head>`'inde, stylesheet'lerden ÖNCE senkron çalışıp `<html data-theme>` yazarak FOUC'u (aç/koyu sıçraması) önlüyor, dashboard/starred/workspaces/account/grid/interface/login/register'ın HEPSİ bu TEK dosyayı paylaşıyor. Renkler `public/assets/theme.css`'teki `--bcc-*` CSS custom property'lerine taşındı (`[data-theme="dark"]`/`[data-theme="light"]` override + `prefers-color-scheme` sistem varsayılanı), `style.css`/`login.css`/`grid-shell.css`/`interface.css`'teki sabit hex renkler bu değişkenlere kademeli geçirildi (marka/durum renkleri — sarı yıldız, admin rozet renkleri, interface.php'nin altın sidebar teması — bilerek dokunulmadı). **Bulunan gerçek bug**: tema checkmark SVG'leri `hidden` attribute'una rağmen görünmeye devam ediyordu — bu tarayıcının UA stylesheet'i `[hidden]{display:none}` kuralını SVG elemanlarına güvenilir uygulamıyor (projede DAHA ÖNCE birkaç kez yaşanan `[hidden]` tuzağının farklı bir varyantı) — `.is-active` sınıf değişimine çevrilerek düzeltildi.
- **Favicon** — `public/assets/logo.png` kaynak alındı, 8 dosyaya (`header.php`, `home_shell_top.php`, `grid.php`, `interface.php`, `login.php`, `register.php`, `logout.php`, `diag.php`) `<link rel="icon">` eklendi (partials sayesinde çoğu tek yerden). **Bulunan gerçek bug**: `logo.png` kare olmadığı (94×44) için favicon sekmede uzamış görünüyordu; `<image>` ile doğrudan `logo.png`'ye referans veren bir SVG favicon da (aynı sebeple VE ayrıca) hiç render olmadı — SVG'ler `<img>`/favicon kaynağı olarak kullanıldığında içindeki HARİCİ kaynakları (aynı origin olsa bile) çekemiyor. Kalıcı çözüm: `favicon.svg` artık PNG baytlarını `data:image/png;base64,...` olarak DOĞRUDAN gömüyor (harici dosya referansı yok), 32×32 kare canvas içine `preserveAspectRatio` ile ortalanmış.

- **2026-07-30 turu (birden fazla iş, tek günde).**
  - `grid.php` sol üst ikonu artık dashboard'daki AYNI base-renkli ikonu gösteriyor (`bcc_base_icon_color`/`bcc_base_icon_svg`, interface.php'deki desenin aynısı).
  - Sekme dropdown'ındaki inert "Veri içe aktar"/"Verileri temizle" butonları gerçek işlev kazandı: `table_import_csv.php` (mevcut alan adlarıyla eşleşen CSV sütunlarını `normalize_cell_value()` ile doğrulayıp ekler, eşleşmeyen/geçersiz hücreler sessizce atlanır) ve `table_clear_data.php` (kayıtları + dosya eki fiziksel dosyalarını temizler, alan yapısı kalır). Yol boyunca bulunan bug: bu dropdown `.gs-table-tabs-scroll`'un `overflow-x:auto`'su yüzünden (spec gereği overflow-y'yi de auto yapıyor) hiç görünmüyordu — `position:fixed` + JS konumlamayla düzeltildi (projede DAHA ÖNCE birkaç kez yaşanan aynı ders).
  - Görünüm sürükle-bırak sıralaması: satırın kendisini canlı taşıyan (ardışık karede thrashing'e açık) eski mantık, fareyi takip eden ayrı bir "hologram" yer tutucuyla (`.gs-view-drag-placeholder`) yeniden yazıldı.
  - Uzun metin zengin editörü: `.grid-wrap` kırpması yüzünden yarıda kesilen popover `position:fixed`'e taşındı; kullanılmayan "Boyut" (font boyutu) kontrolü kullanıcı kararıyla kaldırıldı.
  - Hücre hover rengi `.cell-view`'ın `td`'yi tam doldurmaması yüzünden kenarlarda boşluk bırakıyordu — `height:100%; box-sizing:border-box` ile düzeltildi.
  - **Veritabanı denetimi**: `migrations/002`'deki `idx_audit_log_entity` hiç uygulanmamıştı (canlı DB'de yoktu) — uygulandı + `schema.sql`'e yansıtıldı. `views.is_published` (hiç okunmayan/yazılmayan ölü kolon) ve `idx_user_starred_bases_user`/`idx_user_favorite_views_user` (ilgili UNIQUE anahtarların soldan öneki zaten kapsadığı için gereksiz) kaldırıldı (`migrations/009`). `fields.is_required` artık `cell_update.php`'de gerçekten uygulanıyor (önceden yalnızca kozmetik bir "*" rozetiydi).
  - **`config/database.php` denetimi**: `config/database.local.php` (adı `.gitignore`'da geçip hiç `require` edilmiyordu) artık gerçekten yükleniyor. Yeni `src/error_handler.php` — yakalanmamış hatalar artık kullanıcıya ham stack trace/SQL sızdırmıyor (sayfa isteğinde sade mesaj, API isteğinde JSON, teknik detay yalnızca `error_log()`'a). `bcc_prepare_positional()`'ın regex'i string literal içindeki `:kelime` desenini artık named parametre sanmıyor; `bcc_bind_type()` bool değerleri doğru bağlıyor.
  - **Dokümantasyon denetimi**: `GEREKSINIMLER.md`'ye gerçek durumla çelişen/eksik kalan yerlere (F2 alan tipleri, F6 font boyutu kararı, veri modeli — `record_links`/`tables_meta`/`slack_routing_rules`, roadmap'in donduğu, açık soruların fiilen cevaplandığı) **[GÜNCEL DURUM]** notları eklendi, orijinal metin silinmedi. Bu dosyanın (`PROJE-DURUM.md`) kendisi de denetlendi: Bölüm 4'teki "13 tablo"/"7 alan tipi" yanlış sayıları ve eski dosya haritası güncellendi, kullanılmayan `public/assets/bcc-logo.svg` (kod tabanında sıfır referans, `logo.png`'ye geçişten kalma) silindi.
  - Tüm değişiklikler `/browse` ile canlı Apache üzerinden test edildi (import/clear-data uçtan uca bir CSV ile, sürükle-bırak gerçek mouse event simülasyonuyla, hata yakalayıcı gerçek bir DB hatası tetiklenerek); 6/6+8/8+19/19 regresyon bu turun SONUNDA da yeşil.

- **2026-08-05 — Kayıt detay modalı görsel güncellemesi (Airtable parite).** Grid'de satır genişletme panelinin HTML iskeleti (`#grid-detail-fields` boş `<div>`, overlay/başlık/kapatma) `public/grid.php`'de (satır ~1377), ama alan render döngüsü (`renderDetailFields()`) `public/assets/grid-row-detail.js`'de — kopya yok, ayrı partial'a taşımaya gerek kalmadı. Değişiklikler:
  - Alan etiketlerine tip ikonu eklendi (`renderDetailFields()`) — yeni bir ikon seti icat edilmedi, `theme.css`'teki mevcut `.field-badge--{tip}` seti (grid başlığı/alan sihirbazıyla PAYLAŞILAN) yeniden kullanıldı.
  - `.grid-detail-field` dikey boşluğu (0.9rem→1.4rem) ve input/select/textarea kutuları (border-radius 6→8px, padding 0.4rem→0.65rem/0.8rem) Airtable referansına yaklaştırıldı; odak (focus) mavi glow zaten `theme.css`'teki genel `input:focus`/`textarea:focus` kuralından geliyordu, ikinci bir kural yazılmadı.
  - Yorum paneli (`.grid-detail-comments-form`) input/buton border-radius'u sol taraftaki alan kutularıyla (8px) eşitlendi.
  - Boş yorum durumu (`renderComments([])`) artık düz metin değil, ortalanmış bir konuşma balonu ikonu + "Bir konuşma başlatın" — Airtable'ın "Start a conversation" boş durumuyla birebir.
  - Dosya eki alanı (`.attachment-manager .attachment-file-input`, YALNIZCA bu geniş bağlamda — 260px'lik sıkışık `.attachment-popover`'a dokunulmadı) kesikli çerçeve + sunken zeminle hafifçe iyileştirildi; native `<input type=file>` bir "drop zone" bileşenine dönüştürülmedi çünkü bu, ana grid hücresindeki AYNI paylaşılan bileşeni de etkileyecekti (kapsam dışı bırakıldı, kullanıcıya raporlandı).
  - Renk/font: hiçbir yeni sabit değer eklenmedi, tamamı mevcut `--bcc-*` (theme.css) değişkenlerine bağlı — koyu modda `/browse` ile ayrıca doğrulandı.
  - `[hidden]` tuzağına (bkz. bu dosyada DAHA ÖNCE birkaç kez geçen aynı ders) dokunulmadı; `.grid-detail-overlay[hidden] { display: none; }` override'ı olduğu gibi korundu.
  - `/browse` ile açık mod, koyu mod, dolu yorum listesi VE boş yorum durumu ayrı ayrı ekran görüntüsüyle doğrulandı. `php -l` + 6/6+19/19+8/8 regresyon yeşil.
  - **Aynı gün, ikinci adım — alan yerleşimi iki sütuna çevrildi.** `renderDetailFields()`'te birincil alan (index 0) eski tam-genişlik/etiket-üstte şablonda kalıyor; diğer TÜM alanlar `grid-detail-field-inline` sınıfıyla sol dar etiket (150px, üste hizalı) / sağ değer iki sütununa geçti (`style.css`). `.grid-detail-field` satırlarına şu an hiçbir yerde `hidden` attribute'u set edilmediği grep ile doğrulandı (bu spesifik döngüde aktif bir `[hidden]` bug'ı yoktu), yine de projenin bilinen `[hidden]` tuzağına karşı `.grid-detail-field-inline[hidden] { display: none !important; }` önleyici olarak eklendi. `/browse` ile bir kayıt açılıp iki sütunlu yerleşim ekran görüntüsüyle doğrulandı. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, üçüncü adım — birincil alanın DEĞERİ büyütüldü.** `renderDetailFields()`'te index 0 satırına ek `grid-detail-field-primary` sınıfı verildi; `style.css`'te YALNIZCA `.grid-detail-field-primary .grid-detail-field-value` içindeki input/select/textarea/`.cell-view` hedeflenip `font-size:1.5rem; font-weight:600; line-height:1.3` verildi — `.grid-detail-field-label` (etiket) BİLEREK dokunulmadı, küçük/gri kaldı; kutunun border/padding'i de değişmedi. Yeni bir `display`/`[hidden]` kuralı eklenmedi. `/browse` ile doğrulandı, `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, dördüncü adım — header'a bağlantı kopyala + yorum aç/kapat ikonları.** `grid.php`'de X'ten önce iki `.grid-detail-nav-btn` eklendi (yeni CSS yazılmadı, mevcut sınıf yeniden kullanıldı). `grid-row-detail.js`: (1) bağlantı ikonu `new URL(location.href)` + `record_id` query param'ını `navigator.clipboard.writeText` ile kopyalıyor, reddedilirse (headless/http gibi bağlamlarda olabiliyor — canlı test SIRASINDA gerçekten bu dala düştü, `execCommand('copy')` `true` döndürdüğü doğrulandı) sessizce `execCommand('copy')` fallback'ine düşüyor; **not: bu URL şu an ziyaret edilince kaydı otomatik AÇMIYOR** (grid.php `record_id` param'ını okumuyor — o ayrı bir özellik, bu adımın kapsamında değildi). (2) yorum ikonu `.grid-detail-comments`'e `.is-collapsed` sınıfı toggle'lıyor — `[hidden]` attribute'una hiç dokunulmadı (projenin bilinen tuzağı böylece komple bypass edildi). Bildirim: `grid.js`'teki `showToast()` KULLANILMADI (o `window.BCC_GRID`'e bağlı ve modal açıkken görünmeyen `.gs-grid-footer`'a ekleniyor, `grid-row-detail.js` bilerek grid.js'den bağımsız) — bunun yerine `.ok` (mevcut yeşil başarı rengi) sınıfını kullanan, header'a `position:absolute` ile konumlanan (flex düzenini bozmayan) yeni bir `.grid-detail-toast`. `/browse` ile hem bildirim hem çift yönlü aç/kapat ekran görüntüsüyle doğrulandı. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, beşinci adım — "..." (Diğer seçenekler) açılır menüsü, kalemler bilerek no-op.** `grid.php`'de başlığın yanına native `<details id="grid-detail-more-menu">` eklendi (yeni bir `.is-open` sınıf mantığı İCAT EDİLMEDİ — `[hidden]`/`display` çakışma tuzağından böylece komple kaçınıldı, native `open` attribute'u tek gerçek kaynak). Panel mevcut `.gs-table-tab-menu-panel`/`.gs-table-tab-menu-item` sınıflarını (grid-shell.css) yeniden kullanıyor, 4 kalem: Kaydı gönder / Kaydı çoğalt / Kaydı yazdır / (ayırıcı) / Kaydı sil (`.gs-table-tab-menu-item-danger`, kırmızı) — hepsi mevcut ikon setiyle tutarlı inline SVG + BİLEREK `onClick` yok (işlev sonraki adımda bağlanacak, buton disabled/gri YAPILMADI). `grid-row-detail.js`: `window.bcc_bindDismissable(moreMenu)` hiçbir option override etmeden çağrılıyor — yardımcının varsayılanları (`isOpen`/`close` = `hasAttribute('open')`/`removeAttribute('open')`) zaten native `<details>` için doğru. `style.css`'e yalnızca konumlandırma (sağa hizalı panel, disclosure üçgenini gizleme) eklendi, yeni panel/kalem stili yazılmadı. `/browse` ile doğrulandı: menü açılıyor, 4 kalem + doğru ikon/etiketler görünüyor (ekran görüntüsüyle teyit edildi), dışarı tıklayınca menü kapanıyor (modal açık kalıyor), Escape ile de kapanıyor — **not: Escape aynı anda alttaki kayıt modalını da kapatıyor**, bu `bindDismissable`'ın paylaşılan `document`-seviyeli `keydown` deseninin (her çağrı kendi Escape dinleyicisini bağımsız ekliyor, katman önceliği/`stopPropagation` yok) zaten var olan bir özelliği — bu adımda YENİ bir davranış eklenmedi, projenin başka yerlerinde de (ör. hesap menüsü/görünüm paneli) aynı iç içe geçme şekli var. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, altıncı adım — ESC katmanlaması düzeltildi (menü açıkken ESC artık sadece menüyü kapatıyor, modalı DEĞİL).** Bir önceki adımın notu ("Escape aynı anda alttaki kayıt modalını da kapatıyor") burada giderildi. Kök neden: `bcc_bindDismissable` (dismissable-panel.js) her çağrıda `document`'a BAĞIMSIZ bir bubble-fazlı `keydown` dinleyicisi ekliyor — overlay'in dinleyicisi menünün açık olup olmadığına bakmadan her ESC'te kapanıyordu. Helper 5 BAŞKA yerde de kullanıldığı için (account-menu.js, grid-table-data.js, grid-view-manage.js, home.js — sonuncusunda `entry.menu`+iç içe `subMenu` AYNI gizli bug'a sahip ama bu adımın kapsamı DEĞİL, dokunulmadı) global DEĞİŞTİRİLMEDİ. Bunun yerine `grid-row-detail.js`'e SADECE bu menüye özel, `document`'a `{capture:true}` ile eklenen bir `keydown` dinleyicisi kondu: capture fazı her zaman bubble fazından ÖNCE çalıştığı için (kayıt sırasından bağımsız), menü açıksa ESC'i kendisi kapatıp `stopPropagation()` ile olayın overlay'in bubble-fazlı dinleyicisine ULAŞMASINI engelliyor; menü kapalıysa hiçbir şey yapmıyor, olay normal akıp modalı kapatıyor (davranış aynı kaldı). `bcc_bindDismissable(moreMenu)` çağrısı click-outside için olduğu gibi bırakıldı (o pathte bug yoktu — overlay'in `isClickOutside` şartı zaten `target === overlay`, modal içeriğine/menüye tıklamak overlay'i hiç etkilemiyordu). `/browse` ile doğrulandı: (a) menü açıkken ESC → sadece menü kapanıyor, modal açık kalıyor; (b) menü kapalıyken ESC → modal kapanıyor (eski davranış korunmuş); (c) helper'ın diğer bir kullanım yeri (`home.js`'teki base kartı "..." menüsü) ESC ile ayrıca test edildi, dokunulmadığı için beklendiği gibi bozulmamış. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, yedinci adım — "Kaydı yazdır" işlevsel: window.print() + temiz tek-kayıt çıktısı (Airtable "Print record" paritesi).** `grid.php`'de butona `id="grid-detail-print-btn"` eklendi (diğer 3 kalem — gönder/çoğalt/sil — HÂLÂ no-op), header'dan sonra/body'den sonra iki boş print-only meta `<div>` (`grid-detail-print-meta-top/-bottom`) eklendi. `grid-row-detail.js`: tıklanınca önce `moreMenu.removeAttribute('open')`, sonra `preparePrintView()` — her alan widget'ının (input/select/textarea/checkbox/dosya-eki yöneticisi/salt-okunur `.cell-view`) O ANKİ DOM değerini okuyup yeni bir `.grid-detail-print-value` düz-metin span'ına yazar (textarea/select print'te kendi scroll/dropdown alanı yüzünden boş/kırpılmış çıkabildiği için tarayıcı print motoruna güvenmek yerine değer JS'te okunup ayrı elemana taşınıyor) + üst meta'ya `.gs-base-name` + bugünün tarihi, alt meta'ya kayıt URL'i yazılır — sonra `window.print()`. `style.css`'e YENİ bir `@media print` bloğu eklendi, TAMAMI `.grid-detail-overlay:not([hidden])` (gerekirse `body:has(...)`) ile şartlı — overlay kapalıyken (mevcut "Görünümü yazdır" grid-print akışı) bu blok hiçbir şeye dokunmuyor, `/browse` ile AYRICA doğrulandı (modal kapalıyken tablo eskisi gibi basılıyor). `[hidden]` global kuralına dokunulmadı.

    Doğrulama sırasında bulunan ve düzeltilen 3 gerçek bug (hepsi `/browse` + `page.pdf()` ile — `window.print()` headless'te NATİF diyalog beklediği için asıl tıklama testinde `window.print` geçici olarak no-op'a stub'landı, üretim kodunda STUB YOK): (1) `.grid-wrap`'in (table.grid'i saran div) KENDİ border/border-radius'u var — yalnızca `table.grid`'i gizlemek sarmalayıcıyı boş/ince bir çizgiye çökertiyordu, düzeltme: `table.grid` DEĞİL `.grid-wrap` gizlenir. (2) `body.gs-body{display:flex}` (grid-shell.css) — overlay'i `position:static` yapmak onu bu flex satırına sokup `.gs-main-col` (içeriği tek tek gizlense de KENDİSİ hâlâ flex:1 alan kaplıyordu) yanında dar bir sütuna sıkıştırıyordu; kök çözüm `.gs-main-col`'un TAMAMINI gizlemek (tek tek `.grid-wrap`/`.gs-table-tabs`/`.gs-fields-link` yerine — hepsini zaten sarmalıyor) + `body`'yi print'te `display:block`'a çevirmek. (3) Alan tipi ikonlarını (`.field-badge`, mevcut set) korumak için yazılan genel "beyaz zemin/siyah metin" kuralı `background: transparent !important` (KISALTMA) kullanmıştı — bu `.field-badge`'in KENDİ `background-image` ikonunu da sıfırlıyordu; düzeltme: `background-color: transparent !important` (yalnızca renk katmanı).

    Airtable referansına göre kapsam: birincil alan büyük/kalın başlık + tam genişlik, iki sütunlu ikon+etiket/değer listesi, üstte tarih+base adı, altta kayıt URL'i — HEPSİ var. Sayfa numarası YOK (tarayıcının kendi print header/footer'ı zaten sağlıyor, CSS ile ayrıca üretilmedi — görevin "en azından" bar'ı bunu karşılıyor). `/browse` ile `page.pdf()` çıktısı görsel olarak doğrulandı: tüm 10 alan doğru değerle (checkbox→"Evet", boş alanlar→"—") görünüyor, yorum paneli/header ikonları/arkadaki tablo/sekme şeridi/"Alanları yönet" hiçbiri sızmıyor, ikonlar basılıyor. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, sekizinci adım — "Kaydı gönder" için mail backend'i kuruldu ve gerçek gönderim doğrulandı (arayüz YOK, bkz. Kalan İşler).** `composer require phpmailer/phpmailer` (v7.1.1, `composer.json`/`composer.lock` commit edildi, `vendor/` `.gitignore`'da — bkz. aşağıda). Office 365 SMTP ayarları (`smtp.office365.com:587`, STARTTLS, `bcccrm@bcciletisim.com.tr`) mevcut `config/mail.local.php`'den (Gmail, `src/mailer.php`'nin elle yazılmış istemcisi, e-posta doğrulama akışı — dokunulmadı) BİLEREK AYRI bir dosyada: `config/mail_record_send.local.php` (`.gitignore`'da, şifre burada — git dışı) + şifresiz `config/mail_record_send.local.php.example` (commit edildi). Geçici bir test scripti (`public/_mail_test.php`, sabit alıcı `yigit.aslantas@bcciletisim.com.tr`, `$mail->ErrorInfo` sessizce yutulmadan basılıyordu) ile CLI'dan gerçek bir mail gönderilip gelen kutusunda DOĞRULANDI, sonra script SİLİNDİ (git'e hiç girmedi — sabit alıcı içerdiği ve asıl "gönder" arayüzü ayrı bir adımda geleceği için). Commit öncesi güvenlik kontrolü: `git status` + `git diff --cached` satır satır tarandı, staged diff'te yalnızca `.example`'daki `'BURAYA_SIFRE'` placeholder'ı eşleşti — gerçek şifre/kullanıcı adı/host git'e hiç girmedi. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, dokuzuncu adım — "Kaydı gönder" arayüzü (Airtable "Send record" paritesi), BACKEND'E DOKUNULMADI.** `grid.php`'de kayıt detay overlay'inden AYRI, kendi backdrop'ı olan ikinci bir overlay (`#grid-send-overlay`, z-index 70 — kayıt modalının 60'ının üstünde) — `.grid-detail-overlay` ile AYNI kanıtlanmış `[hidden]`+açık `display:none` override deseni (bilinen `[hidden]`/`display:flex` çakışma tuzağına tekrar düşülmedi). Modal: Kime (placeholder + 15 alıcı üst sınırı, virgülle sayılıp aşılırsa kırmızı uyarı + Gönder disabled), Konu (`BCC_CURRENT_USER_NAME + "'" + BCC_TABLE_NAME + "' tablosundan 1 kayıt paylaştı"`, otomatik dolu/düzenlenebilir), Mesaj (`"İşte '"+tablo+"' tablosundan bu kaydın son hali:"`, otomatik dolu/düzenlenebilir), altında alan önizlemesi, iki toggle (kapalı başlar), İptal/Gönder. `BCC_CURRENT_USER_NAME`/`BCC_TABLE_NAME` — DOM scrape (ör. `.gs-account-name`) yerine diğer `BCC_*` globalleriyle (`BCC_CAN_EDIT` vb.) AYNI `!empty($fields)` script bloğuna eklendi, tek kaynak sunucudan. Alan önizlemesi: yazdırdaki `fieldPrintText(valueWrap)` KOPYALANMADI, AYNEN çağrılıyor (zaten print'e özel bir şey içermiyordu) — yeni `renderSendPreview()` sadece sonucu `.grid-detail-print-value` yerine kendi önizleme listesine yazıyor, etiket+ikon de `.grid-detail-field-label`'ın `cloneNode`'u. Kapatma: `bcc_bindDismissable` backdrop-tık için (`.grid-detail-overlay` ile birebir `isClickOutside: target===overlay` deseni); ESC için "..." menüsünde kurulan AYNI capture-fazlı, `stopPropagation`'lı özel dinleyici deseni tekrar kullanıldı (send modalı açıkken ESC SADECE onu kapatır, alttaki kayıt modalını DEĞİL — helper'a yine dokunulmadı). Toggle-switch için projede hazır bileşen yoktu, `--bcc-accent`/`--bcc-border` ile küçük yeni bir stil eklendi (native checkbox `<label>` içinde gizli, tema otomatik gelir). "Gönder" tıklanınca: PHPMailer/backend'e HİÇ dokunulmadı, `showDetailToast()` (artık `headerEl` parametresi alacak şekilde genelleştirildi, DRY) kendi başlığında **"Gönderme Adım 4'te bağlanacak"** gösterir, modal AÇIK kalır. `/browse` ile canlı doğrulandı (bir seferinde Playwright'ın `click` komutu belirli elemanlarda takılınca `element.click()` JS ile dispatch edilerek aynı sonuç teyit edildi): menü kapanıyor, modal Airtable benzeri Türkçe açılıyor (ekran görüntüsü), Konu/Mesaj doğru dinamik değerlerle dolu, alan önizlemesi ikon+değerle görünüyor, toggle tıklanınca işaretleniyor, 16 alıcıda hata+disabled/2 alıcıda düzeliyor, İptal VE ESC SADECE gönder modalını kapatıyor (kayıt paneli açık kalıyor), Gönder placeholder toast'ı gösterip modalı kapatmıyor. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, onuncu adım — "Gönder" gerçek backend'e bağlandı: PHPMailer ile gerçek gönderim, rol + alıcı-domain kontrolü BACKEND'de.** Yeni uçnokta `public/api/record_send.php` — `record_delete.php`/`comment_add.php` ile AYNI desen (`api_bootstrap` + `api_require_post/login/csrf`, `bcc_find_record($recordId)` ile `team_id` DB'den — istekten DEĞİL). **Rol:** `require_role($team_id, 'editor')` — projenin TEK rol-kontrol mekanizması, ikinci bir yetki deseni yazılmadı; editor/owner geçer, commenter/viewer `require_role()` içinde 403 ile reddedilir (mail HİÇ gönderilmez). Frontend'de de "Kaydı gönder" butonu artık `<?php if ($canEdit): ?>` ile sarmalı (aynı `$canEdit = role in (editor,owner)`, yeni değişken yok) — ama bu yalnızca UX, asıl karar backend'de. **Alıcı:** her adres `filter_var(FILTER_VALIDATE_EMAIL)` (header injection'ı da reddeder) + `@bcciletisim.com.tr` (case-insensitive) + en fazla 15 — ilk ihlalde o adresi adıyla belirten Türkçe hata, gönderim YOK. "Bir kopyasını bana gönder" AÇIKSA `current_user()['email']` (oturumdan, istekten DEĞİL) listeye eklenir ama SONRA aynı domain kontrolünden geçer — istisna yok (bir `@bcc.local`/kişisel Gmail test hesabı kendine göndermeye çalışırsa reddedilir). **Alan önizlemesi sunucuda YENİDEN ÇIKARILMADI** — istemci zaten ekranda gösterdiği önizlemeyi (`collectFieldPreviewData()`, yazdırdaki `fieldPrintText()`'i çağırıyor) `preview_fields` JSON'u olarak gönderir, backend yalnızca `htmlspecialchars()` ile escape'leyip biçimlendirir (XSS/HTML injection korumalı). **"Tablo düzenini kullan":** iki basit HTML varyantı — açıkken tek satırlık bir `<table>` (başlık satırı=etiketler, veri satırı=değerler), kapalıyken `<b>Etiket</b><br>Değer` alt alta liste. Konu'dan `\r`/`\n` temizlenir (header injection). Başarı: modal kapanır + kayıt panelinin header'ında (mevcut `showDetailToast`, copy-link ile AYNI) "Kayıt gönderildi" toast'ı. Hata: modal AÇIK kalır, backend'in Türkçe mesajı `#grid-send-form-error`'da (`.grid-send-error` ile AYNI stil) gösterilir — sessiz yutma yok. `commentPost()` → `apiPost()` olarak yeniden adlandırıldı (artık hem yorumlar hem gönder modalı aynı genel POST+JSON yardımcısını çağırıyor, ikinci bir kopya yazılmadı). DB'ye YAZMA yok (DDL/INSERT/UPDATE yok, gönderim geçmişi loglanmadı — kapsam dışı bırakıldı).

    **Test (gerçek mail + `/browse`, dört senaryo, sonra fixture'lar temizlendi):** (1) owner (`test.admin`) → gerçek `yigit.aslantas@bcciletisim.com.tr` adresine gönderim: modal kapandı, hata yok (gelen kutusu teyidi kullanıcıdan bekleniyor — kendi gerçek hesabına DOKUNULMADI, sadece "Kime" alanına yazıldı). (2) Rol reddi: `test.kullanici01@bcc.local`'i takım 65'e GEÇİCİ commenter yapıp giriş yapıldı — arayüzde buton yoktu (frontend kapısı), backend'e DOĞRUDAN `fetch()` ile bypass denendi → **`403 | Bu işlem için yetkiniz yeterli değil.`** — mail gönderilmedi. (3) Alıcı kısıtı: `someone@gmail.com` yazıldı → **`422`**, `"someone@gmail.com" adresine gönderilemez — yalnızca @bcciletisim.com.tr adreslerine gönderim yapılabilir.` — modal açık kaldı. (4) Kendine gönder: GEÇİCİ `test.editor@bcciletisim.com.tr` (editor, takım 65) oluşturulup Kime BOŞ + toggle AÇIK ile denendi → tek alıcı (kendi adresi) geçerli sayılıp gönderim BAŞARILI, modal kapandı. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on birinci adım — "Kaydı çoğalt" işlevsel (Airtable "Duplicate record" paritesi).** Yeni uçnokta `public/api/record_duplicate.php` — `record_add.php`/`comment_add.php` ile AYNI desen. **Rol:** `require_role($team_id, 'editor')`, ikinci bir yetki deseni yazılmadı; frontend'de buton `<?php if ($canEdit): ?>` ile sarmalı (aynı `$canEdit`), asıl karar backend'de. **Konum:** `record_add.php`'nin "araya ekleme" mantığı AYNEN kullanıldı — orijinalin `position`'ından büyük tüm kayıtlar +1 kaydırılır, kopya `orijinal.position+1`'e (hemen altına) yerleşir. **Alan kopyası:** boş `INSERT INTO records (table_id, position, created_by)` (`record_add.php` ile AYNI satır — `created_by`/`created_at` bu YENİ INSERT'ten geldiği için otomatik taze, orijinalden hiç kopyalanmıyor), ardından `cell_values` TEK bir `INSERT...SELECT` ile kopyalanır (birincil alan ve dosya-eki alanları HARİÇ — tip bilgisine gerek yok, 4 değer kolonu olduğu gibi taşınır). **Birincil alan** ayrı ele alınır: değeri `value_text` kolonunu kullanan bir tipse (single_line_text/long_text/single_select/time) sonuna `" copy"` eklenir (boşsa `"copy"`); nadir durumda (sayı/tarih/kullanıcı tipi birincil alan) değer aynen kopyalanır, ek yapılmaz. **Yorumlar/dosya ekleri** hiç dokunulmuyor (`comments`'e satır açılmıyor, `attachments` kopyalanmıyor) — yeni kayıt doğal olarak temiz/boş başlıyor. Şema DEĞİŞMEDİ (DDL yok, yalnızca mevcut tablolara veri satırı). Yanıt `bcc_render_grid_data_row()` ile üretilir (ikinci bir satır şablonu yok); `grid-row-detail.js`: dönen `row_html` orijinal satırın hemen altına (`record_add.php`'nin `addRecord()` ile AYNI `insertAdjacentElement` deseni, `window.BCC_GRID.renumberRows`/`window.BCC_reapplyFreeze` zaten dışa açık) eklenir, sonra `openDetail(newRow)` ile modal KOPYAYA geçirilir.

    **Test (DB'ye doğrudan sorgu + backend bypass, `/browse` daemon'ı test sırasında çöktüğü için doğrulama DB/curl üzerinden yapıldı):** Owner (`test.admin`) ile kayıt `972` çoğaltıldı → yeni kayıt `1118`, `position=1` (orijinal `972`, `position=0`'ın hemen altında, `973-977` sırayla `2-6`'ya kaydı) — **konum doğru**. `cell_values` satır satır karşılaştırıldı: 6 normal alan orijinalle **birebir aynı**; birincil alan `"Xlsx test satiri 1"` → `"Xlsx test satiri 1 copy"` — **doğru**. `created_at`: orijinal `2026-08-04 10:33:05`, kopya `2026-08-06 12:52:28` (o anki zaman) — **otomatik alan kopyalanmadı, taze**. Kopyada yorum/ek sayısı **0/0**; orijinalin `cell_values`'ı test öncesi anlık görüntüyle **byte-byte aynı**, yorum sayısı değişmedi — **orijinal bozulmadı**. Modal başlığı "Xlsx test satiri 1 copy" gösterdi — **kopya modalda açıldı**. Rol reddi: `test.kullanici01@bcc.local` geçici commenter yapılıp gerçek bir oturumla (curl+CSRF) `grid.php` HTML'inde buton **0 kez** geçtiği (frontend kapısı), backend'e DOĞRUDAN POST atılınca **`403 | Bu işlem için yetkiniz yeterli değil.`** döndüğü ve kayıt sayısının denemeden önce/sonra **7/7** kaldığı (kopya OLUŞMADI) doğrulandı. Test fixture'ları (kopya kayıt, commenter rolü) ve pozisyon kaydırması sonra tamamen temizlendi, DB test öncesi duruma döndü. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on ikinci adım — "Kaydı sil" Adım 3a: `records` tablosuna soft-delete kolonları eklendi (yalnızca DDL, davranış YOK).** `migrations/012_records_soft_delete.sql` — `bases.deleted_at`/`deleted_by` (`migrations/008_bases_soft_delete.sql`) ile AYNI kanıtlanmış desen: `deleted_at DATETIME NULL`, `deleted_by INT UNSIGNED NULL` + `fk_records_deleted_by` (`ON DELETE SET NULL`, `created_by`/`bases.deleted_by` ile AYNI ilke). **Index kararı bilinçli olarak `bases`'ten FARKLI:** tek kolon yerine bileşik `idx_records_table_deleted_at (table_id, deleted_at)` — `records` `bases`'ten çok daha sık sorgulanıyor (her grid yüklemesi) ve her sorgu zaten `table_id = ? AND deleted_at IS NULL` şeklinde olacak; mevcut `idx_records_table` (tek kolon) BİLEREK kaldırılmadı, ayrı bir sadeleştirme kararı olarak sonraya bırakıldı. DDL'den ÖNCE `mysqldump` ile `records` tablosunun tam yedeği alındı (`storage/backups/records_pre_soft_delete_20260806.sql`, 3560 bayt, 17/17 satır — proje sırasında MySQL iki kez çöktüğü için sigorta). Canlı `bcc_core`'a `mysql` CLI ile uygulandı (008-011 ile AYNI yöntem), `schema.sql`'e yansıtıldı. **Doğrulama:** `SHOW CREATE TABLE records` → `deleted_at`/`deleted_by`/`fk_records_deleted_by`/bileşik index HEPSİ doğru eklenmiş; `SELECT COUNT(*)` → 17 toplam = 17 aktif (`deleted_at IS NULL`) = 0 silinmiş — **mevcut hiçbir kayıt yanlışlıkla "silinmiş" görünmüyor**, DDL öncesi/sonrası satır sayısı **17/17 aynı** (veri kaybı yok). **Davranış değişikliği YOK** — hiçbir sorgu henüz `deleted_at` filtresi almadı (Adım 3c'nin işi), `php -l` + 6/6+8/8+19/19 regresyon yeşil (grid aynı çalışıyor).
  - **Aynı gün, on üçüncü adım — "Kaydı sil" Adım 3b: soft-delete İŞLEMİ işlevsel (grid'in gizlemesi HÂLÂ YOK, Adım 3c'nin işi).** Yeni uçnokta `public/api/record_soft_delete.php` — `record_send.php`/`record_duplicate.php` ile AYNI tek-kayıt desen (`bcc_find_record()`, `require_role($team_id, 'editor')`). **BİLEREK mevcut `record_delete.php`'ye DOKUNULMADI** — o dosya grid araç çubuğundaki checkbox ile ÇOKLU seçim "Seçili N kaydı sil" akışına ait ve GERÇEK (hard) `DELETE` yapıyor; davranışını sessizce soft-delete'e çevirmek bu adımın kapsamı DEĞİLDİ, iki delete yolu (hard/checkbox vs soft/tekli-menü) şimdilik BİLEREK ayrı. Yeni uçnokta: kayıt zaten silinmişse (`deleted_at` dolu) `422` + "Bu kayıt zaten silinmiş." (tekrar UPDATE ÇALIŞMAZ); değilse `UPDATE records SET deleted_at=NOW(), deleted_by=:uid WHERE id=:id AND deleted_at IS NULL` (prepared statement — `AND deleted_at IS NULL` aynı zamanda yarış-durumu güvencesi, `bcc_execute()`'ün döndürdüğü etkilenen satır sayısı 0 ise ayrıca hata; ayrı bir `ROW_COUNT()` sorgusu YAZILMADI, `bcc_execute` zaten `mysqli_stmt->affected_rows`'u döndürüyor). GERÇEK `DELETE` HİÇ kullanılmadı. Audit log action'ı hard-delete'in `'record.delete'`'inden BİLEREK farklı: `'record.delete_soft'`. **Onay diyaloğu:** projede özel bir modal deseni YOK, her yerde (çoklu-kayıt-sil/`home.js` base-sil/`grid-view-manage.js`/`team-members.js`) native `window.confirm()` kullanılıyor — burada da AYNI, yeni bir modal İCAT EDİLMEDİ; mesaj `home.js`'teki base-silme onayıyla BİREBİR AYNI ton: `"'{birincil alan}' kaydını silmek istediğinizden emin misiniz? Çöp kutusundan geri yükleyebilirsiniz."` (birincil alan değeri mevcut `primaryFieldTitle()` ile alınır, yeniden yazılmadı). Başarı: `showDetailToast('Kayıt silindi')` (mevcut mekanizma, aynen) modalın HÂLÂ AÇIK header'ında görünür şekilde gösterilip ~700ms sonra `closeDetail()` çağrılır (toast görünmeden modal kapanmasın diye — yeni bir bildirim yolu YAZILMADI, sadece sıralama). Hata: `window.alert()`, modal açık kalır (native `confirm()` zaten "tekrar denenebilir" sağlıyor). Frontend rol kapısı: buton (ve tek amacı onu ayırmak olan divider) `<?php if ($canEdit): ?>` ile sarmalı — commenter/viewer'da DOM'da bile yok, asıl karar backend'de.

    **Test (curl + gerçek oturum/CSRF ile deterministik, `/browse` sadece native `window.confirm()` diyaloğu için — `dialog-accept`/`dialog-dismiss`):** 3 GEÇİCİ test kaydı oluşturulup (gerçek veriye dokunulmadı) sonra temizlendi. (1) Owner (`test.admin`) kayıt `1143`'ü sildi → `{"ok":true}`, DB'de `deleted_at='2026-08-06 14:22:00'`, `deleted_by=499` — **doğru işaretlendi**; `SELECT COUNT(*) WHERE id=1143` → **1** (satır hâlâ DB'de, GERÇEK DELETE kullanılmadı). (2) Aynı kaydı TEKRAR silmeyi denedi → **`422 | Bu kayıt zaten silinmiş.`**, `deleted_at` DEĞİŞMEDİ (aynı `14:22:00`) — ikinci UPDATE çalışmadı. (3) Rol reddi: `test.kullanici01@bcc.local` geçici commenter yapılıp gerçek oturumla giriş yapıldı — `grid.php` HTML'inde buton **0 kez** (frontend kapısı), backend'e DOĞRUDAN POST (kayıt `1144`) → **`403 | Bu işlem için yetkiniz yeterli değil.`**, `deleted_at` **NULL kaldı**. (4) `/browse` ile İptal: onay diyaloğu TAM beklenen metinle çıktı (`"'SOFT DELETE TEST KAYDI 3 (iptal testi)' kaydını silmek istediğinizden emin misiniz? Çöp kutusundan geri yükleyebilirsiniz."`), `dialog-dismiss` ile reddedildi → `deleted_at` **NULL kaldı**. (5) AYNI kayıt (`1145`) üzerinde `dialog-accept` ile onaylandı → modal kapandı, DB'de `deleted_at` **doğru şekilde işaretlendi**. (6) Ekran görüntüsünde 3 test kaydı DA HÂLÂ grid'de görünüyordu (`972`'nin soft-delete edilmiş hâli dahil) — **BEKLENEN**, Adım 3c henüz sorgulara filtre eklemedi, bu bir hata DEĞİL. Fixture'lar (test kayıtları + commenter rolü) sonra tamamen temizlendi. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on dördüncü adım — "Kaydı sil" Adım 3c: TÜM sorgular `deleted_at IS NULL` filtresi aldı, silinen kayıtlar artık gerçekten gizli.** Önce doğrulandı: `bcc_build_grid_records_query()` (`src/schema.php`) grid'in TEK gerçek kaynağı — `public/grid.php` (render/sıralama/filtre/gruplama/satır sayacı) VE `view_export_xlsx.php` (Excel indir) İKİSİ DE bunu çağırıyor, paralel bir sorgu yolu YOK; arama (`grid-toolbar.js`) client-side ve zaten bu sonuçtan beslendiği için ikinci bir değişiklik gerekmedi. Tek satır ekleme (`WHERE r.table_id = :table_id AND r.deleted_at IS NULL`) grid+arama+filtre+sıralama+gruplama+sayaç+export'un HEPSİNİ TEK YERDEN kapsadı. 3a'nın tespit ettiği diğer 8 dosya tek tek değerlendirildi: **ENGELLENDİ** — `cell_update.php`/`attachment_upload.php` (silinmiş kayda hücre/dosya-eki eklenemez, `400 | Bu kayıt silinmiş, düzenlenemez.`), `record_duplicate.php` (silinmiş kaydın orijinal-satır sorgusuna `AND deleted_at IS NULL` eklendi, zaten var olan `404 | Kayıt bulunamadı.` yoluna doğal düştü, sıfır ek kod), `record_add.php`'nin `after_record_id` sorgusu (silinmiş bir kaydın "hemen altına" araya eklemek artık sessizce "sona ekle" fallback'ine düşüyor, hata YOK — geçersiz id'de zaten var olan davranışla AYNI). **ETKİLENMEDİ, bilerek** — `record_delete.php` (çoklu-seçim GERÇEK delete: zaten soft-delete edilmiş bir kaydı ikinci kez "gerçek" silmek "çöpü kalıcı boşalt" ile aynı semantik, hata VERMEMESİ doğru), `table_clear_data.php` ("TÜM kayıtlar silinecek" onayı veriliyor, çöptekiler de silinmeli — tutarlı), `table_import_xlsx.php` ve `record_add.php`/`table_import_xlsx.php`'nin `MAX(position)` sorguları (yalnızca INSERT/sona-ekleme, mevcut kayıt okumuyor/eşleştirmiyor). **BİLEREK dokunulmayan bir yer bulundu:** `bcc_find_record()` (schema.sql, `comment_add.php`/`record_send.php`/`record_duplicate.php`/**`record_soft_delete.php`'nin kendisi** tarafından paylaşılıyor) — bunu global filtrelemek `record_soft_delete.php`'nin 3b'de kurup TEST ETTİĞİMİZ "zaten silinmiş" `422` mesajını SESSİZCE `404`'e çevirip o adımın davranışını KIRARDI, bu yüzden dokunulmadı; yalnızca yukarıdaki dosyalara YEREL filtre eklendi. `comment_add.php`/`record_send.php` (silinmiş kayda yorum/mail) orijinal 9 dosya listesinde yoktu (paylaşılan helper çağırıyorlar, kendi dosyalarında "FROM records" metni yok) — benzer bir boşluk ama kapsam dışı, gelecekte ayrı değerlendirilmek üzere not edildi.

    **Regresyon riski özellikle kontrol edildi:** `_verify_phase4_sort_search.php`/`_verify_phase4_filter.php` kendi geçici kayıtlarını oluşturuyor, DDL'den (3a) beri `deleted_at` varsayılanı `NULL` olduğu için etkilenmediler — **6/6+8/8+19/19 uygulamadan hemen sonra da yeşildi, DUR gerekmedi.** **Test:** taze bir test kaydı oluşturulup grid'de görünür olduğu doğrulandı (curl, `data-record-id` + metin eşleşmesi), soft-delete edildikten sonra AYNI sayfa isteğinde **hem id hem metin 0 kez** geçti — tamamen gizlendi; gerçek kayıtlar (`Xlsx test satiri 1` vb.) ve satır sayısı (6→6, silinen dahil değil) DEĞİŞMEDİ. `cell_update.php` ile silinmiş kaydı düzenleme denendi → `400 | Bu kayıt silinmiş, düzenlenemez.`, DB'de değer DEĞİŞMEDİ. `record_duplicate.php` ile çoğaltma denendi → `404 | Kayıt bulunamadı.`. `record_add.php`'ye `after_record_id`=silinmiş-kayıt gönderildi → hatasız `200`, yeni kayıt sessizce SONA eklendi (pozisyon kayması OLMADI). `/browse` ile "7 kayıt" sayacı (6 orijinal + 1 yeni test kaydı, silinen SAYILMADI) ekran görüntüsüyle doğrulandı. Test kayıtları temizlendi. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on beşinci adım — "Kaydı sil" Adım 3d: çöp kutusu arayüzü + geri yükleme + 7 gün otomatik temizlik — zincirin SON parçası.** YENİ bir modal YAZILMADI — `bases` için zaten var olan Trash (`migrations/008`, hesap menüsü avatarından açılan TEK `.bcc-trash-overlay`, `src/partials/account_menu.php`) analiz edilip AYNI modale "Kayıtlar" bölümü eklendi (tek yerden yönetim). **Backend, 2 yeni uçnokta, `base_restore.php`/`trash_list.php` deseninin BİREBİR aynısı:** `trash_records_list.php` (`records ⋈ tables_meta ⋈ bases ⋈ users`, KVKK takım filtresi + `deleted_at IS NOT NULL` + ebeveyn base silinmemiş olmalı — aksi halde zaten "Base'ler" bölümünde görünen bir base'in kayıtları burada TEKRAR görünürdü) ve `record_restore.php` (`UPDATE records SET deleted_at=NULL, deleted_by=NULL WHERE id=? AND deleted_at IS NOT NULL`). **Bilinçli fark:** `base_restore.php` yalnızca `owner`'a izin veriyor, ama görev editor+owner istedi — bu owner-only kural KÖRLEMESİNE kopyalanmadı, `record_restore.php` `require_role($teamId, 'editor')` kullanıyor. Kayıt zaten aktifse (`deleted_at` zaten NULL) `422 | Bu kayıt zaten aktif, geri yüklemeye gerek yok.` **Birincil alan değeri N+1 sorgu YAZMADAN:** (1) her `table_id` için MIN(position,id) alanı "greatest-n-per-group" deseniyle TEK sorguda, (2) o alan id'leriyle `cell_values` TEK sorguda, (3) grid'in KENDİSİNİN kullandığı `cell_display_text()` (kullanıcı adı çözme/tarih biçimleme dahil) her satıra uygulanıyor — yeni bir "değeri okunur metne çevir" mantığı yazılmadı. **Frontend (`account-menu.js`):** eski `renderTrashItems()` parametreleştirilip `renderTrashSection()`'a çıkarıldı (liste/boş-durum elemanı, id attribute, restore URL'i, id param adı parametre) — base'ler VE kayıtlar AYNI fonksiyonu çağırıyor, kopya satır-render kodu YOK; çöp kutusu açılınca iki liste de tek seferde çekiliyor. **`.bcc-trash-list`/`.bcc-trash-item`/vb. TAMAMEN yeniden kullanıldı**, TEK yeni CSS kuralı `.bcc-trash-section-title` (küçük bölüm başlığı).

    **7 günlük otomatik temizlik — "ziyaret anında kontrol", cron/Task Scheduler YOK:** Tetikleme noktası olarak grid.php'nin HER yüklemesi DEĞİL, **çöp kutusu açılışı** (`trash_records_list.php`'nin başı) seçildi — grid çok daha sık ziyaret ediliyor, çöp kutusu nadiren açılıyor; kimse ziyaret etmezse temizlik gecikir ama bu kabul edilebilir (Airtable'ın kendisi de "bir noktada" arka planda yapıyor, saniye hassasiyeti gerekmiyor). Maliyet: **ikinci bir sorgu YAZILMADI** — zaten listeleme için çekilen `deleted_at IS NOT NULL` satırları PHP'de `strtotime(deleted_at) < strtotime('-7 days')` ile ayıklanıp, yalnızca EXPIRED olanlar varsa tek bir toplu `DELETE FROM records WHERE id IN (...)` çalıştırılıyor (GERÇEK DELETE — bu noktada BİLEREK ve DOĞRU, `cell_values`/`attachments` FK `ON DELETE CASCADE` ile otomatik temizleniyor), her biri `'record.purge'` action'ıyla audit'e yazılıyor. "Kalıcı boşalt" (owner-only manuel purge): **bases Trash'inde böyle bir aksiyon hiç yok** (kontrol edildi, `base_delete.php` zaten sadece soft-delete) — bu yüzden madde atlandı, yeni bir desen İCAT EDİLMEDİ.

    **Test (curl + gerçek oturum, `/browse` yalnızca modal görünümü için):** Test kaydı oluşturulup soft-delete edildi → `trash_records_list.php` doğru `message` ("Sen bir kayıt sildin: X (Icons Demo)"), `relative_date`, `actor_initial`, `can_restore:true` döndürdü. Editor (`test.admin`) geri yükledi → `{"ok":true}`, DB'de `deleted_at`/`deleted_by` **NULL**, kayıt grid'de TEKRAR göründü (id+metin eşleşti). Aynı kaydı tekrar geri yüklemeyi denedi → `422 | Bu kayıt zaten aktif, geri yüklemeye gerek yok.` Rol reddi: kayıt tekrar silinip commenter fixture ile giriş yapıldı — `trash_records_list.php` `can_restore:false` döndürdü (mesaj da doğru şekilde "Test Admin bir kayıt sildi" oldu, "Sen" değil), backend'e DOĞRUDAN POST → `403 | Bu işlem için yetkiniz yeterli değil.`, `deleted_at` DEĞİŞMEDİ. **7 gün simülasyonu:** aynı kaydın `deleted_at`'i DB'den elle 8 gün öncesine çekildi, `trash_records_list.php` ziyaret edildi → yanıt `items:[]`, DB'de `SELECT COUNT(*) WHERE id=...` **0** (satır GERÇEKTEN silindi), `cell_values` de FK ile temizlendi (0), `audit_log`'da `record.purge` kaydı doğru `table_id`/`reason` ile bulundu. `/browse` ile modal ekran görüntüsü alındı: "BASE'LER"/"KAYITLAR" iki ayrı bölüm, satır (avatar+mesaj+zaman+Geri Yükle) doğru render edildi, düzen bozulmadı. Fixture'lar/test kayıtları temizlendi. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on altıncı adım — Adım 3c'de not düşülen boşluk kapatıldı: `comment_add.php`/`record_send.php` silinmiş kayıtta artık çalışmıyor.** İkisi de `bcc_find_record()` kullanıyordu (3c'de global filtrelenmesi BİLEREK reddedilmişti — `record_soft_delete.php`'nin KENDİSİ deleted_at'i "zaten silinmiş" 422'si için okuyabilmek zorunda) — bu yüzden `require_role()`'dan HEMEN sonra `cell_update.php`/`attachment_upload.php` ile AYNI YEREL kontrol deseni eklendi: `SELECT deleted_at FROM records WHERE id=:id`, doluysa `404 | Kayıt bulunamadı (silinmiş).` `bcc_find_record()`'a yine dokunulmadı. **Test:** soft-delete edilmiş bir test kaydına gerçek oturumla yorum eklemeyi denedi → `404 | Kayıt bulunamadı (silinmiş).`, `comments` tablosunda satır sayısı **0** (eklenmedi). Aynı kaydı `record_send.php`'ye gönderdi (gerçek bir alıcıyla) → **AYNI** `404`, kontrol PHPMailer'a ULAŞMADAN önce çalıştığı için mail HİÇ gönderilmedi. Test kaydı temizlendi. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on yedinci adım — üç küçük/ilgisiz iş birlikte.** (1) **Dokümantasyon:** bu dosyanın "Kalıcı Kurallar" bölümüne `users.is_admin` (platform Admin paneli bayrağı) ile `team_members.role='owner'` (takım rol sistemi) arasındaki farkı hatırlatan kalıcı bir uyarı eklendi (kod değişikliği yok) — önceki bir teşhis turunda bir kullanıcının arayüzde "Admin" rozeti görmesinin `is_admin=1`'den geldiği, takım rolüyle (zaten `owner` idi) ilgisi olmadığı doğrulanmıştı. (2) **Yorum paneli kapanınca içerik genişlemiyor iddiası — canlı test edildi, GERÇEK BİR BUG BULUNAMADI.** `.grid-detail-fields`/`.grid-detail-field-inline`/`.grid-detail-field-value` zaten standart flexbox (`flex:1`/`min-width:0`) kullanıyor; `/browse` ile ölçüldü: panel açıkken `.grid-detail-fields`=500px/`.grid-detail-field-value`=302.8px, `.is-collapsed` sonrası `.grid-detail-fields`=760px/`.grid-detail-field-value`=562.8px — matematik tam oturuyor (150px etiket + 12px gap + 562.8px değer = 724.8px = dolgu çıkarılmış tam genişlik, sağda boşluk YOK). Ekran görüntüsüyle de doğrulandı. **Hiçbir CSS değişikliği yapılmadı** — muhtemelen kullanıcının tarayıcısında eski bir görünüm/önbellek anı vardı (`bcc_asset_url()` zaten mtime ile cache-bust ediyor, `bootstrap.php`). (3) **Kopyalanan link artık kaydı otomatik açıyor.** Önceki format aynen korundu: `grid.php?table_id=N&record_id=M` (copyLinkBtn, değişmedi). `grid-row-detail.js`'te `DOMContentLoaded`'ın SONUNA (tüm diğer bağlamalardan sonra) `URLSearchParams(location.search).get('record_id')` okuyup `getAllDataRows()` içinde AYNI id'li satırı bulan ve bulursa `openDetail()` çağıran ~12 satırlık bir blok eklendi — CSS selector'a id gömülmedi (saf JS karşılaştırma, injection riski yok), kayıt şu an render edilen satırlar arasında yoksa (filtre/sayfalama dışı, silinmiş, yanlış tablo) SESSİZCE hiçbir şey yapılmıyor. `/browse` ile üç senaryo doğrulandı: geçerli `record_id` → modal doğru başlıkla otomatik açıldı (ekran görüntüsü); `record_id` YOK → panel kapalı kaldı (eski davranış korunmuş); var olmayan `record_id` → sessizce no-op, konsol hatası yok. DB/DDL değişikliği yok. `php -l` (grid.php) + `node --check` (grid-row-detail.js) + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on sekizinci adım — long_text (Notlar/Uzun Metin) alanında satır sonu görüntüleme bug'ları düzeltildi, VERİ KATMANINA DOKUNULMADI.** Önceki teşhis turunda DB'nin sağlam olduğu doğrulanmıştı (`cell_values.value_text` gerçek `<br>` etiketleri taşıyor, `\n`/`\r` hiç yok — `long_text` zaten sınırlı bir zengin metin alanı, `bcc_sanitize_rich_text()` yazma anında `<div>`/`<p>`'yi `<br>`'a çeviriyor). **Kök neden (üç yerde AYNI hata):** `grid-row-detail.js`'teki `htmlToPlainText()` (detay modalı textarea'sını dolduran) ve `fieldPrintText()`'in salt-okunur `.cell-view` dalı (hem "Kaydı yazdır" hem "Kaydı gönder" e-posta önizlemesi bunu çağırıyor) `.textContent` kullanıyordu — bu, `<br>` elemanlarını HİÇBİR ayırıcı eklemeden siliyordu ("Merhaba<br>a<br>a" → "Merhabaaa"). **Düzeltme:** TEK bir ortak fonksiyon, `htmlToPlainTextWithLineBreaks(html)` — ÖNCE `<br>`/`<br/>`/`<br />` regex ile `\n`'e çevriliyor, SONRA kalan etiketler (off-DOM `<div>.innerHTML` + `.textContent`) temizleniyor. `htmlToPlainText()`'in TEK çağrı yeri (detay modalı textarea'sı) buna yönlendirildi (gereksiz bir wrapper eklenmedi, fonksiyon doğrudan yeniden adlandırıldı); `fieldPrintText()`'in `cellView` dalı `cellView.innerHTML`'i AYNI fonksiyondan geçiriyor artık. `.grid-detail-print-value`/`.grid-send-preview-value` CSS'i (`white-space: pre-wrap`) zaten doğruydu — kontrol edildi, ekstra CSS gerekmedi, salt JS düzeltmesi üçünü de (detay modalı, yazdır, gönder e-postası — `record_send.php`'nin `nl2br(htmlspecialchars(...))` kullandığı da doğrulandı) TEK seferde kapsadı. **Grid hücresi (ayrı bug, sadece kısa satır yüksekliği):** taban `.cell-view`'ın `white-space:nowrap`'i doğal kelime kaydırmasını engelliyor ama gerçek `<br>`'a etkisi yok — sabit kısa yükseklikte çok-`<br>`'lı bir değer hizasız/kırpık görünüyordu. Orta/uzun/ekstra'nın ZATEN kullandığı `-webkit-line-clamp` deseni (satır 2136, dört özellik de TAM: `display:-webkit-box`+`box-orient:vertical`+`overflow:hidden`+`line-clamp`, eksik yoktu) kısa mod için TEK SATIRLIK hâliyle (`-webkit-line-clamp:1`) yeniden kullanıldı — YALNIZCA `.rich-text-view` (long_text) hedeflendi, taban `.cell-view`'a eklenmedi (`table.grid.row-h-short .cell-view.rich-text-view`). `row-h-short` class'ının GERÇEKTEN DOM'da var olduğu grep ile önce doğrulandı (grid.php `<table class="grid row-h-<?php echo $rowHeight ?>">`, `short` varsayılan değer). **Test (canlı, `record_id=421`, 26 `<br>`'lı gerçek "Notlar" değeri — geçici olarak `test.admin`'e o takıma editor üyeliği eklendi, test bitince silindi, kaydın kendisine HİÇ yazma yapılmadı, DB değeri test öncesi/sonrası byte-byte aynı doğrulandı):** detay modalı textarea'sı → `ta.value` gerçek `\n` içeriyor, ekran görüntüsünde satır satır ("Merhaba / a / a"). "Kaydı yazdır" (`window.print` stub'landı, gerçek dialog açılmadı) → `.grid-detail-print-value.textContent` gerçek `\n` içeriyor. "Kaydı gönder" önizlemesi → aynı, ekran görüntüsünde satır satır. Grid hücresi dört satır yüksekliği modunda da (`row_height=short/medium/tall/extra`) ekran görüntüsüyle doğrulandı: kısa modda temiz tek satır + "...", orta/uzun/ekstra modda 2/4/6 satır + `<br>`'ların doğru sayılıp temiz kesildiği (line-clamp `<br>`'ı zaten doğru işliyormuş, orada değişiklik gerekmedi). Regresyon: aynı satırda sayı/tarih/kullanıcı/dosya/tekli-seçim sütunları dört ekran görüntüsünde de değişmedi; ayrıca `.cell-view` (rich-text-view OLMAYAN) elemanının computed style'ı `white-space:nowrap`/`line-clamp:none` olarak doğrulandı (etkilenmedi). `php -l` + `node --check` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, on dokuzuncu adım — kayıt detay modalındaki long_text/textarea alanları auto-grow oldu (içeriğe göre otomatik yükseklik), SADECE GÖRÜNTÜLEME.** Tek oluşturma yeri `buildFieldWidget()`'in long_text dalı (`grid-row-detail.js`) — projede kaç long_text alanı olursa olsun hepsi bu tek yerden geçtiği için "tek yerden" şartı zaten sağlıydı, ek bir soyutlamaya gerek kalmadı. Ortak `autoGrowTextarea(ta)` fonksiyonu (`ta.style.height='auto'` sonra `scrollHeight+'px'`) eklendi, iki tetikleyici: (1) `input` event (sürekli auto-grow — kullanıcı yazarken de büyümeye devam eder), (2) `openDetail()`'de `overlay.hidden = false`'dan HEMEN SONRA tüm `.grid-detail-textarea`'lar üzerinde tek seferlik çağrı. **Bulunan `[hidden]` tuzağı (canlı, gerçek):** `openDetail()` `renderDetailFields()`'i `overlay.hidden = false`'dan ÖNCE çağırıyor — yani textarea'lar oluşturulduğu anda overlay hâlâ `display:none`, bu ağaçtaki HERHANGİ bir elemanın `scrollHeight`'i o an her zaman `0` döner (layout hiç hesaplanmamış). Açılış auto-grow çağrısı bu yüzden `renderDetailFields()` İÇİNE değil, `overlay.hidden = false`'dan SONRAYA kondu. **Üst sınır — canlı testte bir kez düzeltildi:** ilk seçim `max-height: 60vh` idi; 1280x720 standart bir viewport'ta bu 432px'e denk geliyor, ama gerçek `record_id=421` (26 `<br>`, 27 satır) 561px gerektiriyordu — yani "kaydırmadan tam görünsün" beklenen TAM O kaydı bile kırpardı. `600px` sabit değere geçildi (kullanıcının verdiği iki seçenekten diğeri) — 561px'i rahat karşılıyor, 50+ satırlık aşırı bir değeri (test: 60 satır, gerçek yükseklik 1221px) yine de doğru şekilde sınırlayıp native scrollbar'a düşürüyor. **CSS specificity bug'ı (bulunup düzeltildi, commit edilmeden önce):** `.grid-detail-textarea` (0,1,0) yerine `.grid-detail-field-value .grid-detail-textarea` (0,2,0) kullanılmak ZORUNDAYDI — `textarea.cell-input` (0,1,1, satır ~1546) daha spesifik olduğu için sıradan `.grid-detail-textarea` selector'ıyla yazılan `resize:none` sessizce ezilirdi (önceki `resize:vertical` değeriyle bu çakışma görünmüyordu çünkü ikisi de AYNI değeri yazıyordu). **Elle yeniden boyutlandırma tutamacı (`resize: vertical`) BİLEREK KALDIRILDI** — sürekli auto-grow açıkken elle sürükleme bir sonraki tuş vuruşunda anında ezilir, kafa karıştırıcı olurdu; ayrıca elle büyütme `max-height` güvenlik sınırını da anlamsızlaştırırdı. **Test (canlı, geçici olarak `test.admin`'e team_id=1'e editor üyeliği eklendi, test sonunda silindi):** `record_id=421` (27 satır, 561px) → `ta.style.height` gerçek `\n`'lere göre 561px'e büyüdü, `resize:none` doğrulandı, ekran görüntüsünde iç kaydırma çubuğu YOK (dış panel scroll ile son satıra kadar tam görünüyor). `record_id=424` (tek satır "ABC") → 78px'te kaldı (min-height 5rem tabanına yakın), gereksiz büyümedi. Geçici bir test kaydı oluşturulup (`addRecord()`, sonra hard-delete ile temizlendi) 60 satırlık bir değer JS ile `input` event'iyle yazıldı → `scrollHeight=1221px` ama `clientHeight` 600px cap'te sabit kaldı, `hasInternalScroll:true` — ekran görüntüsüyle üst sınırın doğru devreye girdiği doğrulandı. Diğer alan tipleri (`input[type=date]`/`input[type=number]`/`input[type=text]`) computed style'da `maxHeight:none` — hiç etkilenmedi. Grid görünümü (tablo hücreleri, `.grid-detail-field-value` seçicisi hiç eşleşmiyor) ekran görüntüsüyle değişmediği doğrulandı. Test kaydı/team_members satırı temizlendi, `record_id=421`'in DB değeri (165 byte, 26 `<br>`) test öncesi/sonrası byte-byte aynı. DB/DDL değişikliği yok. `php -l` + `node --check` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, yirminci adım — dosya eki (attachment) alanı: TÜM kutu tıklanabilir + sürükle-bırak eklendi (Airtable "Drop files here or click to browse" paritesi).** Tek oluşturma yeri `buildAttachmentManager()` (`grid.js`) — grid'in KENDİ hücre popover'ı (`startAttachmentEdit`) VE `grid-row-detail.js`'in detay paneli AYNI fonksiyonu paylaştığı için düzeltme ikisini de TEK seferde kapsadı. Kök neden: çıplak `<input type="file">` DOĞRUDAN görünür ekleniyordu — tarayıcının küçük native "Dosya Seç" butonu dışındaki dolgulu kesikli çerçeve dekoratifti, tıklanmıyordu. **Düzeltme:** `fileInput.hidden = true` yapılıp bir `<label class="attachment-dropzone">` içine alındı — native `<label>` davranışı sayesinde kutunun HERHANGİ bir yerine tıklamak dosya diyaloğunu native olarak açıyor, ayrı bir `onclick`/`.click()` JS'i YAZILMADI. Upload mantığı `handleFileSelected(file)` adında küçük bir fonksiyona çıkarıldı (`uploadAttachment()`'a HİÇ dokunulmadı) — hem `change` (native seçim) hem yeni `dragover`/`dragleave`/`drop` event'leri AYNI fonksiyonu çağırıyor, kopya kod yok. Projede daha önce hiç drag-drop altyapısı yoktu (grep ile doğrulandı, sıfır sonuç), bu adımda eklendi. **CSS:** `.attachment-file-input` yerine yeni `.attachment-dropzone` — taban kural (dar 260px `.attachment-popover` bağlamı için, ÖNCEKİ oturumda "bu dar bağlama dokunulmasın" kararı korunuyor — yalnızca DAVRANIŞ düzeltildi, BOYUT büyütülmedi) + `.attachment-manager .attachment-dropzone` override (geniş detay paneli, ÖNCEKİ dashed-box görünümü AYNEN, yalnızca selector adı değişti). `:hover`/`.is-dragover` için `--bcc-accent` border + `--bcc-surface-hover` zemin (theme.css'te zaten var olan değişkenler, yeni sabit renk eklenmedi). **Test (canlı, `record_id=421`, geçici olarak `test.admin`'e team_id=1'e editor üyeliği eklendi, test sonunda silindi):** DOM yapısı doğrulandı — native input `hidden`, `<label>` tüm kutuyu kapsıyor; ekran görüntüsünde küçük native buton YOK, "Dosya seçmek için tıklayın veya buraya sürükleyin" metniyle tam kutu — hem "DOSYA" hem "DOSYALAR" alanı AYNI tutarlı görünümde. Gerçek `click` denendi, tarayıcı native dosya seçme diyaloğunu açmaya ÇALIŞTIĞI için headless modda zaman aşımına uğradı (bu, tıklamanın DOĞRU çalıştığının dolaylı kanıtı — kırık bir buton bu şekilde takılmaz). Sürükle-bırak: `dragover` event'i simüle edilip `.is-dragover` sınıfının eklendiği + border renginin `--bcc-accent`'e (`rgb(45,127,249)`) döndüğü hem computed style hem ekran görüntüsüyle doğrulandı. **Uçtan uca gerçek yükleme:** `DataTransfer` ile gerçek bir PNG dosyası (geçerli magic byte'larla) `change` event'i üzerinden simüle edildi → `attachment_upload.php`'ye gerçek POST gitti, `attachments` tablosunda satır oluştu (`field_id`/`original_name`/`mime_type` doğru), ekranda liste satırı belirdi — backend'e HİÇ dokunulmadığı, akışın (tıkla/sürükle → gizli input → change/drop → handleFileSelected → uploadAttachment) uçtan uca çalıştığı doğrulandı. (Sahte/geçersiz içerikli bir test dosyası önce `422` ile reddedildi — bu da backend doğrulamasının bozulmadığının kanıtı.) Test dosyası (DB satırı + `storage/attachments/` altındaki fiziksel dosya) ve team_members satırı temizlendi; `record_id=421`'in `attachments` sayısı test öncesi/sonrası **0**, `Notlar` alanı byte-byte aynı. DB/DDL değişikliği yok (yalnızca test verisi INSERT/DELETE edildi, temizlendi). `php -l` + `node --check` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, yirmi birinci adım — "Created time"/"Created by" otomatik alan tipleri eklendi (Grup B1, DDL YOK).** Önceki teşhis turunda `records.created_at`/`created_by`'nin sağlam olduğu, `records.updated_at`'in güvenilmez olduğu ve "Last modified by" için hiç kolon olmadığı kanıtlanmıştı (Grup B2 — DDL gerektiriyor, BİLEREK bu adımın kapsamı DIŞINDA bırakıldı, `BCC_FIELD_TYPES`'a bir sonraki adım notu eklendi). **Mimari:** bu 2 tipin değeri `cell_values`'ta HİÇ satır olarak yaşamıyor (`records.created_at`/`created_by`'dan doğrudan okunuyor) — `BCC_FIELD_VALUE_COLUMN`'a `'value_date'`/`'value_number'` eklendi (render fonksiyonlarının `$cellRow` ŞEKLİ için, cell_values'ın kendisi için DEĞİL) ve AYRI, yeni `BCC_RECORD_COLUMN_FIELD_TYPES` haritası (`created_at`/`created_by` — gerçek SQL kolon adları) eklendi; `bcc_build_grid_records_query()`/`filter_condition_sql()` bu ikinci haritada bir field_type bulunca `cell_values`'a LEFT JOIN atmadan doğrudan `r.<kolon>` kullanıyor. `filter_condition_sql()`'in `date`/`user` dalları `created_time`/`created_by`'ı da kapsayacak şekilde genişletildi (aynı karşılaştırma mantığı, kopya kod yok). Yeni ortak `bcc_cell_row_for_field()` — `bcc_render_grid_data_row()` VE `bcc_render_grid_row_fields_json()` İKİSİ DE bu iki tip için `$record`'dan `bcc_group_cell_row()` (zaten var, grup başlığı render'ının da kullandığı AYNI yardımcı) ile bir `$cellRow` taklit ediyor — `cell_raw_value()`/`cell_display_text()`'e yalnızca 2'şer `case` eklendi (created_time tam tarih+saat, `date` gibi güne kısaltmıyor; created_by `user` ile BİREBİR AYNI id→ad çözümü). **Salt-okunur zorlaması:** grid hücresinde `editable` class'ı bu 2 tip için hiç eklenmiyor (`grid.js`'in tıkla-düzenle mantığı YALNIZCA bu class'a bakıyor, ikinci bir JS kontrolü gerekmedi); detay modalında `buildFieldWidget()`'in EN BAŞINA rol fark etmeksizin `buildReadOnlyFieldWidget()`'a yönlendiren bir kontrol eklendi (var olan salt-okunur desen — viewer/commenter için zaten kullanılan AYNI fonksiyon — yeniden kullanıldı). **Backend son söz:** `normalize_cell_value()`'a `case 'created_time': case 'created_by':` → `422 | Bu alan otomatik doldurulur, düzenlenemez.` (frontend engeli bypass edilse bile). **İkon:** yeni SVG çizilmedi — `created_time` saat ikonunu (`time` ile AYNI), `created_by` kişi ikonunu (`user` ile AYNI) selector'a eklenerek paylaşıyor (theme.css). `table_fields.php`/`field_create.php`/`bcc_create_field()`/`record_duplicate.php` grep ile kontrol edildi — hiçbiri `field_type`'a göre dallanmadığı için DOKUNULMADI. **Test (canlı, `test.admin`'e GEÇİCİ olarak owner rolü verildi — alan oluşturma owner-only — test sonunda hem alanlar hem üyelik silindi):** "+" sihirbazından "Oluşturulma zamanı"/"Oluşturan" gerçekten oluşturuldu, grid'de doğru tarih (`30.07.2026 16:06` vb.) ve kullanıcı adı (`Yigit Aslantas`, `created_by=2`'den çözüldü) göründü, `created_by` NULL olan eski kayıtlarda boş göründü (gerçek veri durumu, hata değil) — hücrede `editable` class'ı YOK, tıklayınca hiçbir input açılmadı. Detay modalında `grid-detail-field-value-readonly` class'ıyla düz `.cell-view` metni (input/textarea YOK) ekran görüntüsüyle doğrulandı. Backend bypass: `cell_update.php`'ye doğrudan POST (curl, gerçek oturum+CSRF) → **her iki alan için de `422 | Bu alan otomatik doldurulur, düzenlenemez.`**, DB'de `created_at`/`created_by` DEĞİŞMEDİ, `cell_values`'a sahte bir satır da OLUŞMADI. Sıralama: `created_time` ASC/DESC → DB'deki gerçek zaman sırasıyla birebir eşleşti; `created_by` ASC (NULL önce). Filtre: `created_by equals/empty`, `created_time before/after` → hepsi doğru alt kümeyi döndürdü. Gruplama: `created_by`'a göre grupla → 2 grup, başlıklar "(Boş)" ve "Yigit Aslantas" (`cell_display_text()`'in group-header yolu HİÇ değiştirilmeden otomatik doğru çalıştı). Regresyon: normal bir alan (`Ad`, `single_line_text`) hâlâ `class="grid-cell editable"` — etkilenmedi. Test alanları (`fields` satırları) + `test.admin`'in owner üyeliği temizlendi, `records` satır/alan sayıları test öncesi/sonrası aynı. DB/DDL değişikliği yok (yalnızca `fields` tablosuna 2 satır INSERT/DELETE test için, kalıcı DDL yok). `php -l` + `node --check` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, yirmi ikinci adım — "Last modified time"/"Last modified by" eklendi (Grup B2, GERÇEK DDL: `migrations/013_records_updated_by.sql`).** **DDL:** `records.updated_by INT UNSIGNED NULL` + `fk_records_updated_by FOREIGN KEY ... ON DELETE SET NULL` — `deleted_by` (migrations/012) ile BİREBİR aynı desen, idempotent (`IF NOT EXISTS`/`DROP FOREIGN KEY IF EXISTS`). DDL'den ÖNCE `mysqldump` ile `records` tablosunun tam yedeği alındı (`storage/backups/records_pre_updated_by_20260806.sql`), canlı `bcc_core`'a `mysql` CLI ile uygulandı (008/012/013 ile AYNI yöntem), `schema.sql`'e yansıtıldı. **Doğrulama:** `SHOW CREATE TABLE records` → `updated_by`/FK doğru eklenmiş; DDL öncesi/sonrası kayıt sayısı **17/17 aynı**, tüm satırlarda `updated_by` doğal olarak NULL (veri kaybı yok). **Yeni ortak fonksiyon `bcc_touch_record_modified($recordId)`** (`src/schema.php`, `log_audit()` ile AYNI imza felsefesi — `current_user()` kendisi çağrılır) — `records.updated_at`/`updated_by`'ı elle günceller, 3 "içerik değişikliği" yazma noktasından (`cell_update.php`, `attachment_upload.php`, `attachment_delete.php`) çağrılır, HER ÜÇÜ de `bcc_begin_transaction()`/`bcc_commit()`/`bcc_rollback()` ile sarmalandı (`view_save_state.php`'nin "ikisi ayrı olsaydı tutarsızlık olurdu" gerekçesiyle AYNI). **Bulunan gerçek bug (canlı testte yakalandı, tasarım turunda öngörülmemişti):** `records.updated_at`'in `ON UPDATE CURRENT_TIMESTAMP`'i MySQL'in KENDİ otomatik davranışı — satırın HERHANGİ bir kolonu UPDATE edildiğinde (position kaydırma, soft-delete, restore dahil) `bcc_touch_record_modified()` hiç çağrılmasa bile otomatik olarak bumplanıyordu; bu, "pozisyon/silme/geri yükleme 'değişiklik' sayılmasın" tasarım ilkesini SESSİZCE bozuyordu (canlı test: `record_add.php` ile bir kayıt arayerleştirilince komşu kayıtların `updated_at`'i istenmeden değişti). **Düzeltme:** 4 mevcut yazma noktasının (`record_add.php`/`record_duplicate.php`'nin pozisyon kaydırma UPDATE'i, `record_soft_delete.php`/`record_restore.php`'nin `deleted_at` UPDATE'i) SET listesine `updated_at = updated_at` eklendi — MySQL kuralı: bir kolona AÇIKÇA değer atanırsa (kendi değeri bile olsa) otomatik `ON UPDATE` tetiklemesi devre dışı kalır, DDL gerekmez. **last_modified_by boşken created_by'a düşme (Airtable paritesi):** `bcc_cell_row_for_field()`'e eklenen dala göre `updated_by === null` ise `created_by` kullanılır — TEK yerden (hem grid `<td>` hem detay panelinin `data-fields` JSON'u AYNI fonksiyondan geçtiği için ikisi de otomatik doğru) — YALNIZCA render için, `filter_condition_sql()`/sıralama HAM `updated_by` üzerinde çalışmaya devam eder (`empty` filtresi gerçekten "hiç düzenlenmemiş" anlamına gelir, created_by'a düşmez). **Tip entegrasyonu:** `BCC_FIELD_TYPES`/`BCC_FIELD_VALUE_COLUMN`/`BCC_RECORD_COLUMN_FIELD_TYPES`/`BCC_FILTER_OPERATORS`'a Grup B1 ile BİREBİR aynı desende eklendi (`last_modified_time`→`updated_at`, `last_modified_by`→`updated_by`); `cell_raw_value()`/`cell_display_text()`'te created_time/by ile AYNI case dallarına birleştirildi (kopya kod yok); `normalize_cell_value()`/`buildFieldWidget()`'te AYNI salt-okunur/backend-red deseni genişletildi; ikon `time`/`user` ile paylaşılıyor (yeni SVG yok). **Test (canlı, `test.admin`'e GEÇİCİ owner rolü, curl ile gerçek oturum/CSRF, sonunda hepsi temizlendi):** alan oluşturma → grid'de doğru tarih/kullanıcı. Hücre düzenleme (`Ad` alanı) → DB'de `updated_at`/`updated_by` GERÇEKTEN değişti (`499`/"Test Admin"), grid "Son değiştiren"i doğru gösterdi. Dosya ekleme/silme (gerçek PNG, magic-byte doğrulamasından geçti) → İKİSİ DE `updated_at`'i ayrıca bumpladı. **Pozisyon kaydırma (düzeltmeden ÖNCE ve SONRA test edildi)** → düzeltme ÖNCESİ komşu kayıtların `updated_at`'i yanlışlıkla değişiyordu (bulunan bug), düzeltme SONRASI yeni bir kayıt arayerleştirildiğinde komşuların `updated_at`'i **DEĞİŞMEDİ**, yalnızca yeni kaydın kendi taze zaman damgası vardı. Soft-delete/restore → **DEĞİŞMEDİ** (ikisi de, düzeltme sonrası). Çoğaltma → kopyanın `updated_at`'i kendi `created_at`'ine **eşit** (taze satır). Hiç düzenlenmemiş bir kayıtta "Son değiştiren" → `created_by`'a düştüğü (`raw` JSON'da doğrulandı) canlı doğrulandı. Backend bypass → **her iki alan için `422`**, DB'de iz yok. Transaction atomikliği: gerçek bir orta-transaction hatası tetiklenmedi (kod incelemesiyle kanıtlandı — `bcc_begin_transaction`/`commit`/`rollback` gerçek `mysqli` transaction primitifleri, `mysqli_report(MYSQLI_REPORT_ERROR|STRICT)` HER SQL hatasını exception'a çevirir, `catch` bloğu `bcc_rollback()`'i `json_fail()`'den ÖNCE çağırır — 3 yazmanın (cell_values/records/audit_log) hiçbiri ayrı ayrı commit edilmediği için InnoDB'nin standart atomiklik garantisi geçerli). Tüm test verisi (2 alan, 3 test kaydı, 1 dosya) + `test.admin`'in owner rolü temizlendi; `records` toplam satır sayısı DDL-öncesi/test-sonrası **17/17 aynı**. `php -l` + `node --check` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, yirmi üçüncü adım — Currency/Percent/Rating alan tipleri eklendi (Grup C1, DDL YOK).** **Mimari:** üç tip de `BCC_FIELD_VALUE_COLUMN`'da `value_number`'ı `number` ile PAYLAŞIYOR — yeni kolon, yeni tablo, yeni migration YOK. Fark yalnızca (a) `fields.options` JSON'unda küçük bir config (select tiplerinin `choices`/`colors`'ıyla AYNI kolon, ikinci bir mekanizma değil) ve (b) görüntüleme formatı. `bcc_build_field_options()`'a üç erken dal eklendi (`currency_symbol` 5 karaktere kırpılı + `decimal_places` 0-6 clamp; `percent`: `decimal_places` 0-6; `rating`: `max_rating` 1-10 clamp) — hepsi `ctype_digit` + `min/max` ile savunmacı. `normalize_cell_value()`: `currency` `number`'ın case'ine BİRLEŞTİRİLDİ (gövde birebir aynı, kopya kod yok); `percent` İSTİSNAİ — **Airtable paritesi: kullanıcı "45" yazar, DB'ye ONDALIK 0.45 yazılır**, `cell_raw_value()` ×100 ile geri çevirip edit kutusuna "45" gösterir, `cell_display_text()` ×100 + "%" ile "%45" gösterir (Türkçe yazım: işaret ÖNDE); `rating` `[0, max_rating]` aralığına sunucu tarafında zorlanır ve tam sayıya yuvarlanır. `filter_condition_sql()`'in `number` dalı üç tipi de kapsayacak şekilde genişletildi (ikinci bir karşılaştırma mantığı yazılmadı), `percent` filtrede de ÷100 uygulanır — kullanıcı hücreye "45" yazıp filtrede "0.45" yazmak zorunda kalmasın diye. **`cell_display_text()` imza genişlemesi:** 4. parametre `$options` eklendi (ham JSON string de kabul eder, `select_choices_from_options()`'ın AYNI deseni) — **9 çağrı yerinin 8'i güncellendi**, 9'uncu (`interface.php:299`) tipi sabit `'long_text'` olduğu için bilerek atlandı; `trash_records_list.php` ve `slack.php`'nin birincil-alan SQL'lerine `options` kolonu eklendi, `parse_grid_group_rules()` rule dizisine `'options'` anahtarı eklendi (grup başlığının formatlı görünmesi için, `bcc_build_grouped_tree()`'ye yeni parametre eklemek yerine — `field_type`'ın Grup B2'de eklenme deseniyle AYNI). **Rating widget'ı:** `checkbox` İLE AYNI ruh — hücrede her zaman görünür, doğrudan tıkla-kaydet (`startEdit()`'in input-aç/blur-kaydet akışına HİÇ girmez), hover ile önizleme, **aynı yıldıza tekrar tıklamak puanı temizler (0'a döner — Airtable davranışı)**; grid'de event delegation (satır sayısına göre dinleyici çoğalmaz), detay panelinde kendi kapanış-scope'lu dinleyicileri. `applyCellResultToTd()` rating için `textContent` YAZMAZ (yazsaydı tıklanabilir span'ları yok ederdi), sadece `.rating-star-filled` class'ını günceller. **Yarıda kesilen turda bulunup düzeltilen 4 eksik:** (A) sihirbaz formu `currency_decimal_places`/`percent_decimal_places` gönderiyordu ama `bcc_build_field_options()` ortak `decimal_places` okuyordu — **isim uyuşmazlığı, ondalık ayarı hiç uygulanmıyordu**; okuyucu forma uyduruldu ve iki tipin AYRI adları korundu (gizli bir satırın input'ları da forma dahil olur, ortak ad hangisinin `$_POST`'a gireceğini belirsiz bırakırdı). (B) `field-type-wizard.js` hiç güncellenmemişti — üç yeni satır `hidden` kalıp asla açılmıyordu, sembol/max yıldız girilemiyordu; `optionsRow` ile AYNI desende üç satır eklendi. (C) `.rating-star`/`.rating-view` için CSS yoktu (yıldızlar renksiz düz metindi); dolu=altın `#f5b301` / boş=gri `#dadce0`, `rating-view-editable` içinde `cursor:pointer` eklendi — "boş" yıldız ayrı bir `☆` glifi DEĞİL, aynı `★`'ın soluk rengi (genişlik kaymaz, hover titremez). (D) `table_fields.php` düzenleme formunda bu üç tipin input'ları YOKTU — **mevcut bir currency alanının yalnızca ADINI değiştirmek bile `$_POST`'ta sembol bulunmadığı için options'ı sessizce varsayılana (₺, 2) sıfırlıyordu**; kayıtlı options'tan ön-doldurulan input'lar eklendi, adlar oluşturma formuyla BİREBİR aynı. **Test (canlı, yeni `scripts/_verify_group_c1.php` — 54/54, kendi verisini kurar/temizler + tarayıcıyla görsel doğrulama):** özel ayarlarla (`$`/3 basamak, `%`/1 basamak, max 7 yıldız) alan oluşturma → options JSON doğru, gizli satırların input'ları birbirine KARIŞMADI. Currency `1234.5` → `$1.234,500`, DB'de ham `1234.5`. Percent `45` → **DB'de `0.45` (SELECT ile doğrulandı)**, ekranda `%45,0`, edit kutusunda `45`. Rating `5` kabul, `9` (max 7 dışı) ve `-1` **REDDEDİLDİ** (`422`, sunucu tarafı — istemci engeli bypass edilse bile). Grid'de 7 yıldız span, 5'i dolu, `data-options="{"max_rating":7}"`. Tarayıcıda: hover 4. yıldızda 4 yıldızı geçici doldurdu (gerçek değer 0 kaldı), tıklayınca DB'ye `4` yazıldı ve `records.updated_by`/`updated_at` de tetiklendi (Grup B2 mekanizması rating ile de çalışıyor), aynı yıldıza tekrar tıklayınca **0'a döndü**, span'lar yeniden çizilmedi (7 span duruyor), konsol hatası yok. Detay panelinde 7 yıldız/5 dolu, 3. yıldıza tıklayınca DB `3` oldu; Fiyat kutusu ham `1234,5`, Oran kutusu `45` (kullanıcı ölçeği) gösterdi. Sihirbazda tip seçimi doğru satırı açtı (currency→sadece currency satırı) ve **regresyon: `single_select` seçilince hâlâ eski `options-row` açıldı, üç yeni satır kapalı kaldı**; UI'dan gerçek submit ile `CHF`/1 basamak kaydedildi. **Madde D testi:** düzenleme formu kayıtlı değerleri ön-doldurdu, **sadece ad değiştirilip kaydedilince options KORUNDU** (üç tip için de), ayar gerçekten değiştirilince (`€`/0) doğru kaydedildi. Filtre: currency `>100`, percent `>50` (kullanıcı ölçeği, DB'deki 0.80 ile eşleşti), rating `>=5` → hepsi doğru alt küme. Sıralama: currency artan, rating azalan doğru. Gruplama: rating başlığı yıldızlı, currency başlığı `€` formatlı. Export (xlsx): üç format da `sheet1.xml` içinde (`inlineStr`, sharedStrings yok). Çöp kutusu: birincil alanı currency olan silinmiş kayıt `$9.876,50` olarak listelendi. `interface.php`: dördü de (currency/percent/rating/number) doğru render edildi. **Regresyon:** `number` alanı hâlâ formatsız düz sayı (`10`, sembol/yüzde YOK), `created_time`/`last_modified_time` tarih+saat ve salt-okunur (`editable` class YOK), `created_by`/`last_modified_by` kullanıcı adı — hepsi ETKİLENMEDİ. Tüm test verisi (2 test kullanıcısı, 4 tablo, base'ler) temizlendi, DB'de kalıntı YOK. `php -l` + `node --check` + **6/6+8/8+19/19+54/54** regresyon yeşil.
  - **Aynı gün, yirmi dördüncü adım — Autonumber alan tipi eklendi (Grup C2, GERÇEK DDL: `migrations/014_fields_autonumber_next.sql`).** **DDL:** `fields.autonumber_next INT UNSIGNED NOT NULL DEFAULT 1` — alan BAŞINA bağımsız sayaç, idempotent (`ADD COLUMN IF NOT EXISTS`), FK/index YOK (sayaç bir referans değil, yalnızca birincil anahtarla tek satır okunuyor). DDL öncesi `mysqldump` ile `fields` tablosunun tam yedeği alındı (`storage/backups/fields_pre_autonumber_20260807.sql`, 23 satır); temizlik sonrası canlı tablo yedekle **bire bir** doğrulandı (8 kolonluk parmak izi `3bb77503…`, tek fark yedeğin `SET TIME_ZONE='+00:00'` ile UTC yazması — +3sa kaydırınca birebir eşleşti). **Neden yeni kolon:** `records.id` global AUTO_INCREMENT (bir tablonun ilk kaydı 1 değil 1307 olurdu), `records.position` sürükle-bırakla kayar (autonumber bir kayda BİR KEZ atanıp asla değişmemeli), `MAX(value_number)+1` silmede geri sarar ve yarışır. **Atomiklik:** MySQL/MariaDB'nin kendi belgelenmiş sequence idiomu — `UPDATE fields SET autonumber_next = LAST_INSERT_ID(autonumber_next) + 1` tek ifadede hem numarayı ayırır hem sayacı ilerletir; `LAST_INSERT_ID(expr)` BAĞLANTIYA ÖZELDİR, izolasyon seviyesinden bağımsız doğrudur. ⚠️ Footgun belgelendi: bu fonksiyon oturumun last-insert-id'sini EZER, bu yüzden dört çağrı yerinin dördünde de `bcc_last_insert_id()` INSERT'in HEMEN ardından çağrılıyor. **Tek fonksiyon `bcc_assign_autonumbers()`, DÖRT oluşturma noktası:** `record_add.php`, `grid.php` create_record, `record_duplicate.php`, `table_import_xlsx.php` — `grid.php`'nin create_record dalına transaction EKLENDİ (öncesinde hiç yoktu; tek INSERT olduğu için sorun değildi, ama sayaç UPDATE'i + hücre INSERT'i eklenince üçü ayrı commit edilirse numara "yanardı"). **Backfill (`bcc_backfill_autonumber_field()`):** dolu bir tabloya autonumber eklenince mevcut kayıtlar `ROW_NUMBER() OVER (ORDER BY r.position, r.id)` ile görünür sırayla numaralanır (`SET @n := @n+1` hilesi BİLEREK kullanılmadı — INSERT...SELECT + ORDER BY'da değişken değerlendirme sırası belgelenmiş biçimde güvenilmez); silinmiş kayıtlar da numaralanır (geri yüklenince boş kalmasın); yalnızca numarası OLMAYAN kayıtlar doldurulur ve sayaç `GREATEST(...)` ile monoton, böylece autonumber → number → autonumber çevriminde eski numaralar KORUNUR ve çakışma olmaz. İKİ yerden çağrılır: `bcc_create_field()` ve `table_fields.php` update_field (tip çevirisi); ikisi de transaction'a sarıldı. **Salt-okunur zorlaması üç katmanda:** grid hücresinde `editable` class'ı hiç eklenmez, `grid-row-detail.js` salt-okunur widget'a düşer, `normalize_cell_value()` doğrudan `cell_update.php` isteğini 422 ile reddeder. **Çoğaltma:** kopya orijinalin numarasını TAŞIMAZ, taze numara alır — `$excludeIds` (attachment ile AYNI mekanizma) + birincil alan autonumber olduğunda tekil INSERT dalı da atlanır (yoksa `" copy"` eki eklenmese bile NUMARA taşınırdı). **Bulunan gerçek buglar:** (1) `table_import_xlsx.php` — `view_export_xlsx.php` autonumber sütununu DA dışa aktardığı için "dışa aktar → düzenle → içe aktar" turunda o sütun geri geliyordu; her satırda normalize reddi "N hücre atlandı" uyarısını şişiriyor, alan `is_required=1` ise `$filledFieldIds`'e hiç giremediği için TÜM satırlar eleniyor ve içe aktarım SIFIR kayıtla bitiyordu → `attachment` ile aynı mekanizmaya alındı (`$importIgnoredFieldTypes`). (2) `is_required` autonumber'da artık her zaman 0'a zorlanıyor (`bcc_normalize_is_required()`, iki çağrı yeri) — kullanıcının asla dolduramayacağı bir alan "zorunlu" görünmesin; sihirbazda JS ile, düzenleme formunda sunucuda gizlenir. (3) `record_add.php`/`record_duplicate.php` yeni satır HTML'ini `array('id' => ...)` ile render ediyordu; `bcc_cell_row_for_field()` `created_at`/`created_by`/`updated_at`/`updated_by`'a korumasız eriştiği için B1/B2 tipleri olan bir tabloda yeni satır BOŞ render ediliyordu → artık gerçek satır DB'den okunuyor. (4) `theme.css` — Grup C1'in üç tipi (`currency`/`percent`/`rating`) için `--field-icon` hiç tanımlanmamıştı ve `background-image`'ın yedeği olmadığı için rozetleri BOŞ kutu olarak çiziliyordu; dördü (autonumber dahil) birlikte tanımlandı. **Test:** yeni `scripts/_verify_group_c2.php` — **66/66 GEÇTİ** (15 paralel istekte çakışma/boşluk YOK, soft-delete sonrası sayaç geri sarmıyor, tip çevrimi, iki bağımsız sayaç aynı tabloda, xlsx import + export→import turu, birincil alan autonumber, filtre/sıralama, backend bypass 422, C1 ve B1/B2 regresyonu). `php -l` + `node --check` + **6/6+8/8+19/19+54/54** regresyon yeşil.
  - **Aynı gün, yirmi beşinci adım — URL/E-posta/Telefon alan tipleri eklendi (Grup A, DDL YOK) + `BCC_GROUP_DIR_LABELS` mevcut bug'ı düzeltildi.** **Mimari (teşhis turunda ölçülüp seçilen UCUZ YOL):** üçü de `value_text`'i `single_line_text` ile PAYLAŞIYOR — yeni kolon/tablo/migration YOK ve **`cell_display_text()` imzası HİÇ DEĞİŞMEDİ, 9 çağrı yerinin hiçbirine dokunulmadı** (C1'in aksine). Kritik karar: `cell_display_text()` bu üç tip için de DÜZ METİN döndürmeye devam ediyor, çünkü çıktısı `view_export_xlsx.php` (Excel), `src/slack.php` (bildirim), `grid.php` (grup başlığı) ve `trash_records_list.php`'ye de gidiyor — oralara HTML sızmamalı. Linkleştirme YALNIZCA gerçekten HTML üreten iki noktada: `bcc_render_linkified_cell()` (grid `<td>`) ve `cell_update.php`'nin `display_link` yanıtı. **Doğrulama YUMUŞAK — hiçbir giriş reddedilmez** (`normalize_cell_value()` üçünü de `single_line_text` dalında işler): katı doğrulamanın bedeli import'a binerdi (`table_import_xlsx.php` normalize reddini "N hücre atlandı" diye sessizce düşürür) ve güvenlik için gerekmez, çünkü linkleştirme whitelist tabanlı; ayrıca `filter_var(FILTER_VALIDATE_EMAIL)` IDN/Unicode adresleri (`üye@örnek.com`) reddeder — Türkçe bir üründe gerçek kayıp. Kontroller sadece LİNKLEŞTİRME KAPISI (`bcc_cell_link_href()`): URL yalnızca `^https?://` ile başlıyorsa (çıplak alan adına otomatik `https://` EKLENMEZ, kapsam dışı), e-posta `filter_var` geçerse `mailto:`, telefon `+`/rakamlara indirgenip ≥7 rakam kalırsa `tel:` — geçmezse düz metin, ikon yok. Telefonda SAKLANAN değer kullanıcının yazdığı hâliyle kalır, indirgeme yalnızca href içindir. **Link kurma tek noktada:** zengin metnin (`bcc_sanitize_rich_text_node`) mevcut `<a>` bloğu KOPYALANMADI, `bcc_build_safe_link()`'e çıkarıldı ve ikisi de oradan geçiyor; şema whitelist'i parametre — zengin metnin VARSAYILANI (yalnızca `http/https`) bilerek KORUNDU çünkü orada href'i KULLANICI yazar, hücre linkleri (`BCC_CELL_LINK_SCHEMES`) `mailto`/`tel`'i de içerir çünkü orada href'i kullanıcı değil sunucu KURAR. **UX kararı — hover'da aç ikonu:** metnin KENDİSİ `<a>` DEĞİL; link ayrı bir ikon olarak sağda durur (`visibility:hidden`, `td:hover`'da görünür — `display:none` değil ki hover'da metin kaymasın). Böylece hücreye tıklamak HER ZAMAN düzenlemeyi açar (diğer tüm hücrelerle tutarlı), yalnızca ikona tıklamak yeni sekmede linki açar (`grid.js`'te `e.stopPropagation()`). İkon SVG'si mevcut Feather ikon setiyle aynı çizim dili, yeni kütüphane YOK. **İstemci:** `cell_update.php` `display_link: {href, text}` döndürür (`display_chips` ile AYNI desen); şema whitelist'i SUNUCUDA uygulanır, istemciye yalnızca zaten güvenli href gider. Değer linkleştirilemiyorsa anahtar `null` olarak YİNE gönderilir — hiç gönderilmemesi bug olurdu (geçerli bir URL'yi "abc" yapan kullanıcıda eski ikon ekranda asılı kalırdı). `grid.js` ikonu `createElementNS` ile kurar — **yeni kodun hiçbir yerinde `innerHTML`/string birleştirme YOK**. **Düzenlenebilirlik:** Grup B/C'nin salt-okunur zorlaması burada YOK — `$isReadOnlyFieldType`, detay panelinin salt-okunur listesi ve otomatik-tip red bloğu DEĞİŞTİRİLMEDİ (üçü zaten varsayılan olarak düzenlenebilir); `buildInput()`'a mobil klavye için `type=url/email/tel` eklendi (detay modalı aynı fonksiyonu paylaştığı için bunu bedava aldı). Detay modalının salt-okunur yolu canlı `<td>`'nin `innerHTML`'ini kopyaladığı için ikonu da bedava alıyor (CSS'te o bağlamda her zaman görünür yapıldı). **Aynı turda düzeltilen MEVCUT BUG:** `BCC_GROUP_DIR_LABELS` (`src/schema.php`) Grup B1/B2/C1/C2 ile eklenen SEKİZ tipin hiçbirini içermiyordu ama `public/grid.php` bu diziye KORUMASIZ erişiyordu; gruplama paneli yalnızca `attachment`'ı elediği için bu tiplerden birine göre gruplamak "Undefined index" notice'ı + BOŞ yön etiketleri üretiyordu (C2 test kapsamının dışında kalmıştı — filtre ve sıralama test edilmiş, gruplama edilmemişti). İki katmanlı düzeltme: eksik girişler tamamlandı (`attachment` dışında TÜM tipler) + `grid.php`'deki okuma `isset()` ile korundu, böylece gelecekte eklenecek bir tip diziye yazılmayı unutursa notice yerine makul varsayılana düşer. Ayrıca `filter_condition_sql()`'in `$isTextLike` dizisine üç yeni tip eklendi (yoksa `empty`/`not_empty` yalnızca `IS NULL` bakıp `''` hücrelerini sessizce kaçırırdı) ve `theme.css`'e üç `--field-icon` yazıldı (C1'in düştüğü tuzak tekrarlanmasın diye AYNI turda). **Test:** yeni `scripts/_verify_group_a.php` — **79/79 GEÇTİ**. **XSS (vurgu):** `javascript:alert(1)` değeri KABUL edilir (yumuşak doğrulama) ama HİÇ linkleşmez — hücrede tek bir `<a>` bile üretilmez, değer kaçırılmış düz metin kalır, `display_link` `null` döner (güvensiz href istemciye HİÇ gitmez) ve sayfanın tamamında `href="javascript:` dizgisi bulunmaz; `a@b.com" onmouseover="alert(1)` ile attribute kaçışı denendi → `ENT_QUOTES` ile `&quot;` olarak kaçırıldı, olay dinleyicisi enjekte OLMADI; `<script>` etiketi kaçırıldı. Zengin metin whitelist'i GENİŞLEMEDİ (regresyon: `mailto:` ve `javascript:` long_text'te hâlâ soyuluyor). Excel çıktısında ve Slack metninde HTML/`<a href` SIZMADI. `empty`/`not_empty` filtresi hem NULL hem `''` kaydı buldu. **Tarayıcı testi (gerçek Chromium):** geçerli satırda 3 ikon / geçersiz satırda 0 ikon; ikon rest'te `visibility:hidden`, hover'da `visible` ve yer kapladığı için metin KAYMADI; hücre metnine tıklayınca `type="url"` input'u doğru değerle açıldı; İKONA tıklayınca düzenleme AÇILMADI ve sayfa değişmedi; hücreyi `abc` yapınca ikon CANLI olarak kaldırıldı, geri `https://yeni.example.org` yapınca ikon `href`/`target=_blank`/`rel=noopener noreferrer`/gerçek `<svg>` düğümüyle geri geldi ve `cell-view-linkified` class'ı uygulandı. Tüm test verisi temizlendi (`fields` 23 satır, `%@bcc-test.local` kullanıcısı 0). `php -l` + `node --check` + **6/6+8/8+19/19+54/54+66/66** regresyon yeşil.
  - **Aynı gün, yirmi altıncı adım — çoğaltmadaki `" copy"` eki KOLON bazlıdan TİP WHITELIST'ine çevrildi (DDL YOK); dört ayrı bug tek kod yoluyla kapandı.** **Teşhis:** `record_duplicate.php` eki `$primaryColumn === 'value_text'` koşuluyla veriyordu — ama `value_text`'i **yedi** tip paylaşıyor (`single_line_text`, `long_text`, `single_select`, `time`, `url`, `email`, `phone`) ve bunların çoğunun bir BİÇİM SÖZLEŞMESİ var. Serbest metne `" copy"` eklemek zararsızken, biçimli bir değere eklemek onu bozuyordu. Canlı ölçümle (gerçek Chromium'da `a.href` okunarak) doğrulanan dört bug: (1) **`url` — en zararlısı, çünkü hata GÖRÜNMÜYORDU:** `"https://example.com copy"` linkleştirme regex'ine (`^https?://`, yalnızca BAŞA çapalı) HÂLÂ uyuyor, yani link ÜRETİLİYOR ve ikon çıkıyordu; tarayıcı ise boşluğu `%20` olarak **HOST'a** katıyordu (`host = "example.com%20copy"`, `resolved = "https://example.com%20copy/"`) → kullanıcı sessizce **var olmayan bir alan adına** gidiyordu. (Yol içeren URL'lerde boşluk path'e giriyor ve 404 oluyordu — yani hata URL'nin şekline göre değişiyordu, ama her hâlükârda yanlış hedef.) (2) **`email`:** `"user@example.com copy"` `FILTER_VALIDATE_EMAIL`'i geçemiyor, `mailto:` linki tamamen kayboluyordu (görünür kayıp, sessiz değil). (3) **`single_select`:** `"Seçenek copy"` alanın `choices` listesinde OLMAYAN bir değer — DB'ye geçersiz bir seçim yazılıyordu. (4) **`time`:** `"14:30 copy"` geçerli bir saat değil. (`phone` tek zararsız olanıydı — rakam indirgemesi `" copy"`yi zaten atıyor, `tel:` href'i değişmiyordu; tutarlılık için o da artık ek ALMIYOR.) **Çözüm:** karar artık "hangi kolonda duruyor" değil **"biçim sözleşmesi var mı"** — yeni `$GLOBALS['BCC_DUPLICATE_SUFFIX_FIELD_TYPES'] = array('single_line_text', 'long_text')` whitelist'i. Tipe özel dal YAZILMADI; dört bug'ın dördü de AYNI tek satırla kapandı ve `$primaryColumn` değişkeni tamamen kaldırıldı. Whitelist bilinçli olarak "unutulunca ek almaz" tarafına düşüyor — yanlış ek eklemek eksik ek eklemekten çok daha zararlı. `record_duplicate.php`'nin başlığındaki tip listesini elle sayan yorum da (Grup A'dan sonra bayatlamıştı) güncellendi. **Test:** yeni `scripts/_verify_duplicate_suffix.php` — **27/27 GEÇTİ**: `url`/`email`/`phone`/`time`/`single_select` birincil alanlarında kopya değeri DEĞİŞMİYOR ve link/saat/seçim hâlâ geçerli (kopya satırının HTML'inde `href="https://example.com"` var, `href="https://example.com copy"` YOK); **regresyon** olarak `single_line_text` ve `long_text` `" copy"` ekini HÂLÂ alıyor, boş birincil değerde whitelist'teki tip `"copy"` alırken `url` BOŞ kalıyor; **Grup C2 regresyonu** olarak birincil alan `autonumber` iken kopya hâlâ taze numara alıyor ve `value_text`'e `"copy"` sızmıyor. `php -l` + `node --check` + **79/79+54/54+66/66+19/19+8/8+6/6** regresyon yeşil.
  - **Aynı gün, yirmi yedinci adım — Görünüm türü ortak temeli + Form görünümü (Grup View-Form, GERÇEK DDL: `migrations/015_views_form_token.sql`).** **Önceki teşhis:** `views.view_type` kolonu VARDI ama TAMAMEN ÖLÜYDÜ — hiçbir yerde okunup dallanmıyor, üç yerde sabit `'grid'` yazılıyordu; tip seçici arayüz, whitelist ve alternatif render sayfası YOKTU. **DDL:** `views.form_token CHAR(32) ascii_bin NULL` + `form_enabled TINYINT(1) NOT NULL DEFAULT 0` + `UNIQUE uq_views_form_token`. `form_token` ŞART çünkü herkese açık linkin `views.id` ile kurulması KVKK ekip izolasyonunu DOĞRUDAN delerdi (id ardışık AUTO_INCREMENT — 405/404/403 denemek BAŞKA TAKIMLARIN formlarını bulmaya yeterdi). Token `views.config` JSON'una DEĞİL ayrı kolona konuldu çünkü token ile SATIR ARANIYOR (`WHERE form_token = ?`) ve JSON içi anahtar indekslenemez. `ascii_bin`: hex zaten ASCII (utf8mb4 karakter başına 4 bayt ayırıp index'i şişirirdi), ikili karşılaştırma tam eşleşme verir. NULL serbest + UNIQUE: MySQL/MariaDB çoklu NULL'a izin verdiği için 10 mevcut grid görünümü çakışmaz. DDL öncesi `mysqldump` yedeği alındı (`storage/backups/views_pre_form_token_20260807.sql`, 10 satır); uygulama sonrası satır sayısı **10/10** ve 9 kolonluk içerik parmak izi **birebir aynı** (`4bdf1b42fd9fdc5a0d54ccb5935eae92`). ⚠️ **`form_enabled` için özel gerekçe:** bu tabloda TAM OLARAK bu hata bir kez yapıldı — `views.is_published` eklenip hiç kullanılmadan `migrations/009` ile silinmişti. Bu yüzden kolonun gerçekten okunduğu ÜÇ nokta migration yorumunda baştan taahhüt edildi ve uygulama sonunda grep ile doğrulandı: `form.php:31` (kapalı form render EDİLMEZ), `form_submit.php:41` (kapalı forma gönderim REDDEDİLİR), `form_edit.php:189/204` (anahtarın durumu) + `view_create.php:70` ve `form_edit.php:119` yazma. **Ortak temel:** `$GLOBALS['BCC_VIEW_TYPES']` whitelist'i (`grid`/`form`, `BCC_FIELD_TYPES` deseniyle — ENUM değil, yeni tür DDL gerektirmez) + `bcc_view_route_for()` TEK yönlendirme noktası (bilinmeyen tür `grid`e düşer, fail-safe). "+ Yeni oluştur..." tek butondan `<details>` TİP SEÇİCİye çevrildi; liste `BCC_VIEW_TYPES`'tan DÖNER, elle yazılmaz — Kanban/Calendar eklenince menü kendiliğinden büyür. `view_create.php` artık tipi whitelist'e karşı doğruluyor (uymayan `422`), adı türe göre numaralıyor ("Form 1"), form ise AYNI transaction'da `random_bytes` ile token üretiyor ve `redirect_url`'i SUNUCUDAN döndürüyor (istemci artık `/grid.php?...` dizgisini kendi kurmuyor). `grid.php`'ye erken yönlendirme: gelen `view_id` grid değilse 302 (eski linkler/yer imleri doğru sayfaya düşer). **Salt-okunur liste merkezileştirildi:** `bcc_render_grid_data_row()` içindeki satır içi literal `$GLOBALS['BCC_READONLY_FIELD_TYPES']`'a çıkarıldı (davranış birebir aynı) — form da AYNI listeyi okuyor, ikinci bir kopya YOK. **Form mimarisi — grid.php içinde erken dal DEĞİL, İKİ AYRI DOSYA:** `form_edit.php` (oturumlu, editor+, tasarımcı: alan seçimi/başlık/açıklama/teşekkür metni/Slack anahtarı/aç-kapat/link kopyalama) ve `form.php` (ANONİM doldurucu). Gerekçe: `grid.php` 1400+ satır ve tamamı tablo varsayımı üzerine kurulu; ayrıca anonim erişimi `require_role('editor')` çağıran bir dosyaya sokmak "bu satıra kim ulaşabilir" sorusunu her `if` için yeniden sormak olurdu. **Formda gösterilmeyen tipler (üç katmanlı filtrenin birinci katmanı, `bcc_field_allowed_in_form`):** beş salt-okunur tip + `attachment` (anonim dosya yükleme ayrı ve çok daha riskli) + `long_text` (sanitize edilmiş HTML → depolanmış XSS yüzeyi) + **`user`** (tasarım turunda gözden kaçmıştı, uygulama sırasında yakalandı: değeri bir `users.id` ve `normalize_cell_value()` onu EKİBİN ÜYE LİSTESİNE karşı doğruluyor — anonim doldurucuya seçenek sunmak ekip üyelerinin ad/id'lerini SIZDIRMAK olurdu). **`form_submit.php` — projenin İLK kimlik doğrulamasız yazma uç noktası.** Yedi kapı, hepsi SUNUCUDA: (1) token biçimi+DB (`view_type='form'` VE `form_enabled=1`), (2) honeypot, (3) zaman-bazlı HMAC nonce, (4) `form_fields` whitelist'i (İSTEMCİDEN gelen alan listesine ASLA güvenilmez), (5) tip filtresi (config kurcalanmış olsa bile), (6) `normalize_cell_value()` (kopya doğrulama YAZILMADI), (7) `is_required`. `created_by = NULL` (kolon zaten nullable), `log_audit()` DEĞİŞTİRİLMEDEN çalışıyor (`current_user()` null → `user_id` NULL; `audit_log.user_id` nullable, hâlihazırda 1250 satır NULL — ek DDL gerekmedi). **Klasik CSRF token'ı BİLEREK YOK:** CSRF kurbanın YETKİSİNİ kötüye kullanmaktır; burada gönderen zaten yetkisiz ve form zaten herkese açık, ayrıca `csrf_token()` `$_SESSION` gerektirdiği için her bot ziyaretine bir oturum dosyası açardı. Yerine "otomasyonu pahalılaştıran" iki katman: honeypot (`display:none` KULLANILMADI — bazı botlar onu bilerek atlar; `position:absolute; left:-9999px` + `aria-hidden` + `tabindex=-1`) ve durumsuz HMAC nonce (render↔gönderim arası <3sn ise reddedilir). Bot yakalanınca HATA DÖNMEZ, "başarılı" döner ama kaydetmez — "yakalandın" geri bildirimi saldırganın honeypot'u tespit etmesini sağlardı. **Slack bildirimi form gönderimlerinde VARSAYILAN KAPALI** (anonim spam ekibin kanalına taşmasın; tasarımcı açıkça açmalı). `form.php` hiçbir tablo/base/ekip meta verisi sızdırmaz, `robots noindex` taşır ve geçersiz token ile kapalı form AYNI 404 metnini gösterir (ayrım saldırgana bilgi verirdi). **`bcc_assign_autonumbers()` artık BEŞ çağrı yerine sahip** — `form_submit.php` beşincisi; Grup C2'de "DÖRT çağrı yeri" diyen üç yorum da beşe güncellendi. **⚠️ BİLİNEN SINIR (kodda ve burada açıkça belgeli):** nonce TEK KULLANIMLIK DEĞİL — durumsuz olduğu için geçerli bir nonce 2 saat boyunca defalarca kullanılabilir; "sayfayı hiç açmadan POST'layan" saldırganı engeller, "bir kez açıp döngüye sokan"ı engellemez. Tek kullanımlık yapmak sunucu tarafında durum gerektirir — bu, onaylanan kapsamda BİLEREK dışarıda bırakılan Katman 3'ün (IP rate-limit tablosu) işidir. Spam gerçekleşirse sıra: (1) formu kapat, (2) Katman 3'ü ekle. **Test:** yeni `scripts/_verify_view_form.php` — **77/77 GEÇTİ**, tamamı OTURUMSUZ (çerezsiz) isteklerle: sahte nonce `422`, 3sn'den hızlı gönderim `422`, honeypot dolu → başarılı yanıt AMA kayıt OLUŞMADI, zorunlu alan boş `422`, whitelist dışı `field_id` enjeksiyonu (`long_text`/`user`/`created_by`/`attachment`) → o alanlara HİÇBİR ŞEY yazılmadı, `form_enabled=0` → hem sayfa hem gönderim `404`, geçersiz token `404`, **`view_id` ile erişim ÇALIŞMIYOR** (ardışık id keşfi kapalı), başarılı gönderimde `created_by` NULL + `audit_log.user_id` NULL + doğru autonumber. `php -l` + `node --check` + **27/27+79/79+54/54+66/66+19/19+8/8+6/6** regresyon yeşil.
  - **Aynı gün, yirmi sekizinci adım — "+ Yeni oluştur..." YÜZEN açılır menü oldu + sütun başlığı menüsünün dışarı-tık bug'ı AYNI kök nedenle düzeltildi (DDL YOK, tamamen istemci katmanı).** **Kök neden TEKTİ:** `grid-table-tabs.js` tüm `<details name="gs-table-tab-menu">` grubunun kapanmasını (karşılıklı dışlama + dışarı tık + Escape) yönetiyor ama seçicisi SINIF ADI listesiydi (`.gs-table-tab-menu, .gs-tool-details`). `grid.php`'de bu `name`'i taşıyan 16 `<details>`ten ikisi listede yoktu: **`.grid-th-menu`** (sütun başlığı ▾ — Sırala/Filtrele/Grupla, bildirilen bug) ve **`.gs-view-create-menu`** (Grup View-Form ile eklenen tip seçici). `grid-column-menu.js`'nin yorumu bunu zaten itiraf ediyordu ("bu grupta dışarı-tık dinleyicisi zaten hiçbirinde yok") — yorum yazıldığında doğruydu, sonra grup yönetimi eklendi ama o sınıf seçiciye alınmadı. Bu, aynı hatanın **ÜÇÜNCÜ** tekrarıydı (dosyanın kendi yorumu ikincisini anlatıyor: toolbar panelleri de bir kez dışarıda kalmıştı). **Düzeltme sınıf eklemek DEĞİL:** seçici, grup üyeliğini ZATEN tanımlayan özniteliğe çevrildi — `document.querySelectorAll('details[name="gs-table-tab-menu"]')`. Böylece ileride eklenecek hiçbir menü listeye yazılmayı unutamaz, dördüncü tekrar mümkün değil. **Değişikliğin tamamen EKLEMELİ olduğu tarayıcıda ölçüldü:** eski seçici 14 eleman, yeni seçici 18; **kaybolan 0**, kazanılan tam olarak `gs-view-create-menu` + 3× `grid-th-menu`. **Yüzen konumlandırma üçüncü kez kopyalanmadı:** `position:fixed` + açılışta hesapla + scroll'da tazele bloğu `grid-add-field.js` ve `grid-column-menu.js`'de BİREBİR duruyordu (ikisinin yorumu da "AYNI teknik" diyordu); ortak `bcc_bindFloatingPanel()` yardımcısına çıkarıldı (`dismissable-panel.js`, `bcc_bindDismissable`'ın yanına) ve her iki eski çağrı yeri ile yeni create menüsü buna bağlandı. `position:fixed` zorunlu çünkü paneller `overflow:auto` taşıyan atalarda (`.grid-wrap`, `.gs-view-drawer`) — absolute konumlandırma onları kırpardı. **Yardımcıya taşınırken bulunan gerçek bug:** iki eski kopyanın İKİSİNDE DE `resize` dinleyicisi YOKTU — pencere yeniden boyutlandırılınca panel bağlandığı butondan kopuyordu; ortak yardımcıya eklendi, yani düzeltme iki eski çağrı yerine de bedava geldi. Viewport taşma koruması (`grid-column-menu.js`'de bulunmuş "en sağdaki sütunda panel ekran dışına taşıyor" bug'ı) artık TÜM yüzen panellerde geçerli. **Test (gerçek Chromium):** panel `position:fixed`, buton altında (top 197 / buton bottom 193), **alttaki görünüm listesi YERİNDE kaldı** (top 249 → 249, itilmedi), dışarı tık kapattı, Escape kapattı, "Form" seçilince hem görünüm oluştu (`view_type=form`, `form_enabled=1`, token üretildi) hem `form_edit.php`'ye yönlendirildi, karşılıklı dışlama çalıştı (create açıkken sütun menüsü açılınca create kapandı), **sütun başlığı menüsü artık dışarı tıkla kapanıyor** (bildirilen bug), Sort paneli regresyonu yeşil. **375px dar ekran:** create paneli taşmadı (left 62/right 262), sütun paneli tam `375−8−210=157`'ye kelepçelendi. ⚠️ **Test yöntemi notu:** `<details>`'in `toggle` olayı spec gereği ASENKRON — tıklama ile aynı JS çağrısında `getBoundingClientRect()` okumak konumlandırmadan ÖNCEYE denk geliyor ve "taşma var" gibi YANLIŞ POZİTİF üretiyor; ölçümler tıklama ve okuma ayrı adımlara bölünerek tekrarlandı. `php -l` + `node --check` (tüm dosyalar) + **77/77+27/27+79/79+54/54+66/66+19/19+8/8+6/6** regresyon yeşil.
  - **Aynı gün, yirmi dokuzuncu adım — Kanban görünüm türü eklendi (Grup View-Kanban, DDL YOK).** **DDL gerekmedi ve bu tasarım turunda doğrulandı:** Formun DDL gerektirmesinin TEK sebebi herkese açık link SIRRIydı (`form_token` ile SATIR ARANIYOR, JSON içi anahtar indekslenemez); Kanban'da böyle bir arama yok, tüm ayarlar `views.config` JSON'una sığıyor (`kanban_field_id`, `kanban_card_fields`). **Ortak temel:** `BCC_VIEW_TYPES`'a `kanban`; `bcc_view_route_for()`'un iki değerli ternary gövdesi **`BCC_VIEW_ROUTES` haritasına** çevrildi (üçüncü tür ile ternary zinciri okunaksızlaşacaktı — harita dördüncü türü, Calendar'ı, saf veri eklemesine indiriyor, fonksiyon gövdesine bir daha dokunulmaz); `theme.css`'e `.view-type-badge--kanban` ikonu (kontrol listesinin 2. maddesi, `--view-icon` tanımsız kalsa rozet boş kutu çizerdi). **Üç ortak yardımcı çıkarıldı (kopya yazılmadı):** `bcc_config_field_id_list()` — `form_fields` ve `kanban_card_fields` AYNI güvenlikli id-çözümünü paylaşıyor (`is_scalar` süzgeci bir güvenlik kontrolü: dizi gelirse `(int)` cast'i 1 üretir ve gerçek bir field_id'ye denk gelebilir); `bcc_update_view_config()` — `views.config` oku-değiştir-yaz bloğu `view_config_update.php` ve `form_edit.php`'de BİREBİR duruyordu, Kanban üçüncüsünü gerektirince tek yere alındı (aynı JSON'a dört ayrı özellik yazdığı için "ezme" disiplini kritik); `bcc_kanban_config_from_view()` — `bcc_form_config_from_view()` ile aynı aile. **Sütunlama:** yalnızca `single_select` (`bcc_field_allowed_for_kanban`) — gerekçe koda dayalı: sonlu+sıralı liste, üyelik doğrulaması ve renk haritası ÜÇÜ DE hazır. `multiple_select` bilerek dışarıda (bir kayıt birden fazla sütunda görünür, "kartı taşı" belirsizleşir), `checkbox`/`user`/`date` de gerekçeleriyle. Sütunlar `choices`'tan O SIRAYLA türetilir, **"Atanmamış" EN BAŞTA ve ZORUNLU** (hücresi olmayan/boş kayıtlar aksi hâlde tahtada HİÇ görünmezdi). `choices`'ta artık bulunmayan bir değer taşıyan kayıtlar da "Atanmamış"a düşer ama **ham değeri kartta kesikli bir rozetle gösterilir** — veri kaybı görünümü olmasın diye. **Sürükle-bırak YENİ UÇ NOKTA GEREKTİRMEDİ:** kartı taşımak = o hücreye yeni değeri yazmak, yani `cell_update.php`'nin tam olarak yaptığı iş. O uçnokta zaten CSRF + oturum + `require_role('editor')` + kayıt↔alan sahipliği + soft-delete kontrolü yapıyor VE `normalize_cell_value()` sayesinde değerin gerçekten bir `choices` üyesi olduğunu doğruluyor — özel bir "kartı taşı" uçnoktası bu doğrulamaları KOPYALAMAK olurdu. İstemcide `bcc_bindColumnDrag()` iskeleti yeniden kullanıldı (ikinci bir sürükleme motoru yazılmadı), taşıma İYİMSER (hata olursa kart eski yerine GERİ ALINIR). **Render:** ayrı `kanban.php` — Form'un iki gerekçesinden yalnızca biri geçerliydi ve körü körüne kopyalanmadı: (a) `grid.php` 1400+ satır ve ikinci yarısının tamamı tablo varsayımı → GEÇERLİ, (b) anonim erişim ayrı dosya ister → Kanban oturumlu olduğu için GEÇERSİZ. Sunucu tarafı paylaşıldı: `bcc_build_grid_records_query()` ile **filtre ve soft-delete süzgeci bedava** geldi. **Detay modalı denendi ve KULLANILMADI — ölçülen gerekçeyle:** `grid-row-detail.js` `window.BCC_GRID`'e **20**, grid'e özel eleman id'lerine **33** kez bağlı ve modalin DOM'u `grid.php`'nin içinde; Kanban'a taşımak GRİDİ OLMAYAN bir sayfaya grid'in tüm istemci yığınını yüklemek olurdu, ki `kanban.php`'yi ayrı sayfa yapma gerekçesiyle çelişirdi. Bunun yerine **derin link** (`grid.php?table_id=N&record_id=M`) — `grid-row-detail.js:1338` URL'deki `record_id`'yi zaten okuyup paneli açıyor, sıfır yeni bağımlılık. Sürükleme sonrası gelen `click` bastırılıyor (`data-drag-moved`), yoksa kartı taşıyan kullanıcı istemeden detaya yönlendirilirdi. (Modali paylaşılabilir bir partial'a çıkarmak ayrı bir tur işi.) **Yetki grid ile BİREBİR AYNI** — Kanban'da sürüklemek "o hücreyi düzenlemek" demek, yeni bir izin kavramı icat edilmedi; istemcideki `canEdit` yalnızca kozmetik, gerçek kapı `cell_update.php`'nin `require_role('editor')`'ı. **Boş durum:** tabloda hiç `single_select` yoksa Kanban YİNE oluşturulabilir (tip seçiciyi tablo durumuna göre gri yapmak menüye tablo bilgisi taşımak olurdu ve kullanıcıyı çıkmaza sokardı), `kanban.php` net bir mesaj + `table_fields.php`'ye "Tekli seçim alanı oluştur" bağlantısı gösterir. **Test:** yeni `scripts/_verify_view_kanban.php` — **68/68 GEÇTİ**: sütunlar doğru sırayla türedi, hücresi olmayan/geçersiz değerli kayıtlar "Atanmamış"ta (ham değer görünür), soft-delete edilmiş kayıt tahtada YOK, kart alanları seçiminde birincil/sütun/yabancı id ELENDİ, diğer config anahtarları (`frozen_column_count`) EZİLMEDİ, taşımada `updated_at`/`updated_by` değişti, GEÇERSİZ seçenek `422`, `multiple_select`/yabancı `field_id`/grid görünümüne yazma `422`, **viewer ve commenter** için `data-can-edit=0` + panel YOK + `is-draggable` YOK + backend bypass `403` + DB değişmedi. **Tarayıcı (gerçek Chromium):** tahta 4 sütunla render oldu, kart "Yapilacak"tan "Bitti"ye sürüklendi, sayaçlar `1,0,1,2` oldu ve DB'de `Durum='Bitti'`, `updated_by` doğru kullanıcı. ⚠️ **Test yöntemi notu:** sentetik `MouseEvent`'te `buttons` ayarlanmazsa `grid-column-drag.js`'in "fare bırakılmış" koruması (`e.buttons === 0`) sürüklemeyi iptal ediyor; ayrıca headless'ta boyama olmadan `requestAnimationFrame` tetiklenmiyor — ikisi de YANLIŞ POZİTİF üretti, `buttons:1` ve aralara ekran görüntüsü (kare zorlama) ile çözüldü. Ürün kodunda hata yoktu. `php -l` + `node --check` + **78/78+27/27+79/79+54/54+66/66+19/19+8/8+6/6** regresyon yeşil.
  - **Aynı gün, otuzuncu adım — tablo oluşturma akışında iki tutarsızlık düzeltildi (DDL YOK) + kalıcı test süreci değişikliği.** **Teşhis (istek üzerine, salt-okunur):** yeni tablo ekleme akışı `public/base_tables.php`'de yaşıyor (`create_table` aksiyonu, tam sayfa POST — ayrı bir `table_create.php` YOK), owner-only; grid'deki tablo sekmelerinin yanındaki `+` bir buton DEĞİL, `base_tables.php`'ye giden bir BAĞLANTI (Airtable'daki yerinde açılan `+`'tan farklı, iki adımlı). Görünüm ekleme akışıyla (`api/view_create.php`, AJAX, editor) TAMAMEN ayrı — iki `+` var ve farklı davranıyorlar, karışma yok. Yeni tablo TAMAMEN boş geliyor (0 alan, 0 kayıt, 0 görünüm); görünüm tembel oluşuyor (`bcc_get_or_create_default_view()` ilk grid açılışında). Akış uçtan uca çalışıyor (izole test base'inde gerçek uç noktalarla doğrulandı: oluştur → grid'de aç → sil → audit izi). **Düzeltme 1 — rol tutarsızlığı:** `grid.php`'deki tablo `+`'sı `$canEdit` (editor+) ile gösteriliyordu ama oluşturma owner-only; editor `+`'yı görüp tıklıyor ve ayrı sayfada "owner rolü gerekir" duvarına çarpıyordu. ``$isOwner`'a çevrildi — bu, ALAN oluşturma `+`'sında ZATEN düzeltilmiş olan hatanın aynısıydı, tablo `+`'sı o düzeltmeden pay almamıştı. **Düzeltme 2 — transaction:** `create_table` ve `delete_table` aksiyonları `bcc_begin_transaction`/`commit`/`rollback` ile sarıldı (`view_create.php`/`record_add.php`/`table_clear_data.php` ile AYNI gerekçe: `log_audit()` patlarsa satır zaten yazılmış olurdu ve kullanıcı hata görüp tekrar denerken ikinci bir tablo oluştururdu). Silmede ekstra önemli: `tables_meta` silinince alanlar/kayıtlar/görünümler CASCADE ile gidiyor, audit yazılamazsa "neyin silindiğini söyleyen hiçbir kayıt olmadan yok olmuş tablo" kalırdı. **Atomiklik HEM kod incelemesiyle HEM AMPİRİK kanıtlandı:** `mysqli_report(MYSQLI_REPORT_ERROR|STRICT)` her SQL hatasını istisnaya çeviriyor → `catch` → `rollback`; ayrıca aynı primitiflerle transaction içinde INSERT + kasıtlı hatalı audit INSERT'i çalıştırılıp `tables_meta` satırının GERÇEKTEN geri alındığı doğrulandı (gerçek InnoDB üzerinde). **Test:** 26/26 GEÇTİ — owner `+`'yı görüyor, editor GÖRMÜYOR, editor backend bypass hâlâ `403`, owner oluşturup silebiliyor, audit satırları yazılıyor; **regresyon:** editor hâlâ görünüm `+`'sını görüyor ve hücre düzenleyebiliyor, alan `+`'sındaki önceki düzeltme bozulmadı. ⚠️ **KALICI SÜREÇ DEĞİŞİKLİĞİ (bu adımda kararlaştırıldı):** görsel doğrulama gerekmeyen testler artık `/browse` ile DEĞİL, sunucu-taraflı HTTP betikleriyle (gerçek oturum + gerçek uç noktalar, `scripts/_verify_*.php` deseni) yapılıyor. Gerekçe: /browse daemon'ı bu makinede birkaç komutta bir oturumu düşürüyor ve sayfa sessizce en son açık olana — tekrar tekrar KULLANICININ GERÇEK base'ine — dönüyor; bu gün içinde gerçek `Test` tablosunda (table 8) altı artefakt oluşturdu (`12.45` adlı boş alan, iki boş kayıt, üç görünüm — biri CANLI form token'lı). Hepsi denetimle bulunup korumalı yöntemle (tablo koşulu + boşluk teyidi + tam 1 satır doğrulaması) silindi, hiçbirinde veri kaybı olmadı. Sunucu-taraflı betikler kayamaz: hedef tablo/base id kodda sabit, oturum düşerse istek BAŞARISIZ olur, yanlış yere yazmaz. /browse artık yalnızca gerçekten görsel/CSS/sürükle-bırak kanıtı gerektiğinde ve HER chain login + açık `goto` ile başlayarak kullanılıyor. **Bu turda kapsam dışı bırakılan (raporlandı):** `rename_table` ve `move_table` aksiyonları hâlâ transaction'sız; `move_table` özellikle dikkat ister çünkü `bcc_reorder_sibling()` İKİ ayrı UPDATE yapıyor ve yarım kalan bir takas iki satırı aynı `position`'da bırakır. `php -l` + 6/6+8/8+19/19 regresyon yeşil.
  - **Aynı gün, otuz birinci adım — `rename_table`/`move_table` transaction'a sarıldı + `bcc_reorder_sibling()`'ın transaction sözleşmesi değişti (DDL YOK).** **Kod incelemesi beklenenden farklı bir tablo çıkardı:** `move_table`'ın iki UPDATE'i ZATEN aynı transaction'daydı (`bcc_reorder_sibling()` kendi `begin/commit`'ini açıyordu), yani "ilk UPDATE commit olur, ikincisi patlar" senaryosu bugün de mümkün DEĞİLDİ. Gerçek boşluk başka yerdeydi: `bcc_commit()` `log_audit()`'ten ÖNCE çalışıyordu, yani takas kalıcı olur ama audit yazması patlarsa geriye **sırası değişmiş, hiçbir iz bırakmamış** bir satır kalırdı — `create_table`'ta düzeltilen sorunun aynısı. **Engel — iç içe transaction:** çağıran tarafta `bcc_begin_transaction()` açıp sonra bu fonksiyonu çağırmak mysqli'de İÇTEKİ commit'in DIŞTAKİNİ erkenden commit etmesine ve rollback korumasının SESSİZCE kaybolmasına yol açardı (düzeltmeden beter). **Çözüm:** transaction sorumluluğu çağırana taşındı (`bcc_assign_autonumbers()` için zaten kurulmuş sözleşme) — `bcc_reorder_sibling()` artık kendi transaction'ını AÇMIYOR, iki UPDATE'i çağıranın transaction'ı içinde yapıyor. **DÖRT çağrı yerinin DÖRDÜ de güncellendi** (yalnızca `base_tables.php`'yi düzeltmek diğer üçünü transaction'sız bırakırdı): `base_tables.php` (move_table), `table_fields.php` (move_field), `api/view_reorder.php` (görünüm sırası), `slack_settings.php` (kural sırası). `rename_table` da sarıldı; `base_tables.php`'nin DÖRT aksiyonu (create/rename/delete/move) artık atomik. **Bilinçli davranış değişikliği — `view_reorder.php`:** `log_audit()` istisnası eskiden SESSİZCE YUTULUYORDU ("zaten gerçekleşmiş bir eylemi yanlışlıkla başarısız gösterme" gerekçesiyle). O gerekçe, sıralamanın audit'ten ÖNCE kalıcılaştığı bir dünyada geçerliydi; artık ikisi aynı transaction'da olduğu için audit yazılamazsa sıralama da geri alınıyor ve istemciye `500` dönmek DOĞRU cevap oluyor. Ayrıca o dosyada transaction yetki/varlık kontrollerinden SONRA açılıyor — `json_fail()` `exit` ettiği için erken bir çıkış açık transaction bırakırdı. **ATOMİKLİK AMPİRİK KANITLANDI (yarım-takas senaryosu doğrudan simüle edildi):** `bcc_reorder_sibling()`'in KENDİSİ çağrılıp transaction içinde iki UPDATE yapıldı, sonra kasıtlı bir SQL hatası tetiklendi → **her iki UPDATE de geri alındı**, her satırın `position`'u BİREBİR eski değerinde, hiçbir satır aynı `position`'u paylaşmıyor. **Test:** yeni `scripts/_verify_reorder_atomicity.php` — **52/52 GEÇTİ**: rename (ad+açıklama+audit), move (yukarı/aşağı/sınır durumu, çakışma yok), yarım-takas rollback kanıtı, rename rollback kanıtı, DÖRT çağrı yerinin canlı regresyonu (move_field / view_reorder / slack kural sırası — hepsinde sıra değişti, çakışma yok, audit yazıldı), kod incelemesi (fonksiyon içinde transaction KALMADI, dört çağrı yerinde `begin/commit/rollback` simetrik). Testler **sunucu-taraflı HTTP** ile yapıldı (/browse KULLANILMADI, yeni süreç kuralı) ve gerçek base (id 15) tablo sayısı/kayıt sayısı/**tablo sıralaması** önce-sonra karşılaştırmasıyla korundu. **Fikstür notu:** `slack_routing_rules` şeması `value`/`webhook_id` kullanıyor (`match_value`/`webhook_url` DEĞİL) ve `webhook_id` bir FK — test fikstürü şemadan doğrulanarak düzeltildi. `php -l` + 19/19+8/8+6/6 ve tüm önceki suite'ler yeşil.
  - **Aynı gün, otuz ikinci adım — Grid görünümü için "PDF olarak indir" ve "PNG olarak indir" (DDL YOK, YENİ UÇ NOKTA YOK).** **Teşhis istenen işin bir varsayımını çürüttü:** "..." menüsüne `window.print()` çağıran bir kalem EKLENMESİ istenmişti, ama `gs-view-print-item` ("Görünümü yazdır") ZATEN vardı ve tam olarak `window.print()` çağırıyordu — yeni bir kalem menüye davranışı birebir aynı iki satır koyardı. Kullanıcı onayıyla kalem **yeniden adlandırıldı** ("PDF olarak indir"; id ve aksiyon AYNEN korundu, tarayıcının print diyaloğunda varsayılan hedef zaten "PDF olarak kaydet"). Bu turun asıl işi **print CSS cilası** oldu; eski print bloğu 18 satırdı ve `thead` tekrarı, `page-break-inside`, `@page`, sayfaya sığdırma HİÇ YOKTU, ayrıca tablo sekmesi şeridi/"Alanları yönet"/sütun ▾ menüleri/"+" sütunu/genişlet butonu/checkbox'lar kâğıda BASILIYORDU. **Mimari karar — TEK kural listesi, İKİ tüketici:** ortak kurallar yeni `assets/grid-export.css`'te ve grid.php'ye `media="print"` ile bağlı, yani PDF yolu **hiçbir JS'e bağımlı değil** (Ctrl+P dahil); "PNG olarak indir" ise html2canvas'ın `onclone` KOPYASINDA aynı link'in `media`'sını `"all"`a çevirerek AYNI listeyi kullanıyor (klon `screen` medyasında render edildiği için kurallar `@media print`e hapsedilseydi PNG'ye hiç uygulanmaz ve liste ikinci kez yazılmak zorunda kalırdı). Canlı DOM'a hiç dokunulmuyor (ekranda titreme yok). Yalnızca kâğıda özgü olanlar (`@page`, sayfa bölme, sığdırma) `grid-shell.css` `@media print`te. **Landscape:** `@page` bir sınıfa göre şartlanamadığı için `grid-view-manage.js` kuralı çalışma anında enjekte ediyor; karar ÖLÇÜLEN GENİŞLİĞE göre DEĞİL sütun sayısına göre (`>= 6`) — `table.grid`'in `min-width:100%`i dar bir tabloda bile `scrollWidth`'i ~1400px okutup HER tabloyu landscape yapardı (denendi, ölçü kullanılamıyor). **html2canvas 1.4.1 (MIT) YEREL dosya:** `public/assets/vendor/html2canvas.min.js`, sha256 `e87e5507…eab8cb`; kütüphanedeki tüm mutlak URL'ler beyaz listede (SVG namespace + banner), **ağdan hiçbir şey çekmiyor**, CDN referansı sıfır. Sayfa açılışında DEĞİL, ilk tıklamada yükleniyor (`window.html2canvas` tıklamadan önce `false`, sonra `true` olarak doğrulandı) — 198KB her grid açılışının önüne konmuyor. **Veri kapsamı:** PDF ve PNG DOM'u bastığı için filtre/sıralama/gizli sütun ZATEN uygulanmış geliyor; ikinci bir parse/sorgu YAZILMADI ve Excel ile tutarlılık sunucu tarafında grid HTML'i ↔ `.xlsx` karşılaştırmasıyla kanıtlandı. **/browse ile bulunan GERÇEK BUG (görsel doğrulama olmasa kaçardı):** klonda sol şerit/görünüm paneli gizlenince `.gs-main` genişliyor ve `min-width:100%` tabloyu ekrandakinden geniş yayıyordu — ölçüldü: **canlı 926px → klon 1240px**. Canvas canlı ölçüyle açıldığı için sağdaki 314px KIRPILIYOR, altta da gizlenen "+" satırı kadar boş şerit kalıyordu (metin bu yüzden "ortalanmış" görünüyordu; hiza her zaman `left`'ti, kayan şey sütun sınırlarıydı). Düzeltme: sütun genişlikleri ekrandan ölçülüp klona `table-layout:fixed` ile dayatılıyor, gizli "+" sütunu genişliğe / "+" satırı yüksekliğe katılmıyor. Düzeltmeden sonra PNG 1772x420 (= canlı 886x210'un 2x'i), Kod sütun sınırı 537 ≈ canlı 539. **Eşik SERT ENGEL DEĞİL:** 500 satır / 12000px aşılırsa Türkçe onay diyaloğu ("Bu görünüm büyük, PNG yavaş/okunmayabilir. Excel önerilir. Devam edilsin mi?"); iptal → indirme YOK, devam → iniyor (520 satırlık tabloda ikisi de doğrulandı). **KAPSAM SINIRI:** yalnızca Grid; `kanban.php`/`form.php` ve `kanban.js` hiç dokunulmadı (teste bağlandı). **Test — yeni `scripts/_verify_grid_export.php` 67/67 GEÇTİ** (sunucu-taraflı HTTP: menü/varlık, CDN'sizlik, Apache'nin dosyaları servis etmesi, grid HTML ↔ .xlsx için filtre/sıralama/gizli sütun/üçü bir arada tutarlılığı, kapsam sınırı, kırpma bug'ının regresyonu) + **/browse ile görsel kanıt** (5 sayfalık PDF: `792x612 YATAY`, "Sehir"/"Notlar"/"Durum" başlıkları **5 sayfanın 5'inde de**, 90 kayıt **hiçbiri iki sayfada değil** yani satır bölünmesi YOK, "Alanları yönet"/"Paylaş"/"Shift-Enter" **hiç yok**; dar tablo `612x792 DİKEY`; filtreli PDF 30 kayıt/2 sayfa/"Notlar" başlığı yok). PDF metni Chrome'un altküme CID fontlarıyla yazıldığı için düz `(...)` taraması hiçbir şey bulmuyordu — `scripts/_pdf_inspect.php` fontların `/ToUnicode` CMap'lerini (font BAŞINA ayrı, çünkü glif numaraları çakışıyor) çözerek sayfa sayfa metin çıkarıyor. Gerçek base (id 15) tablo/alan/kayıt/görünüm sayıları önce-sonra değişmedi. **Regresyon:** "Kaydı yazdır" (kayıt bazlı, `style.css`'in `:not([hidden])` ile şartlı ayrı bloğu) ve "Excel indir" bozulmadı; `php -l` + `node --check` (tüm dosyalar) + 19/19+8/8+6/6 yeşil.
  - **Aynı gün, otuz üçüncü adım — grid sütun genişliği sürükleyerek ayarlanabilir oldu (DDL YOK, YENİ UÇ NOKTA YOK).** **Önceki teşhis (salt-okunur tur):** sütun genişliği kodu HİÇ YOKTU — `col-resize`/`resizer` araması tüm `public/` içinde sıfır sonuç, şemada hiçbir `width` kolonu yok, genişlikler tamamen tarayıcının auto tablo yerleşimine bırakılmıştı (`table.grid { width:auto; min-width:100% }`). Kullanıcının "sürüklenebiliyor mu" sorusunun kaynağı büyük olasılıkla `.grid-freeze-handle`: tam bir resize tutamacının duracağı yerde (başlığın sağ kenarında, `cursor:grab`, hover'da mavi çizgi) duruyor ama **dondurulan sütun sayısını** ayarlıyor. **YERLEŞİM KARARI — opt-in `table-layout: fixed`:** kaydedilmiş genişlik YOKSA tablo bugünkü otomatik yerleşiminde kalıyor, yani mevcut hiçbir görünüm değişmiyor; kullanıcı bir kenarı İLK kez sürüklediğinde `grid-column-resize.js` o anki TÜM sütun genişliklerini ekrandan ölçüp `<colgroup>`'a yazıyor ve `fixed`'e geçiyor (geçiş dikişsiz — yalnızca çekilen sütun değişiyor). `fixed` şart: auto layout'ta bir `<th>` width'i yalnızca ÖNERİdir, `min-width:100%` esnemesi onu ezerdi. Sabit moda geçilince `min-width:100%` de kapatılıyor (`.grid-has-col-widths`), yoksa fixed layout artan alanı sütunlara oransal dağıtıp sürüklenen pikseli bozardı; toplam genişlik tabloya inline yazılıyor. **KALICILIK:** `views.config.column_widths`, `frozen_column_count` ile AYNI oku-değiştir-yaz mekanizması (`bcc_update_view_config`). Anahtar biçimi `{"row":219,"f123":320}` — düz `{field_id:px}` yerine önekli, çünkü (a) satır no sütununun bir `field_id`'si yok ama fixed layout'ta genişliği olmak ZORUNDA, (b) `json_decode(...,true)` sayısal string anahtarları int'e çevirip `row` ile karışık anahtarlı bir dizi üretirdi. **BULUNAN GERÇEK BUG (bu iş olmasa görülmezdi):** `view_config_update.php` `frozen_column_count`'u `isset` kontrolü olmadan `: 1` varsayılanına düşürüp HER İSTEKTE yazıyordu — yalnızca genişlik gönderen bir istek kullanıcının dondurma ayarını SESSİZCE 1'e geri alırdı. Artık her iki anahtar da yalnızca gönderildiğinde yazılıyor, hiçbiri gönderilmezse `400`. **TUTAMAÇ AYRIMI (istenen kısıt):** dondurma 12px şerit / `cursor:grab` / MAVİ çizgi / tabloda TEK; genişlik 7px şerit / `cursor:col-resize` / GRİ çizgi / HER başlıkta. Donmuş kenar sütununda ikisi buluşuyor — genişlik tutamacı orada 12px sola kaydırılıp `z-index` altta tutuluyor; canlı ölçümle kanıtlandı: `resize=[614,621] z=9`, `freeze=[624,636] z=10`, **3px boşluk, üst üste binme YOK**, `elementFromPoint` her iki merkezde de doğru tutamacı döndürüyor. **`grid-freeze-columns.js`'e HİÇ DOKUNULMADI**; sürükleme iskeleti de kopyalanmadı — `bcc_bindColumnDrag()` (kanban/dondurma/görünüm sıralamasının kullandığı aynı iskelet) yeniden kullanıldı ve boyutlandırma bitince dondurma ofsetleri o dosyanın kendi açtığı `window.BCC_reapplyFreeze` kancasıyla tazeleniyor. **SINIRLAR:** min 80px / max 800px, TEK kaynak `src/schema.php` — istemci sunucudan gelen `BCC_MIN/MAX_COLUMN_WIDTH` ile sürüklerken kırpıyor, sunucu kaydederken YENİDEN kırpıyor (istemciye güvenilmiyor; başka tablonun `field_id`'si de whitelist'te düşüyor). Boş harita göndermek anahtarı SİLİYOR = otomatik yerleşime dönüş. **KIRPMA YERİ SEÇİMİ:** sabit modda içerik sütunu genişletemediği için taşan metnin kırpılması gerekti; `<th>`'ye `overflow:hidden` VERİLEMEZ — iki tutamaç da hücreden dışarı taşan mutlak konumlu çocuklar, ikisi de yarıdan kesilirdi. Bu yüzden yalnızca alan adı (`.grid-th-label`, yeni sarmalayıcı) kırpılıyor; `<td>` kırpılıyor ama düzenleme hâlindeki hücre (`.editing`) muaf, çünkü dosya-eki popover'ı (`position:absolute`, hücrenin ALTINA sarkıyor) kırpılırdı. **PRINT/PNG:** kâğıtta `table-layout:auto !important` + `col{width:auto}` ile sabit yerleşim çözülüyor (piksel genişlikler ekran için ölçüldü, sayfayla ilgisi yok — sabit kalsalardı sığdırma çalışmazdı); PNG ekranı yansıttığı için genişlikleri KORUYOR, bu yüzden kural `grid-export.css`'te değil `grid-shell.css @media print`te. Her iki tutamaç da iki çıktıda da gizli. **Test — yeni `scripts/_verify_column_resize.php` 65/65 GEÇTİ** (savunmacı okuyucu/kırpıcı birim testleri, uç noktanın iki özelliği birbirini ezmemesi + eski bug'ın regresyonu, rol/CSRF, opt-in render kanıtı, colgroup içeriği, gizli alanın dışarıda kalması, tutamaç ayrımı, export/print regresyonu). **/browse ile canlı kanıt:** başlangıçta `table-layout=auto` + colgroup YOK; +120px sürükleme 320→440 (birebir), sonra `fixed`+colgroup; -2000px sürükleme 80px'te duruyor; **sayfa yenilendikten sonra genişlik duruyor** (colgroup sunucudan geliyor). Genişlik ayarlıyken PDF **değişmedi** (5 sayfa, landscape, başlık 5/5 sayfada, 90 kayıt bölünmesiz) ve PNG **yeni genişlikleri yansıtıyor** (Ad 80px dar / Kod 347px geniş, 645 CSS px = ekran 685 eksi gizli "+" sütunu). **KAPSAM SINIRI:** yalnızca Grid; `kanban.php`/`form.php`/`kanban.js` dokunulmadı (teste bağlandı). `php -l` + `node --check` (tüm dosyalar) + 19/19+8/8+6/6 + `_verify_grid_export` 67/67 yeşil. **Test yardımcısı notu:** `_verify_grid_export.php`'nin `grid_header_names()`'i düz `<th>` arıyordu, `<th data-col-key="fN">` gelince eşleşme sıfıra düştü — ÜRÜN değil yardımcı kırıldı (aynı turdaki `.xlsx` ve kayıt karşılaştırmaları geçmişti), alan sütununu açıkça hedefleyecek şekilde düzeltildi.
  - **Aynı gün, otuz dördüncü adım — kayıt/doğrulama maili: tek kurumsal göndericiye taşındı + kurumsal HTML tasarımı (DDL YOK).** **Teşhis:** projede birbirinden habersiz İKİ mail hattı vardı — doğrulama maili `src/mailer.php`'nin ELLE YAZILMIŞ SMTP istemcisiyle kişisel bir **Gmail** hesabından düz metin olarak, "Kaydı gönder" ise **PHPMailer** ile **Office 365** (`bcccrm@bcciletisim.com.tr`) üzerinden HTML+AltBody olarak gidiyordu. Ayrılık kaza değildi: `mail_record_send.local.php`'nin kendi yorumu *"Bu dosya BİLEREK `config/mail.local.php`'den AYRI ... buraya dokunulmadı"* diyor — yani eski hat sadece dokunulmadan bırakılmıştı. `bcc_send_mail()`'in tüm projede TEK çağıranı `register.php` olduğu için taşıma başka hiçbir yeri etkilemedi. **SPAM — dürüst sınır:** kararın ağırlıklı kısmı gönderen itibarı ve DNS'tir; SPF (`include:spf.protection.outlook.com`), DKIM (M365 Defender → iki CNAME) ve DMARC (`_dmarc` TXT) **kodla yapılamaz, kullanıcının işi** — bu bir "spam çözüldü" turu DEĞİL. Koddan düzeltilen şey şuydu: görünen ad kurumsal ("BCC-Core"), gönderen adres kişisel bir Gmail'di; bu, alıcı sunucularda **"display name spoofing"** kalıbı olarak puanlanan aktif bir kırmızı bayraktı. **Yapılan:** `bcc_send_mail()`'in `smtp` dalı PHPMailer'a taşındı ve `bcc_smtp_config()` ile `config/mail_record_send.local.php`'yi okuyor (dosya adı artık tarihsel — o config "kaydı gönder"e değil PROJENİN TEK SMTP HESABINA ait). `record_send.php`'ye DOKUNULMADI; aynı dosyayı okuduğu için hesap birliği kendiliğinden sağlandı. Elle yazılmış `bcc_smtp_send()` SİLİNMEDİ, `bcc_smtp_config()` içinde geriye dönük yedek olarak duruyor. Multipart (Body + AltBody) ve `Reply-To: info@bcciletisim.com.tr` bedava geldi; düz metin parçası HTML'den `strip_tags` ile TÜRETİLMİYOR, elle yazılıyor. Görünen ad `config/mail.php`'de "BCC İletişim"; From ADRESİ config'ten gelmek zorunda (Office 365 kimlik doğrulanan kutudan farklı From'u reddeder). Konu sadeleşti ("BCC-Core — e-postanızı doğrulayın" → "BCC-Core hesabınızı etkinleştirin"). `config/mail.local.php` yeniden yazıldı: Gmail kimlik bilgileri ve Uygulama Şifresi silindi (git'e hiç girmemişti — geçmiş tarandı, dört `*.local.php` dosyasının hiçbiri hiç commit edilmemiş). **BULUNAN GERÇEK KUSUR — doğrulama linki:** `register.php` linki `$_SERVER['HTTP_HOST']`'tan kuruyordu, yani localhost'ta çalışan uygulamada maile `http://localhost/verify_email.php?token=...` yazılıyor ve **alıcı erişemiyordu**; ayrıca HTTP_HOST istemciden gelen bir başlık olduğu için e-postaya gömmek host-header enjeksiyonuna kapı bırakıyordu. Yeni `config/app.php` + `bcc_app_base_url()`: `$APP_BASE_URL` (yeni `config/app.local.php`, `.gitignore`'a eklendi; şablonu `app.local.php.example` commit'li) doluysa o, boşsa eski davranış. **Bu makinede hâlâ boş olduğu için test mailindeki link localhost — canlıya çıkarken doldurulmalı.** **TASARIM:** yeni `src/mail_template.php` → `bcc_mail_html_shell()` — tablo tabanlı layout + inline CSS (mail istemcileri flexbox/grid/harici `<style>` desteklemez), `theme.css`'ten ELLE kopyalanmış marka renkleri (#1a56db / #2d7ff9), CTA butonu ("Hesabımı Etkinleştir") + altında ham bağlantı yedeği, footer'da site / iki telefon (ikisi de aynı WhatsApp hattına) / mailto / Google Maps. **LOGO:** GitHub'a yükleme GEREKMEDİ — `https://bcciletisim.com.tr/assets/images/logo.png` zaten yayında ve `public/assets/logo.png` ile birebir aynı dosya (7864 bayt); gönderen alan adıyla aynı kaynaktan gelen görsel istemcilerde en yüksek güveni görüyor. Maile localhost adresi konmadı. Şu an TEK tüketici doğrulama maili; `record_send.php`'nin kendi `bcc_build_send_email_html()`'i duruyor, birleştirme ayrı iş. **Test — yeni `scripts/_verify_mail_verification.php` 24/24** (gönderen kimliği, hesap birliği, multipart, sekiz şablon öğesi, uzak kaynakların gerçekten 200 dönmesi, bağlantı kurulumu) + argümanla **gerçek gönderim 25/25**, `yigit.aslantas@bcciletisim.com.tr` adresine Office 365 üzerinden gönderildi. Tasarım `/browse` ile ekran görüntüsüyle doğrulandı; **bulunan test artefaktı:** önizleme `file://` ile açıldığında logo `0x0` geliyordu — Chromium'un file-origin kısıtı, mailin sorunu değil; `http://localhost` üzerinden servis edilince `94x44` yüklendi (geçici önizleme dosyası hemen silindi). **Test yardımcısı notu (ikinci kez aynı ders):** iki kontrol ham metinde `gmail.com` / `HTTP_HOST` arıyordu ve DEĞİŞİKLİĞİN GEREKÇESİNİ anlatan YORUMLARA takılıp yanlış KALDI veriyordu; `token_get_all()` ile yorumlar soyularak bakılıyor artık (grid-export.css'teki `@media` vakasının aynısı). `php -l` temiz; 19/19+8/8+6/6+67/67+65/65 yeşil. **Sırada kullanıcıda:** SPF/DKIM/DMARC kayıtları, `$APP_BASE_URL`, ve artık kullanılmayan Gmail Uygulama Şifresinin iptali.
  - **Aynı gün, otuz beşinci adım — `table_fields.php` arayüz yenilemesi (DDL YOK, YENİ UÇ NOKTA YOK, DAVRANIŞ DEĞİŞMEDİ).** **Turun asıl riski görsel değil YAYILMA riskiydi:** sayfa `.settings-card`/`.settings-table`/`.settings-btn`/`.settings-breadcrumb` sınıflarını **dokuz** başka sayfayla paylaşıyor (`admin/index`, `admin/create_user`, `admin/create_team`, `admin/assign_team`, `bases`, `base_tables`, `form_edit`, `kanban`, `slack_settings`) ve alan tipi grid'i `src/partials/field_type_wizard_fields.php`'den gelip **`grid.php`'nin "+" popup'ıyla** paylaşılıyor. `home.css`/`theme.css`'i değiştirmek o sayfaları sessizce yeniden tasarlardı. **Çözüm:** tüm yeni kurallar yeni `assets/table-fields.css`'te ve **istisnasız** `.tf-page` kapsamı altında; dosya yalnızca bu sayfaya bağlı. Bağlamak için `home_shell_top.php`'ye **eklemeli** bir `$homeExtraCss` kancası eklendi — tanımsızsa boş diziye düşer, kabuğu paylaşan diğer sayfalar hiç etkilenmez (teste bağlandı). Paylaşılan partial ve `field-type-wizard.js` HİÇ değiştirilmedi. **İKİ GERÇEK YERLEŞİM KÖK NEDENİ BULUNDU:** (1) `#new-field-form` `.settings-form-stacked` sınıfını taşıyor ve o sınıfın `max-width: 420px`i var; tip grid'i formun İÇİNDE olduğu için 420px'e sıkışıp `auto-fill minmax(150px)` iki sütuna düşüyordu — 21 tipin alt alta uzamasının ("sayfa sonsuz uzuyor") sebebi buydu. (2) Kartın altındaki ~800px'lik ölü alan: `.settings-form` `flex-wrap: wrap` taşıyor, `.settings-form-stacked` yönü `column`a çeviriyor; **sütun yönlü flex + wrap** Chromium'da kabın ana bloktan devraldığı boş alandan yükseklik türetmesine yol açıyor — ÖLÇÜLDÜ: içerik toplamı 358px iken form **1157px**, `nowrap` verilince **383px**. İkisi de yalnızca `.tf-page` altında düzeltildi; paylaşılan `.settings-form` kuralına dokunulmadı (yatay formlar satır kaydırmayı ondan alıyor). Sonuç: sayfa yüksekliği **~1990px → 1208px**. **YAPILAN:** içerik `max-width: 1080px` ile ortalandı (sağdaki ölü alanın kök nedeni `.home-main`'in `flex:1` ile tüm genişliği kaplaması ama 5 sütunlu bir tablonun onu dolduramamasıydı); breadcrumb sessiz gri + ikon + hover-yüzey oldu (vurgu mavisi kaldırıldı — bunlar birincil eylem değil); alan tipi hap rozetine döndü (ikon `.field-type-badge`'in KENDİ `background-image`'ından, kopyalanmadı); "Düzenle"/"Sil"/↑↓ dolu zeminli metin butonları eşit ölçülü **hayalet ikon butonlarına** dönüştü (zemin yalnızca hover'da, sil hover'da `--bcc-danger-soft`), POST mekanizması ve CSRF formları AYNEN korundu — yalnızca görünüm ve `aria-label`/`title` değişti; "Zorunlu" sütunu tik ikonlu vurgu rozeti; boş durum kesikli çerçeveli kutu. **ARAMA:** 21 alan tipi için arama kutusu, sayfaya özel `assets/table-fields.js` ile ÇALIŞMA ANINDA `#new-field-type-step`'in İÇİNE ekleniyor — dışına konsaydı sihirbaz 2. adıma geçince (`typeStep.hidden = true`) ekranda asılı kalırdı; 8'den kısa listede hiç eklenmiyor (grid popup'ı zaten dar). Filtre `[hidden]` DEĞİL ayrı bir sınıf kullanıyor (grid öğelerinde `display` `[hidden]`ı ezer — projede yaşanan tuzak). **Yeni sabit renk YOK**, hepsi `--bcc-*` token'ı; koyu tema `/browse` ile ayrıca doğrulandı. **Test — yeni `scripts/_verify_table_fields_ui.php` 28/28**: her selector'ın `.tf-page` ile başladığı, `home.css`/`theme.css`/partial/wizard-js'e sızma olmadığı, kabuk kancasının eklemeli olduğu ve altı sayfanın `$homeExtraCss` atamadığı, iki kök nedenin düzeltildiği, arama kutusunun davranışı, sabit renk olmadığı. **/browse canlı:** 9 fonksiyon testi (arama filtresi, boş durum, tip seçimi → adım 2, "Tip değiştir", tekli seçim → seçenekler satırı, autonumber → zorunlu gizleme) ve tam CRUD turu (alan oluştur → ↑ taşı → düzenle → sil, DB'den doğrulandı). **REGRESYON kanıtı:** `grid.php` "+" popup'ında `table-fields.css` yüklü DEĞİL, `.tf-page` yok, `.field-type-option` hâlâ `radius 6px / padding 8px 11.2px / tek sütun`; `base_tables.php` ve `slack_settings.php`'de `.settings-card` hâlâ `radius 10px / padding 24px`. `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil. **Not:** mail suite'indeki ağ bağımlı iki kontrol geçici bir DNS kesintisinde hatalı KALDI veriyordu (bir taramada 24/25, hemen ardından 25/25) — kontrol zayıflatılmadan tek seferlik yeniden deneme eklendi.
  - **Aynı gün, otuz altıncı adım — `base_tables.php` aynı tasarım sistemine taşındı + ortak stylesheet çıkarıldı (DDL YOK, DAVRANIŞ AYNI).** `base_tables.php` yapı olarak `table_fields.php`'nin neredeyse aynısı (breadcrumb → sayaçlı kart → `settings-table` + satır aksiyonları → düzenle/oluştur formları), bu yüzden **kurallar kopyalanmadı**: bir önceki adımın `table-fields.css`'i ikiye ayrıldı — ortak iskelet yeni **`assets/settings-page.css`**'e (sayfa kabı, breadcrumb, kart, veri tablosu, hayalet ikon butonları, sayaç rozeti, form alanları, birincil buton, boş durum), yalnızca alan tipi kavramına ait olanlar (`tf-type-pill`, `tf-required-yes`, tip arama kutusu, `field-type-grid` iyileştirmeleri) `table-fields.css`'te kaldı. Kapsam sınıfı `.tf-page` → **`.sp-page`** olarak genelleştirildi ve paylaşılan sınıflar `tf-*` → `sp-*` yeniden adlandırıldı (`sp-icon-btn`, `sp-count`, `sp-primary-name`, `sp-move-group`, `sp-muted`); `table_fields.php` de bu ada taşındı. `base_tables.php` yalnızca `settings-page.css` yüklüyor, `table-fields.css`/`table-fields.js` YÜKLEMİYOR. **YAN KAZANÇ:** bir önceki turda bulunan **sütun yönlü flex + `flex-wrap: wrap`** hatası (kabın içeriğinden ~800px uzun olması) artık ortak dosyada `.sp-page .settings-form-stacked` üzerinde — yani `base_tables.php`'nin "Yeni Tablo"/"Tabloyu Düzenle" formları da aynı düzeltmeden bedava yararlanıyor (o sayfada latent duruyordu, bkz. önceki adımın notu). **YAPILAN (base_tables):** breadcrumb "← Base'lere dön" → ikonlu, sessiz gri, hover-yüzey; başlık tipografisi ortaklaştırıldı; "Tablolar (n)" → sayaç rozeti; **tablo adı birincil gezinme öğesi** oldu (ağırlaştırıldı, hover'da vurgu + alt çizgi + beliren "git" oku); boş açıklama `—` ile sessizleşti; "Düzenle"/"Sil"/oklar `sp-icon-btn` hayalet ikon butonlarına döndü (POST/CSRF formları AYNEN, yalnızca görünüm + `aria-label`/`title`); "Yeni Tablo" formuna placeholder'lar ve odak halkası, "Tablo Oluştur" butonuna yumuşak gölge + `:active` bastırma eklendi. **Test — `scripts/_verify_table_fields_ui.php` → `_verify_settings_pages_ui.php` olarak yeniden yazıldı, 49/49**: iki CSS dosyasında da her selector'ın `.sp-page` ile başladığı, **ortak iskeletin `table-fields.css`'te TEKRARLANMADIĞI** (her paylaşılan sınıf için ayrı kontrol), sayfaya özgü sınıfların ortak dosyaya SIZMADIĞI, `home.css`/`theme.css`/partial/wizard-js'e sızma olmadığı, altı sayfanın `$homeExtraCss` atamadığı, iki yerleşim kök nedeninin düzeltildiği, arama kutusunun yalnızca `table_fields`'ta olduğu, iki dosyada da sabit HEX renk bulunmadığı. **/browse canlı:** `base_tables.php` tam CRUD turu (oluştur → ↑ taşı → düzenle → sil) ve `table_fields.php`'nin sınıf taşımasından sonra bozulmadığı (genişlik 1080, 32 ikon butonu, 8 hap, arama kutusu, form 332px). **REGRESYON kanıtı:** `bases.php` ve `slack_settings.php` yalnızca `theme.css`+`home.css` yüklüyor, `.sp-page` yok, `.settings-card` hâlâ `radius 10px / padding 24px`; `grid.php` "+" popup'ında `.field-type-option` hâlâ `radius 6px / padding 8px 11.2px`. `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil.
  - **Aynı gün, otuz yedinci adım — `account.php` iki sütunlu hesap panosuna dönüştü (DDL YOK, YENİ UÇ NOKTA YOK).** Sağ yarının boş kalmasının kök nedeni sayfanın tek dar sütun olmasıydı; `.ac-grid` ile `minmax(0,1fr) / 320px` yerleşime geçildi (1080px altında tek sütuna iner, sağ panel `sticky` → `static`). Ortak iskelet yine `settings-page.css`'ten; sayfaya özgü olanlar yeni `assets/account.css`'te, hepsi `.sp-page` kapsamında. **UYDURULMAYAN WIDGET'LAR — bilinçli:** istenen "İki faktörlü doğrulama", "Aktif oturumlar", "API anahtarları", "Bildirim ayarları" bu uygulamada **YOK** (users tablosunda 2FA kolonu yok; oturumlar PHP native `$_SESSION`'da, DB'de tutulmuyor; API anahtarı ve bildirim ayarı özelliği hiç yok). Sahte bir "2FA: Kapalı" göstergesi olmayan bir güvenlik ayarını varmış gibi, ölü bir "API anahtarları" linki de olmayan bir sayfayı varmış gibi gösterirdi — **ekrana konmadılar** ve bu, teste bağlandı (yorumsuz kod üzerinde ilgili metinlerin geçmediği doğrulanıyor). **GERÇEK VERİDEN kurulan üç widget:** *Hesap durumu* (e-posta doğrulama, hesap aktifliği, bu oturum / önceki giriş / son 30 günde giriş sayısı — hepsi `audit_log`'un gerçek `user.login` kayıtlarından, `login.php:25` zaten yazıyor; üyelik tarihi), *Kullanım özeti* (ekip/base/tablo/kayıt sayıları ve `attachments.file_size`'ın GERÇEK toplamı — tahmin değil; hepsi `$teamIds` ile KVKK-izole), *Hızlı erişim* (yalnızca gerçekten var olan sayfalara: Çalışma alanları, Yıldızlılar, Ana sayfa, ve `is_admin` ise Admin paneli — teste bağlandı: her linkin hedef dosyası diskte var mı diye kontrol ediliyor). **YAPILAN:** profil başlığı büyük avatar + ad/e-posta + durum rozetleri (fotoğraf değiştirme tetikleyicisi BİLEREK YOK — avatar yükleme özelliği yok, olmayan bir akışa işaret ederdi); alan satırları büyük harf sessiz etiket + hayalet "Düzenle" tetikleyicisi + ayraç çizgileri; ekipler ikonlu kartlara ve role göre tonlanan `.ac-role` haplarına döndü (**`.ws-collab-role` KULLANILMADI** — o sınıf `team_members.php`/`workspaces.php` ile paylaşılıyor); tehlikeli bölge yumuşak kırmızı zemin/kenarlık + ham metin link yerine gerçek `.ac-btn-danger` butonu. **İKİ GERÇEK BUG BULUNDU VE DÜZELTİLDİ (ikisi de `/browse` ile yakalandı):** (1) **`[hidden]` tuzağı** — `account-page.js` satır içi düzenlemeyi tamamen `hidden` özniteliğiyle yönetiyor; `.account-row-form`/`.account-row-display`'e verdiğim `display:flex` tarayıcı varsayılanının `display:none`'ını ezdiği için **sayfa açılır açılmaz TÜM düzenleme formları (şifre ve hesap silme dahil) AÇIK geliyordu**; `.sp-page [hidden] { display: none !important }` koruması eklendi. (2) **Yanlış güvenlik bilgisi** — `current_user()` (src/auth.php) yalnızca `id, email, full_name, is_admin, is_active, last_seen_notifications_at` döndürüyor; doğrulama rozetini `$user['email_verify_token'] === null` ile hesaplamak, tanımsız anahtar NULL sayıldığı için rozeti hesabın gerçek durumundan BAĞIMSIZ olarak **her zaman "doğrulandı"** yapıyordu. Kolonlar artık açıkça sorgulanıyor; token yazılıp/silinerek HTTP ile kanıtlandı ("doğrulanmadı" ↔ "doğrulandı"). Ayrıca `.account-card-profile`'ın home.css'teki `display:flex`i yeni başlıkla çakışıyordu (avatar tek başına uzun bir sütunda dikey ortalanıyordu), `display:block`a çevrildi. **Test — `_verify_settings_pages_ui.php` 80/80'e çıktı** (yeni H bölümü: uydurma widget yokluğu, hızlı erişim linklerinin gerçek dosyaya gitmesi, `[hidden]` koruması, 12 JS kancasının korunması, rozetin açık sorgudan okunması, `.ws-collab-role`'a dokunulmaması). **/browse canlı:** 8 işlev testi (satır içi düzenle/iptal, şifre formu aç/kapa, silme formu aç/kapa, şifre göster/gizle), dar ekranda tek sütuna inme, koyu tema. **Test yardımcısı notu — ÜÇÜNCÜ kez aynı ders:** "uydurma widget yok" kontrolleri ham metinde arayınca, hangi widget'ın neden YAPILMADIĞINI anlatan YORUMLARA takılıp yanlış KALDI veriyordu; `token_get_all()` ile PHP + HTML yorumları soyularak bakılıyor artık (grid-export.css `@media` ve mail.local.php `gmail.com` vakalarının aynısı). `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil.
  - **Aynı gün, otuz sekizinci adım — `slack_settings.php` entegrasyon yönetim ekranına dönüştü (DDL YOK, YENİ UÇ NOKTA YOK, SUNUCU TARAFI DEĞİŞMEDİ).** Sayfa aynı `.sp-page` tasarım sistemine alındı (`settings-page.css` + yeni `slack-settings.css`). **TASARIM SİSTEMİNE DÖRT YENİ ORTAK BİLEŞEN eklendi** (sayfaya özel dosyada değil, `settings-page.css`'te — sonraki sayfalar da kullansın): `.sp-status` durum hapı, `.sp-toggle` anahtarı, `.sp-note` bilgi kutusu, `.sp-code` maskeli-sır rozeti. **theme.css'e İKİ TOKEN EKLENDİ:** istenen "soft green" için palette yeşil yoktu; `--bcc-success` / `--bcc-success-soft` `danger` ikilisiyle AYNI desende, üç tema bloğuna da (açık / `[data-theme=dark]` / `prefers-color-scheme`) eklendi. Değişiklik **tamamen eklemeli** — `git diff` 9 ekleme / **0 silme**, hiçbir mevcut token veya kural değiştirilmedi, yani kabuğu paylaşan 26 sayfa etkilenmiyor (teste bağlandı: `--bcc-accent`/`--bcc-danger`/`--bcc-danger-soft` değerlerinin aynı kaldığı doğrulanıyor). **YAPILAN:** breadcrumb ikonlu/sessiz; başlık + açıklama satırı; üç açıklama bloğu düz paragraftan ikonlu, sol şeritli bilgi kutusuna döndü; durum sütunları yeşil "aktif" / gri "pasif" haplarına; maskeli webhook URL'leri monospace kod rozetine; **tüm** aksiyonlar (Düzenle/Sil/↑↓/Pasifleştir-Aktifleştir) ortak `.sp-icon-btn` hayalet ikon butonlarına (`settings-btn-sm` sayfada **sıfır** kaldı, teste bağlandı); "Aktif" ham checkbox'ları `.sp-toggle` anahtarına (`name="is_active" value="1"` sözleşmesi AYNEN korundu, teste bağlandı); webhook formu iki sütunlu grid; boş durumlar kesikli çerçeveli kutulara. **"Yeni kural ekle" ARTIK SATIR İÇİ:** dört alan (Alan/Koşul/Değer/Hedef webhook) + buton `settings-form-stacked` ile alt alta dizilmek yerine tek satırda yatay grid (1000px altında iki, 560px altında tek sütuna iner). `slack-routing.js`'e **dokunulmadı**; bağlı olduğu `#routing-rule-field` / `#routing-rule-value` id'leri korundu. **Test — `_verify_settings_pages_ui.php` 80/80 → 101/101** (yeni I bölümü: CSS bağlama, dört ortak bileşenin sayfaya özel dosyada TEKRARLANMAMASI, eski chunky butonların kalmaması, kural formunun yatay grid olması, routing-js kancaları, toggle sözleşmesi, yeni token'ların üç blokta da olması ve mevcut token'ların değişmemesi). **/browse + sunucu-taraflı canlı kanıt:** `slack-routing.js` hâlâ Alan→Değer dolduruyor (`[Trendyol, Yves Rocher, Hepsiburada]`); toggle tıklanabilir ve `name/value` korunmuş; kural pasifleştirme ve ↑ taşıma POST'ları DB'den doğrulandı (`Trendyol aktif=1→0`, `Yves Rocher pos1→pos0`); webhook ekleme/silme, **formun kendi ürettiği alanlarla** POST edilerek uçtan uca doğrulandı (2→3→2 webhook); koyu temada yeşil token doğru tonlanıyor (`rgb(108,193,126)`). **Not:** `/browse` daemon'ı bu tur bir kez daha çöktü (bilinen sorun) — ekleme/silme testi bu yüzden sunucu-taraflı HTTP'ye alındı, `restart` sonrası koyu tema ayrıca çekildi. `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil.
  - **Aynı gün, otuz dokuzuncu adım — `workspaces.php` iki sütunlu çalışma alanı panosuna dönüştü (DDL YOK, SUNUCU TARAFI DEĞİŞMEDİ).** Sağdaki ölü alanın **iki** kök nedeni vardı: (1) sayfa tek dar sütundu (kartlar üstte, detay altta) ve `.home-main`'in genişliğini dolduramıyordu; (2) katılımcılar **tek sütunlu sonsuz dikey liste**ydi. Çözüm: `280px / 1fr` iki sütunlu yerleşim (sol = çalışma alanı seçici, sağ = seçili alanın başlığı + aksiyonları + katılımcıları) ve katılımcılar `auto-fill minmax(280px,1fr)` çok sütunlu ızgaraya alındı. 980px altında tek sütuna iner, seçici yatay şeride döner. **PAYLAŞILAN `.ws-*` SINIFLARINA HİÇ DOKUNULMADI** — `.ws-collab-avatar` `grid.php`/`interface.php`/`team_members.php`/`grid-row-detail.js`, `.ws-collab-role` ve `.ws-detail` ise `team_members.php` tarafından kullanılıyor; sayfa artık ortak `.sp-avatar` / `.sp-role` bileşenlerini ve kendi `.wsx-*` sınıflarını kullanıyor (teste bağlandı: eski sınıflar hem CSS'te yeniden tanımlanmıyor hem markup'ta kullanılmıyor). **TASARIM SİSTEMİNE İKİ ORTAK BİLEŞEN eklendi:** `.sp-role` (dört rol için ayrı ton) ve `.sp-avatar`. **theme.css'e İKİ TOKEN EKLENDİ:** istenen "Owner: soft purple" için palette mor yoktu; `--bcc-role-owner` / `--bcc-role-owner-soft`, success ikilisiyle AYNI desende üç tema bloğuna da eklendi — `git diff` **11 ekleme / 0 silme**, hiçbir mevcut token değişmedi. Diğer roller mevcut token'lardan: editor=accent (mavi), commenter=success (yeşil), viewer=sunken (slate). Canlı ölçüldü: `owner rgb(241,235,250)`, `editor rgb(232,240,254)`, `viewer rgb(250,250,251)`. **UYDURULMAYAN ÖZELLİKLER — bilinçli:** (a) istenen "+ Yeni Çalışma Alanı" kartı **herkese gösterilmiyor** — çalışma alanı = takım ve takım oluşturmak `admin/create_team.php`'nin işi (`require_admin()`); kart yalnızca `is_admin` olana, o gerçek sayfaya link olarak basılıyor, aksi hâlde sıradan kullanıcıyı 403'e götüren ölü bir vaat olurdu. (b) istenen "rol değiştir dropdown / üyeyi çıkar" bu sayfada **yok** — `team_members.php`'nin işi (`assign`/`remove` aksiyonları orada); satır üstünde beliren kısayol sahte bir dropdown değil, o sayfaya giden **gerçek** link. (c) "Ayarlar" butonu **hâlâ devre dışı** — çalışma alanı ayarları diye bir özellik yok (bu dosyanın kendi başlık yorumundaki onaylanmış karar); çalışıyormuş gibi göstermek yanıltıcı olurdu, `title` ile nedeni yazıldı. **ÖLÜ BUTON GERÇEK OLDU:** "Oluştur" hiçbir şey yapmayan `disabled` bir butondu — artık base oluşturmanın gerçek sayfası olan `bases.php`'ye giden "Base oluştur" bağlantısı. "Paylaş" ise birincil butona yükseltilip ne yaptığını söyleyen adı aldı ("Katılımcıları yönet"). **ARAMA:** 8'den uzun katılımcı listesinde `workspaces.js` çalışma anında ad+e-posta üzerinde filtreleyen bir kutu ekliyor (`table-fields.js` ile aynı desen; filtre `[hidden]` değil ayrı sınıf — grid öğesinde `display` `[hidden]`ı ezer). **Test — `_verify_settings_pages_ui.php` 101/101 → 130/130** (yeni J bölümü: kapsam, beş paylaşılan `.ws-*` sınıfının hem CSS'te hem markup'ta kullanılmaması, ortak bileşenlerin sayfaya özel dosyada tekrarlanmaması, uydurma özellik koruması — hover kısayolunun gerçek link olması ve markup'ta `<select>` bulunmaması, admin kartının `is_admin` koşulu içinde ve hedefinin gerçekten var olması, "Ayarlar"ın hâlâ disabled kalması, ızgara ve arama davranışı, yeni token'ların üç blokta olması). **/browse canlı:** arama ad+e-postada çalışıyor (`test.kullanici1`→4, `export`→1, eşleşmeyen→0 + boş mesajı), rol hapı renkleri, hover kısayolunun varsayılan `opacity:0` olması, disabled butonun `cursor:not-allowed`'ı, dar ekranda tek sütuna inip seçicinin yatay olması, koyu tema. **Test yardımcısı notu:** admin kartı kontrolündeki tek parça regex kırılgandı (`[^)]*` açgözlü davranıp `=== 1`i yutuyor, doğru markup'ta bile KALDI veriyordu) — iki net kontrole bölündü. `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil.
  - **Aynı gün, kırkıncı adım — `form_edit.php` iki sütunlu form oluşturucuya dönüştü (DDL YOK, SUNUCU TARAFI DEĞİŞMEDİ).** Tek dikey liste `1fr / 360px` iki sütuna alındı: sol = paylaşım kutusu + form ayarları + Kaydet, sağ = forma girecek alanlar (yapışkan). **TEK `<form>`** — iki sütun onun içinde, "Kaydet" hepsini birlikte gönderiyor; alan adları (`form_enabled`/`form_title`/`form_description`/`form_success_message`/`form_slack_notify`/`form_fields[]`) AYNEN korundu. Paylaşım kutusu durum hapı (AÇIK/KAPALI), monospace link alanı, ikonlu kopyalama butonu ve **amber uyarı kutusu** aldı; ham checkbox'lar `.sp-toggle` anahtarına, alan listesi ikonlu seçilebilir kartlara döndü. **theme.css'e İKİ TOKEN EKLENDİ:** amber için `--bcc-warning` / `--bcc-warning-soft` (success ve role-owner ile AYNI desende, üç tema bloğuna da) — `git diff` **10 ekleme / 0 silme**. Uyarı kutusu (`.sp-note--warn`) ORTAK dosyada. **ÜÇ GERÇEK SORUN BULUNDU, ÜÇÜ DE `/browse` İLE:** (1) **Alan tipi ikonları hiç görünmüyordu** — devraldığım markup `.field-badge` kullanıyor ama o sınıf `style.css`'te tanımlı ve bu sayfa `style.css` YÜKLEMİYOR (kabuk yalnızca theme.css + home.css + sayfaya özel dosyayı basıyor); `theme.css`'te tanımlı `.field-type-badge`'e geçildi. Bu benim değil, **devraldığım** bir kusurdu. (2) **`:has()` dinamik durumda çalışmıyor** — ilk sürüm seçili kartı `.fe-field-card:has(input:checked)` ile boyuyordu ve uygulanmıyordu; ölçümle daraltıldı: selector DOĞRU eşleşiyor (`matches()` true), önceliği de yeterli, `:has()` desteği de var (enjekte edilen kural çalıştı) — ama `checked` DEĞİŞTİĞİNDE stil geçersizleştirme tetiklenmiyor; eleman DOM'dan çıkarılıp geri konunca kural anında uygulanıyordu. Çözüm: kart görünümü `<label>`'dan girdinin **hemen sonraki kardeşi** `.fe-field-inner`'a taşındı, `input:checked + .fe-field-inner` klasik kardeş geçersizleştirmesini kullanıyor. (3) **Kopyalama butonuna `<svg>` konulamaz** — paylaşılan `share-popover.js` kopyalamada `btn.textContent`'i "Kopyalandı" ile değiştiriyor, bir svg çocuk o anda silinirdi; ikon CSS `::before` **maskesiyle** veriliyor (pseudo-element textContent'ten etkilenmiyor) ve o dosyaya hiç dokunulmadı. Yeşil "kopyalandı" parlaması için `form-edit.js` AYNI butona ikinci, **yalnızca görsel** bir dinleyici bağlıyor — panoya yazma mantığı kopyalanmadı (teste bağlandı: `clipboard`/`execCommand` bu dosyada geçmiyor). **BONUS DÜZELTME:** herkese açık form linki `$_SERVER['HTTP_HOST']` yerine `bcc_app_base_url()` kullanıyor — `register.php`'deki AYNI düzeltme; link dışarıya paylaşılmak için üretildiğinden istek host'una dayanması hem yanlış adres üretebilir hem host-header enjeksiyonuna kapı bırakır. **Test — `_verify_settings_pages_ui.php` 130/130 → 156/156** (yeni K bölümü: kapsam, `share-popover.js`'in değişmemesi ve üç kancasının korunması, kopyala butonunda `<svg>` bulunmaması, `::before` maskesi, `form-edit.js`'in kopyalama mantığı içermemesi, CSS kurallarında `:has()` kalmaması, kardeş seçici ve markup sırası, doğru ikon sınıfı, altı form alanının korunması, tek `<form>`, `bcc_app_base_url()`, yeni token'lar). **/browse canlı:** alan seçimi (accent kenarlık `rgb(45,127,249)` + dolu tik + sayaç), **kaydetme turu DB'den doğrulandı** (`form_fields` = Ad/Marka/Tarih, `form_slack_notify=1`, `form_enabled=1` — yenilemeden sonra seçili geldi), kopyalama (metin "Kopyalandı" + ikon ayakta + yeşil parlama), koyu tema (amber `rgb(58,47,24)`, seçili kart `rgb(28,43,77)`), dar ekranda tek sütun. **Test yardımcısı notları:** (a) `[^>]*` kalıbı girdi etiketinin içindeki PHP echo bloğunun `>`'ünde duruyordu; (b) **bir `//` yorumunun içine PHP kapanış etiketi yazmak PHP modunu KAPATIYOR** ve dosyanın kalanını düz metin olarak bastırıyordu — açıklama blok yoruma çevrildi. `php -l` + `node --check` temiz; 19/19+8/8+6/6+67/67+65/65+25/25 yeşil.

---

## 6. Kalan İşler

**"Kaydı gönder" tamamlandı** (mail backend + arayüz + gerçek gönderim, Bölüm 5 sekiz/dokuz/onuncu adımlar) — şu anda bilinen bir kalan iş yok. Bilinçli kapsam dışı bırakılan: gönderim geçmişi loglama (istenirse ayrı bir iş).

**Alan tipi çalışmasının TAMAMI bitti — Grup B1, B2, C1, C2 ve A tamamlandı; bu başlıkta bilinen bir kalan iş YOK.** Created/Last modified time-by (Grup B1+B2, Bölüm 5 yirmi birinci/yirmi ikinci adımlar), Currency/Percent/Rating (Grup C1, yirmi üçüncü adım), Autonumber (Grup C2, yirmi dördüncü adım) ve URL/E-posta/Telefon (Grup A, yirmi beşinci adım) TAMAMLANDI. Toplam **on bir** yeni alan tipi eklendi (B1: 2, B2: 2, C1: 3, C2: 1, A: 3); `$GLOBALS['BCC_FIELD_TYPES']` artık 10 yerine **21** tip içeriyor. **DDL bilançosu:** yalnızca İKİ grup gerçek DDL gerektirdi — B2 (`migrations/013`, `records.updated_by`) ve C2 (`migrations/014`, `fields.autonumber_next`); B1, C1 ve A hiç şema değişikliği gerektirmedi (mevcut `value_*` kolonları paylaşıldı, fark yalnızca `fields.options` JSON'u ve/veya görüntüleme katmanı). **Yeni tip eklerken atlanmaması gereken ALTI yer** (dört ayrı turda dördü de en az bir kez unutuldu, hepsi bug oldu): `BCC_FIELD_TYPES` · `BCC_FIELD_VALUE_COLUMN` · `BCC_FILTER_OPERATORS` + `filter_condition_sql()`'in `$isTextLike`/sayısal dalı · **`BCC_GROUP_DIR_LABELS`** (C1/C2'de unutuldu, Grup A turunda notice bug'ı olarak yakalanıp düzeltildi) · **`theme.css`'te `--field-icon`** (C1'de unutuldu, rozet boş kutu çiziliyordu; Grup A turunda düzeltildi) · **`BCC_DUPLICATE_SUFFIX_FIELD_TYPES`** — çoğaltmada birincil alana " copy" eki eklenen tiplerin whitelist'i. ⚠️ Bu **whitelist**, yani yeni bir tip buraya yazılmazsa ek ALMAZ; bu kasıtlı olarak güvenli taraf, ama yeni bir SERBEST METİN tipi eklenirse elle eklenmesi gerekir. Karar eskiden tip değil KOLON bazlıydı (`$primaryColumn === 'value_text'`) ve `value_text`'i yedi tip paylaştığı için biçim sözleşmesi olan dört tipi bozuyordu (bkz. yirmi altıncı adım). Her grubun kendi canlı doğrulama betiği var: `scripts/_verify_group_c1.php` (54/54), `_verify_group_c2.php` (66/66), `_verify_group_a.php` (79/79), `_verify_duplicate_suffix.php` (27/27), `_verify_view_form.php` (78/78), `_verify_view_kanban.php` (68/68), `_verify_reorder_atomicity.php` (52/52).

**Görünüm türleri — ortak temel + Form TAMAMLANDI; sıradaki adımlar Kanban ve Calendar.** Grup View-Form (Bölüm 5, yirmi yedinci adım) ile `views.view_type` ölü bir kolondan gerçek bir dallanma noktasına dönüştü: `$GLOBALS['BCC_VIEW_TYPES']` whitelist'i, `bcc_view_route_for()` tek yönlendirme noktası, "+ Yeni oluştur..." tip seçici (liste whitelist'ten DÖNER) ve ilk somut tür olarak **Form** (herkese açık, anonim, `form_token` ile). **Yeni bir görünüm türü eklerken atlanmaması gereken DÖRT yer** (alan tiplerindeki aynı sınıf hatanın görünüm karşılığı, `BCC_VIEW_TYPES` yorumunda da yazılı): `BCC_VIEW_TYPES` · **`theme.css`'te `.view-type-badge--<tür>` ikonu** (`--view-icon` tanımsızsa rozet BOŞ kutu çizilir) · `bcc_view_route_for()` · `view_create.php`'nin tür-özel kurulum dalı. **Kanban TAMAMLANDI** (Bölüm 5, yirmi dokuzuncu adım) — DDL gerekmedi, `views.config` yeterli oldu; sürükle-bırak `cell_update.php`'ye bağlandı, yeni uç nokta yazılmadı.

> **SIRADAKİ HIZLI EK TUR — "Sütuna kart ekle" (Kanban).** Bu turda BİLEREK dışarıda bırakıldı. Kanban UX'inin çekirdeğine yakın ama `record_add.php` + `cell_update.php` ardışık İKİ çağrı gerektiriyor (ya da tek bir yeni uç nokta): sütunun `+` butonu boş bir kayıt oluşturup o kaydın `single_select` hücresine sütunun değerini yazmalı. Karar noktası: iki ardışık çağrı mı (mevcut uç noktalar, ama araya düşen hata "sütunsuz kayıt" bırakır) yoksa tek transaction'lı yeni bir uç nokta mı. **Bu, Calendar'dan ÖNCE yapılacak.**

**Calendar için (henüz başlanmadı):** hangi `date` alanına göre yerleşeceği (`views.config`) + ay/hafta görünümü. DDL gerektirmemeli — Form'un DDL gerektirmesinin tek sebebi herkese açık link SIRRIydı.

**Sütun içi sıralama (Kanban) BİLEREK KAPSAM DIŞI ve bu bir tercih değil, bir BULGU:** projede kayıtlar için HİÇBİR yeniden sıralama mekanizması yok — `bcc_reorder_sibling()`'in whitelist'i `tables_meta/fields/views/slack_routing_rules`, `records` KASITLI olarak dışarıda; grid'de satır sürükleme de yok. `records.position` yalnızca ekleme sırası ve "araya ekle" kaydırmasıyla yazılıyor. Eklemek = yeni uç nokta + konumlandırma stratejisi + "sort kuralı aktifken elle sıra ne anlama geliyor" sorusu.

- **"..." (Diğer seçenekler) menüsü — 4 kalemin güncel durumu:**
  - **Kaydı yazdır** → TAMAM, işlevsel (Bölüm 5, yedinci adım — `window.print()` + temiz tek-kayıt çıktısı).
  - **Kaydı gönder** → TAMAM, işlevsel (Bölüm 5, sekiz/dokuz/onuncu adımlar — modal + rol/domain kontrollü PHPMailer backend'i).
  - **Kaydı çoğalt** → TAMAM (işlevsel: editor+ rol kontrolü backend'de, normal alanlar kopyalanır, birincil alana ' copy' eklenir, otomatik alanlar sıfırlanır, yorumlar/ekler taşınmaz, kopya alta eklenir ve modalda açılır — Bölüm 5, on birinci adım).
  - **Kaydı sil** → TAMAM, zincirin TAMAMI bitti (Bölüm 5 on üçüncü/on dördüncü/on beşinci adımlar): soft-delete işaretleme + onay diyaloğu + rol kontrolü (editor+owner silebilir/geri yükleyebilir) + grid/filtre/arama/sıralama/export'un silinen kaydı gizlemesi + hesap menüsündeki Çöp Kutusu'nda "Kayıtlar" bölümü (kim/ne zaman/hangi tablo + Geri Yükle) + 7 gün sonra otomatik kalıcı silme (ziyaret-anında kontrol, cron yok). Kapsam dışı bırakılan: manuel "çöpü boşalt" (bases Trash'inde de yok, tutarlı).
- **`vendor/` .gitignore'da** — projeyi yeni çeken biri `composer install` çalıştırmalı (composer.json/composer.lock commit'te, vendor/ değil).

**Tamamlandı (bu oturum):** Önceki turun "Gelecek İşler" notu — "kendi seviyende veya altında davet et" kuralı (Airtable: dört rolün hepsi, Read-Only dahil, kendi seviyesinde/altında davet edebilir, eşit dahil — bkz. support.airtable.com/docs/base-permissions + managing-billable-collaborators FAQ, /browse ile bu oturumda doğrulandı) — artık UYGULANIYOR. `team_members.php` owner-only kısıtlamasından çıktı (`require_role($teamId, 'viewer')`), `$myRank` `current_user_role_in_team()`'den okunuyor, `bcc_assignable_roles()` (src/auth.php) ortak mantığı taşıyor. Airtable'ın Share popup'ı (grid.php'de "Paylaş" — Launch'ın soluna eklendi) + tam Collaborators paneli (team_members.php: arama/rol filtresi/CSV export/"Ekleyen" (audit_log'dan türetilir, yeni kolon YOK)/"Eklenme tarihi" sıralama/toplu çıkarma) paritesi kuruldu. Gerçek e-posta ile yeni hesap davet etme hâlâ YOK (kapsam dışı bırakılmaya devam ediyor) — popup'taki atama kutusu yalnızca var olan aktif kullanıcılar arasında arama yapar.

**Bilerek yapılmayanlar:** Automations / Forms / Interfaces sekmeleri grid üst barında hiç YOK. Grid üst barındaki Share ailesi artık ÜÇ buton: "Paylaş" (Airtable Share paritesi, YENİ), "Bağlantı" (eski "Share" — view-link kopyalama, adı değişti karışmasın diye) ve interface.php'nin ayrı "Share and Sync" butonu (view share link, DOKUNULMADI). `login`/`register` auth shell refactoru (kazanç/risk oranı düşük). Base kartındaki ⋯ (diğer aksiyonlar) menüsü — GEREKSINIMLER.md'de tanımlı değil, ikon görünür ama tıklanınca hiçbir şey yapmaz (yalnızca kart-navigasyonunu tetiklememesi için `stopPropagation` var, başka işlevi yok). Sol alt köşede bir "yardım (?)" / "gstack benzeri" ikon YOK (araştırıldı, bkz. Biten İşler) — istenirse ayrı bir iş olarak açılabilir.

**Bilinen küçük kusur:** `bcc_sanitize_rich_text()` — sanitizer'a tarayıcı editörü DIŞINDAN (doğrudan API çağrısıyla) çıplak `<`/`>` içeren düz metin (ör. `"5 < 10 & 3 > 1"`) gönderilirse, `&lt;`/`&gt;` olarak korunması beklenirken libxml'in ayrıştırıcısı bu diziyi geçersiz bir etikete benzetip sessizce düşürüyor (`"5  1"` çıkıyor) — güvenlik açığı DEĞİL (hiçbir etiket/script sızmıyor), yalnızca metin sadakati kaybı; gerçek yol (grid.js'in contenteditable editörü) bu karakterleri tarayıcı tarafından zaten `&lt;`/`&gt;` kaçırılmış gönderdiği için pratikte hiç tetiklenmiyor.

---

## 7. Çalışma Yöntemi

- Kod **Claude Code** yazar; Claude (bu sohbet) danışman — brifing hazırlar, onay ekranlarını inceler
- Komutlar **tek tek** onaylanır: hep `1 = Yes`. Toplu izin (`2`) **ASLA**
- Silme öncesi: "bunu kullanan kaldı mı?" grep kontrolü
- Onay ekranlarında kod sık **kırpık** görünür (önizleme) → şüphede `php -l`
- Tüm PHP komutlarında `C:/php73/php.exe` — düz `php` XAMPP'in 8.2 CLI'ı
- **Yeni panel eklerken eski işlevsiz butonu SİLMEYİ ilk adım yap** (üç kez unutuldu)
- CSS değişince tarayıcıda **Ctrl+Shift+R** (önbellek iki kez yanılttı)

**Test betikleri** (her büyük değişiklikten sonra):
```
C:/php73/php.exe scripts/test_isolation.php            → 6/6
C:/php73/php.exe scripts/_verify_phase4_sort_search.php → 8/8
C:/php73/php.exe scripts/_verify_phase4_filter.php      → 19/19
```

**Git akışı** (her özellik sonunda, ayrı cmd'den):
```
cd C:\xampp\htdocs\bcc-core
git add <değişen dosyaları tek tek say>
git commit -m "kisa aciklama"
git push
```
`git add .`/`git add -A` KULLANILMAZ (2026-07-30'da düzeltildi) — istemeden ilgisiz/
gizli bir dosyayı (ör. yanlışlıkla oluşan bir `config/database.local.php`) commit'e
eklemeyi önler, `git status` ile ne eklendiği her zaman gözle görülür.
Geri alma: `git checkout .` (son commit'e döner)

### Yeni sohbet akışı (token tasarrufu)

Uzun sohbetlerde her mesaj tüm geçmişi taşır. Bu yüzden **her özellik için yeni sohbet**:

1. Yeni sohbet aç, `docs/PROJE-DURUM.md` ve `docs/YAPILACAKLAR-UI.md` dosyalarını **ekle** (ataç ikonu)
2. "Şimdi şu maddeyi yapacağız, Claude Code için brifing hazırla" de
3. Özellik bitince Claude Code'a dokümanları güncellettir:
   ```
   [Özellik] tamamlandı ve test edildi. İki dokümanı güncelle:
   1) docs/YAPILACAKLAR-UI.md — ilgili bölümü tamamen sil, kalan maddeleri yeniden numaralandır.
   2) docs/PROJE-DURUM.md — "Biten İşler"e bir satır ekle, "Kalan İşler" tablosundan sil.
   Başka hiçbir şeye dokunma.
   ```
4. Commit'le, sohbeti kapat

**Sohbet içinde tasarruf:**
- Lint / grep / "sunucu ayakta mı" gibi rutin onaylarda doğrudan `1` de, sorma
- Sadece **silme içeren**, **şüphelendiğin** veya **anlamadığın** edit'leri danış
- Onay ekranlarındaki uzun SVG kodlarını kırpabilirsin

**Airtable görselleri:** Saklamaya gerek yok. Bir ekranı Airtable'a benzetirken
o an ekran görüntüsü alıp sohbete at — hesaba zaten erişim var.

---

## 8. Sorun Giderme

**"Sonsuz yüklenme" / bağlantı hatası** → MySQL takılmıştır.
XAMPP'te **Stop → 5 sn bekle → Start**. "MySQL yeşil" ≠ "MySQL sağlıklı".
Doğrulama: tarayıcıdan `localhost/diag.php` → "Bağlantı başarılı".

**Şifre sıfırlama** (kullanıcı kendi çalıştırır, şifre paylaşılmaz):
```
cd C:\xampp\htdocs\bcc-core
C:\php73\php.exe -r "require 'config/database.php'; $e='EPOSTA'; $p='YENI_SIFRE'; $h=password_hash($p,PASSWORD_DEFAULT); bcc_execute('UPDATE users SET password_hash=:h WHERE email=:e', array(':h'=>$h,':e'=>$e)); echo 'ok';"
```

**Test kullanıcısı oluşturma:** Admin paneli → Yeni Kullanıcı (aktif gelir) → Ekibe Ata.
Temizlik: `DELETE FROM users WHERE email LIKE '%@bcc-test.local'`

**Klasik hata:** `cd C:\xampp\htdocs\bcc-core` yapmadan komut çalıştırmak.

---

## 9. Devir/Deploy Notları

**`src/bootstrap.php`'deki session cookie ayarı** — `secure => false` yalnızca
localhost/HTTP içindir (kodun kendi yorumu da bunu söylüyor). Proje HTTPS
arkasına alınarak canlıya çıkarken bu **`true` yapılmalı**, yoksa oturum
çerezi HTTPS üzerinden de şifresiz (secure olmayan) bağlantılarla
paylaşılabilir hale gelir.
