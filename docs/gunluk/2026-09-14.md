# 2026-09-14

Üç iş yapıldı: kullanıcının "grid içi değişiklikler Slack'e düşüyor mu?"
sorusunun salt-okunur teşhisi (§1, **kod değişikliği yok**), 08-09 Eylül'den
beri çalışma ağacında bekleyen değişikliklerin **commit edilip push'lanması**
(§2) ve gridde **Tab ile hücreler arası geçiş** (§3, tek dosya + yeni test).

## Yapılanlar

### 1. "Grid içi değişiklikler Slack bildirimlerine düşüyor mu?" — teşhis

- **Soru:** Kullanıcı localhost'ta gride girip değişiklik yaptı, mesajın
  gidip gitmediğini sordu.
- **Yöntem:** Yalnızca **salt-okunur sorgular** (`audit_log`, `records`,
  `slack_webhooks`, `slack_watched_fields`) + `bcc_slack_pending_records()` /
  `bcc_find_slack_webhook()` çağrıları. Hiçbir veri yazılmadı, hiçbir dosya
  değiştirilmedi. `bcc_slack_flush_table()` **bilerek çağrılmadı** — o
  fonksiyon kayda damga yazar, yani ölçüm aracı değil.

#### Ölçülen durum (saat 08:54)

| Kayıt | Son dokunuş | Damga |
|---|---|---|
| 112095 | 08:52:19 | NULL — bekliyor |
| 112094 | 08:52:36 | NULL — bekliyor |

`bcc_slack_pending_records(2992, …, 0)` → **2 kayıt**,
`bcc_slack_pending_records(2992, …, 180)` → **0 kayıt**.

**Cevap: sistem çalışıyor, mesajlar sıraya girmişti — sadece bekleme süresi
dolmamıştı.** Hata yok; 09-09'da kurulan kuralın kendisi bu (son yazımdan
sonra 180 sn boşta kalma). İki tetikleyiciden biri bekleniyordu: gridden
çıkış (`pagehide` → bekleme 0) ya da sayfada kalınırsa 60 sn'lik yoklamanın
180 sn'yi yakalaması.

#### Yapılandırma teyidi

| Kontrol | Sonuç |
|---|---|
| `bcc_find_slack_webhook(2992, 214)` | id **193**, `#social`, ekip geneli, aktif |
| `slack_watched_fields` (tablo 2992) | **7 alan** — 8952, 8953, 8954, 8956, 8957, 9037, 9038 |
| 09-09'dan bugüne kaçan mesaj | **yok** — arada gride hiç dokunulmamış |

#### Canlı teyit (birkaç dakika sonra ölçüldü)

```
audit_log: 2026-09-14 08:55:34  slack.notify_sent
audit_log: 2026-09-14 08:56:46  slack.notify_sent
records 57525 → damga 2026-09-14 08:56:46
```

Kullanıcı gride devam ederken bekleyen özetler **kendiliğinden gitti**.
09-09 §2'deki ayrılma-pingi düzeltmesi ve 180 sn'lik yoklama zinciri
üretimde çalışıyor; `slack.notify_failed` değil `notify_sent` yazıldığı için
webhook 2xx döndü, mesajlar gerçekten `#social` kanalına düştü.

- **Değişen dosyalar:** **yok.**
- **Değişen veritabanı:** **yok** (yalnızca `SELECT`).
- **Test:** teşhis; ayrıca `_verify_slack_integration.php` A+B bölümleri
  **19/19 GEÇTİ** (C bölümü yine demo ekibi olmadığı için atlandı — 09-09'da
  da böyleydi).

### 2. 08-09 Eylül çalışması commit edildi ve push'landı

- **İstek:** "Bu değişikliği de commit ve push eder misin."
- **Durum:** 09-08 ve 09-09 oturumlarının kodu **tarayıcıda doğrulanmadığı
  için bilerek commit'lenmemişti** (bkz. `docs/DEGISEN-DOSYALAR-VE-TABLOLAR.md`
  §2.2). Doğrulama 09-09 §4'te geldi, bugün §1'de bir kez daha üretim
  koşullarında görüldü — engel kalmadı.
- **Yapılan:** Çalışma ağacındaki 19 değiştirilmiş + 6 yeni dosya, karışık tek
  bir commit yerine **konuya göre dört commit**e ayrıldı. `git add .`
  kullanılmadı; dosyalar tek tek eklendi, her commit öncesi `git status` +
  `git diff --cached` ile bakıldı.

| # | Commit | Kapsam |
|---|---|---|
| 1 | `feat(slack)` | Toplu bildirim sistemi — `src/slack.php`, `public/api/{cell_update,cells_bulk_update,record_add,record_duplicate,table_import_xlsx,slack_flush}.php`, `public/{grid,interface,kanban}.php`, `src/schema.php`, `schema.sql`, `public/assets/grid-slack-flush.js`, `scripts/_verify_{slack_batch,slack_integration}.php`, `docs/CANLIYA-ALMA.md` |
| 2 | `fix(grid)` | Toplu silme kalıcı silmek yerine çöp kutusuna taşıyor — `public/api/record_delete.php`, `public/assets/grid-row-detail.js`, `scripts/_verify_trash_purge_attachments.php` |
| 3 | `fix(ui)` | Hata sayfası hizalaması — `src/partials/error_page.php`, `public/assets/login.css`, `scripts/_verify_error_pages.php` |
| 4 | `docs` | `docs/gunluk/2026-09-08.md`, `docs/gunluk/2026-09-09.md`, `docs/gunluk/2026-09-14.md`, `docs/DEGISEN-DOSYALAR-VE-TABLOLAR.md` |

- **Sır taraması (commit öncesi zorunlu):** `git diff` + **takip edilmeyen
  altı dosya ayrıca** tarandı (`git diff` onları göstermez). Aranan:
  `hooks.slack.com/services/…` gerçek webhook adresi, `password=`, `api_key`,
  `secret=`, `token=`, özel anahtar başlıkları. **Tek isabet**
  `docs/gunluk/2026-09-08.md:30`, o da gerçek bir adres değil kodun
  `https://hooks.slack.com/` ön ek kontrolünü anlatan cümle. Gerçek webhook
  URL'i hiçbir dosyada yok — yalnızca veritabanında duruyor.
- **Test:** Değişen 17 PHP dosyasının hepsinde `C:/php73/php.exe -l` **temiz**.
  `_verify_slack_integration.php` 19/19. Kod 09-09'da tam yeşil turla test
  edilmişti ve o günden beri **hiçbir satır değişmedi**.


### 3. Grid'de Tab ile hücreler arası geçiş

- **İstek:** "Gridde herhangi bir hücre seçili iken Tab ile soldan sağa hücreler
  arası geçiş yapabilelim."
- **Mevcut durum:** `grid-cell-select.js` ok tuşlarını (`ArrowUp/Down/Left/Right`),
  `Shift+ok` ile aralık genişletmeyi, `Ctrl+A` ile tümünü seçmeyi ve `Escape` ile
  temizlemeyi zaten destekliyordu. **`Tab` hiç ele alınmıyordu** — tarayıcının
  varsayılan davranışı çalışıyor, yani odak gridden çıkıp araç çubuğundaki bir
  sonraki düğmeye atlıyordu.
- **Yapılan:** `keydown` dinleyicisine `Tab` dalı eklendi.

  | Tuş | Davranış |
  |---|---|
  | `Tab` | Bir sağdaki hücre. Satırın sonundaysa **bir alt satırın ilk hücresi** |
  | `Shift+Tab` | Bir soldaki hücre. Satırın başındaysa **bir üst satırın son hücresi** |
  | Son satırın son hücresinde `Tab` | **Yerinde kalır** — tablo dışına çıkmaz |
  | İlk satırın ilk hücresinde `Shift+Tab` | Yerinde kalır |
  | Çoklu seçim varken `Tab` | Seçim **teke iner**, odak hücresinden devam eder |

  Üç ayrıntı bilerek böyle:
  1. **`e.preventDefault()` çağrılıyor** — yoksa tarayıcı seçimi taşırken bir
     yandan odağı da araç çubuğuna atardı, iki imleç birden oynardı.
  2. **Dal `keyboardBelongsToGrid()` guard'ından SONRA** duruyor. Yani hücre
     **düzenleme modundayken** (`td.editing`, input/textarea odakta) Tab'a
     dokunulmuyor — oradaki mevcut davranış aynen korunuyor.
  3. **Sarmalama sınırda durur.** `wrapCell()` tablo dışına taşarsa `null`
     döndürüyor; alternatifi (başa dönmek) 150 satırlık bir tabloda kullanıcıyı
     habersizce en üste fırlatırdı.
- **Değişen dosyalar:**
  - `public/assets/grid-cell-select.js` — yeni `wrapCell(row, step)` yardımcısı
    (`cellAt`'in hemen altında); `keydown` dinleyicisinde `ARROWS` bloğunun
    ÖNÜNE `Tab` dalı
  - `scripts/_verify_grid_tab_nav.php` — **YENİ**, 18 kontrol
- **Test:** `_verify_grid_tab_nav.php` **18/18 GEÇTİ** + **gerçek tarayıcıda
  ölçüldü.** Kaynak taraması tek başına bir klavye davranışını kanıtlamaz, bu
  yüzden `grid-cell-select.js`'in aynı kopyası 3×3'lük bir DOM fikstürüne
  (gerçek `tr[data-record-id]` / `td.grid-cell` yapısı, `src/schema.php:1467`
  ile aynı sınıflar) yüklenip headless Chromium'da gerçek tuşlar gönderildi:

  | Ölçüm | Sonuç |
  |---|---|
  | `a1`'den 4 kez Tab | `b1` → `c1` → **`a2`** → `b2` (sarmalama çalışıyor) |
  | Ardından 3 kez Shift+Tab | `a2` → **`c1`** → `b1` (geri sarmalama) |
  | `c3` (son hücre) + Tab | `c3` — yerinde kaldı, `document.activeElement` **BODY** (odak kaçmadı) |
  | `a1` + Shift+Tab | `a1` — yerinde kaldı, odak BODY |
  | Shift+ok ile 4 hücre seçip Tab | Seçim **1** hücreye indi, çapa `c2` |

  `C:/php73/php.exe -l` + `node --check` temiz. **Veritabanına hiç
  dokunulmadı** — fikstür statik bir HTML dosyası, oturum açılmadı.

- **⚠️ Erişilebilirlik ödünü (bilerek):** Bir hücre seçiliyken Tab artık gridin
  içinde kalıyor, yani klavyeyle araç çubuğuna geçmek için önce **`Escape`** ile
  seçimi temizlemek gerekiyor. Elektronik tablo uygulamalarının standart
  davranışı bu; çıkış yolu (`Escape`) zaten vardı ve teste bağlandı.


## Değişen dosyalar ve veritabanı — 2026-09-14

### Bugün YAZILAN kod (tek iş, §3)

| Dosya | Satır | Ne değişti |
|---|---|---|
| `public/assets/grid-cell-select.js` | **+34** | Yeni `wrapCell(row, step)` yardımcısı (`cellAt`'in hemen altında); `keydown` dinleyicisinde `ARROWS` bloğunun ÖNÜNE `Tab` dalı. Dosyanın geri kalanına dokunulmadı |
| `scripts/_verify_grid_tab_nav.php` | **+127 (YENİ)** | 18 kontrol: Tab dalı, `preventDefault`, `shiftKey` yönü, sarmalama, guard sırası, ok tuşlarının bozulmadığı, `grid.php`'nin betiği bastığı |

**Bu ikisi dışında bugün hiçbir kod dosyasına dokunulmadı.**

### Bugün SADECE COMMIT'LENEN kod (§2 — 08-09 Eylül'de yazılmıştı)

Bu dosyalar bugün **değiştirilmedi**, yalnızca git'e alındı. Ayrıntıları
`docs/DEGISEN-DOSYALAR-VE-TABLOLAR.md` §2 ve §3'te:

| Commit | Dosya sayısı | Kapsam |
|---|---|---|
| `26f0d44` | 16 (4'ü yeni) | Slack toplu bildirim sistemi |
| `23d27d5` | 3 | Toplu silme → çöp kutusu |
| `cbcae46` | 3 | Hata sayfası hizalaması |

### Belgeler

| Dosya | Ne |
|---|---|
| `docs/gunluk/2026-09-14.md` | **YENİ** — bu dosya |
| `docs/gunluk/2026-09-08.md`, `docs/gunluk/2026-09-09.md` | Vardı, bugün git'e alındı |
| `docs/DEGISEN-DOSYALAR-VE-TABLOLAR.md` | Vardı, bugün git'e alındı; sonuna 2026-09-14 bölümü eklendi |
| `docs/PROJE-DURUM.md` | §5 "Biten İşler"e turun özeti (+41 satır) |

### Veritabanı — BUGÜN HİÇBİR DEĞİŞİKLİK YOK

**Ne yapısal (DDL) ne veri değişikliği yapıldı.** Bugün çalıştırılan her sorgu
`SELECT`'tir.

| Tablo | Neden okundu | Yazıldı mı |
|---|---|---|
| `audit_log` | gönderim denendi mi, ne zaman | **hayır** |
| `records` | bekleyen kayıt / damga durumu | **hayır** |
| `slack_webhooks` | mesaj nereye gidiyor | **hayır** |
| `slack_watched_fields` | hücre bildirimi hangi tabloda açık | **hayır** |
| `cell_values` | `bcc_slack_pending_records()` içinden (okuma) | **hayır** |

⚠️ İki ayrım önemli:

1. **§1'deki teşhis sırasında `bcc_slack_flush_table()` BİLEREK çağrılmadı** —
   o fonksiyon kayda `slack_notified_at` damgası yazar, yani ölçüm aracı değil.
   Bunun yerine alt katmandaki `bcc_slack_pending_records()` okundu.
2. **08:55 ve 08:56'da `records.slack_notified_at` yazıldı ve `audit_log`'a iki
   `slack.notify_sent` satırı düştü — bunu BEN yapmadım.** Kullanıcı gridde
   çalışırken uygulamanın kendi boşaltma akışı yazdı; teşhisin kanıtı zaten
   budur (§1).
3. **§3'ün (Tab) testi de veritabanına dokunmuyor** — `_verify_grid_tab_nav.php`
   saf kaynak taraması, tarayıcı testi ise statik bir HTML fikstürü üzerinde
   yapıldı, oturum bile açılmadı.

### Canlıda bekleyen DDL (bugünden değil, 08 Eylül'den)

Bugün commit'lenen `26f0d44` **canlıda henüz uygulanmamış bir şema değişikliği
gerektiriyor** — kod deploy edilmeden önce çalıştırılmalı:

```sql
ALTER TABLE records ADD COLUMN slack_notified_at DATETIME NULL DEFAULT NULL;
UPDATE records SET slack_notified_at = NOW(), updated_at = updated_at
WHERE slack_notified_at IS NULL;
```

Adımlar ve atlanırsa ne olacağı: `docs/CANLIYA-ALMA.md` §3.2.1. Yerelde bu iki
adım 08 Eylül'de zaten uygulandı.

## Açık maddeler

### A. Regresyon paketi bugün ÇALIŞTIRILMADI — bilerek

`_verify_slack_batch.php`, `_verify_error_pages.php` ve
`_verify_trash_purge_attachments.php` üçü de `_test_slack_guard.php`
kullanıyor; betik koşarken **aktif webhookların hepsini pasife alıyor**.
Kullanıcı tam o sırada gridde çalışıyordu ve `bcc_slack_flush_table()`
gönderecek adres bulamasa bile kaydı **damgalıyor** (09-09 §4'te ölçüldü).
Yani betik o anda koşsaydı kullanıcının bekleyen bildirimleri **sessizce
yutulurdu**. Paket, kullanıcı gridden çıktıktan sonra çalıştırılmalı.

### B. Kanban görünümünde ayrılma pingi yok

`grid-slack-flush.js` yalnızca `public/grid.php:1759`'da yükleniyor.
`public/kanban.php` sayfa yüklemesinde boşaltma yapıyor (`:82`) ama
**`pagehide` pingi atmıyor** — kanban'da yapılan bir değişiklik ancak 180 sn
dolup bir sonraki sayfa yüklemesinde/yazımında gider, "çıkınca hemen gönder"
davranışı orada çalışmaz. Tek satırlık `<script>` etiketiyle kapatılabilir;
kullanıcıya soruldu, karar bekleniyor.

### C. Bildirim yalnızca TEK tabloda açık

`slack_watched_fields` tablosunda sadece **tablo 2992** var. Ekip 1'in
`#genel` webhook'u (id 18) duruyor ama o ekipte izlenen alan tanımlı değil,
dolayısıyla başka bir gridde çalışılırsa hücre bildirimi **hiç çıkmaz**.
Tablo 8'in iki webhook'u hâlâ **sahte test URL'i** (`.../services/TEST/…`).

### Önceki günlerden devreden açık maddeler

`docs/gunluk/2026-09-08.md` A, B, B2 ve `docs/gunluk/2026-09-09.md`
"Dünden devreden" bölümü **hâlâ açık**.
