# 2026-09-15

## Yapılanlar

### 1. Eylül değişiklik raporu (kod değişikliği YOK)
- **İstek:** 07, 08-09 ve 14 Eylül'de hangi dosyaların hangi iş için
  değiştiğinin özeti.
- **Yapılan:** `docs/gunluk/`, `docs/DEGISEN-DOSYALAR-VE-TABLOLAR.md` ve git
  geçmişinden derlenip sohbette verildi. 07 Eylül işleri (Excel 40MB, canlıya
  alma rehberi düzeltmeleri, demo giriş notu, 64/64 turu, `main` ortak ata
  tuzağı) günlük sistemi öncesi olduğu için `PROJE-DURUM.md` ve commit
  mesajlarından okundu.

### 2. "Yeni Alan" penceresinde "Tip değiştir" bağlantısı amatör görünüyordu
- **Rapor:** Seçilen tipin yanındaki "· Tip değiştir" altı çizili mavi metin
  bağlantısı olarak duruyordu (ekran görüntüsü: grid, boş tablo → Yeni Alan).
- **Kök neden:** `.link-btn` (`public/assets/style.css:123`) düz
  `text-decoration: underline` metin bağlantısı; ayırıcı olarak elle yazılmış
  `·` kullanılıyordu. Ayrıca `table_fields.php` kabuğu `style.css`'i hiç
  yüklemediği için orada `.link-btn` tanımsızdı — buton tarayıcının varsayılan
  görünümüne düşüyordu.
- **Yapılan:** Satır flex düzenine alındı, `·` kaldırıldı; buton ince çerçeveli
  küçük bir ikincil düğmeye çevrildi (üzerine gelince vurgu rengi zemin +
  çerçeve, klavye odağında `outline`). Kurallar `theme.css`'e kondu çünkü o
  dosya hem grid'de hem ana kabukta (`table_fields.php`) yüklü. `.link-btn`
  sınıfı butonda BİLEREK bırakıldı: `style.css:107`'deki
  `form.stacked button:not(.link-btn)` kuralı onu çıkarırsa butonu büyük mavi
  "gönder" düğmesine boyardı. Yeni seçici `.link-btn.field-type-change`
  (özgüllük 0,2,0) `style.css`'teki `.link-btn`'i sıra fark etmeksizin eziyor.
  JS kancaları (`#new-field-type-change`, `#new-field-type-chosen-label`)
  değişmedi.
- **Değişen dosyalar:**
  - `src/partials/field_type_wizard_fields.php:29-31` — `<p>`'ye
    `field-type-chosen`, butona `field-type-change` sınıfı; etiket `<span>`'e
    alındı, `·` silindi
  - `public/assets/theme.css:151-192` — yeni `.field-type-chosen`,
    `.link-btn.field-type-change` (+ `:hover`, `:focus-visible`)
- **Etkilenen ekranlar:** partial üç yerde basılıyor — `public/grid.php:1359`
  (sütun ekleme), `public/grid.php:1608` (boş tablo "Yeni Alan"),
  `public/table_fields.php:355` (Alanları yönet).
- **Test:** `php -l` temiz. gstack `/browse` ile statik fikstür (partial PHP
  ile basıldı, `theme.css` + `style.css` + `home.css` yüklü, oturum/veritabanı
  yok): açık tema, üzerine gelme ve koyu tema ekran görüntüleri alındı;
  hesaplanan stil `text-decoration: none`, `1px solid` çerçeve, `6px` köşe.
  Gerçek sayfada kullanıcının Ctrl+Shift+R ile bakması gerekiyor.

### 3. "Yeni Alan" penceresinde sağ taraf boş kalıyordu
- **Rapor:** §2'den sonra ekran görüntüsü: pencere 680px, "Alan adı" kutusu
  yarısında bitiyor, sağ taraf boş.
- **Kök neden:** `form.stacked input/select/textarea` genel kuralı
  (`public/assets/style.css:44-50`) `max-width: 360px` veriyor. Pencere ise tip
  seçme adımındaki ızgara için `max-width: 680px` (`style.css:2698`). İkinci
  adımda alanlar 360px'te kalıyordu.
- **Yapılan:** Yalnızca boş tablo "Yeni Alan" penceresinde
  (`#gs-empty-fields-modal`) metin kutusu ve seçenekler alanı tam genişliğe
  yayıldı; para birimi satırı (sembol + ondalık basamak) iki eşit sütuna alındı.
  Yüzde / yıldız sayı kutuları bilerek dar bırakıldı (tek rakam). Genel
  `form.stacked` kuralına ve grid başlığındaki "+" açılır paneline
  (`grid.php:1359`) dokunulmadı — o panel dar, 360px orada doğru.
- **Değişen dosyalar:**
  - `public/assets/style.css:2712-2723` — üç yeni kural, `#gs-empty-fields-modal`
    kapsamlı
- **Test:** `/browse` statik fikstür: 1000px'te yalnızca alan adı; seçenekler +
  para birimi satırları açıkken; 400px telefon genişliğinde. Üçünde de kutular
  pencereyi dolduruyor, taşma yok.

### 4. Uzun metin: alt alta yazılan satırlar tekrar açınca yan yana kayıyordu
- **Rapor:** Uzun metin hücresine alt alta kalın / italik / link satırları
  yazılıyor. Grid'de yan yana görünmesi normal (grid tek satır gösterir), ama
  not tekrar açılınca link satırı üstteki metnin yanına kayıyor.
- **Ölçülen:** tablo 7141, kayıt 112250'nin saklanan değeri (salt-okunur
  `SELECT`): `<b>Uzun metin<br></b><i>Uzun metin</i><a …>Uzun metin</a><br><br><br>`
  — italik ile link arasındaki satır sonu veritabanında YOK, yani kayıp
  kaydederken oluşuyor, gösterimde değil.
- **Kök neden:** Chromium Enter'da yeni satırı `<div>` olarak açıyor —
  gerçek tarayıcıda (`/browse`) üretildi:
  `<b>Uzun metin</b><div><i>Uzun metin</i></div><div><a>Uzun metin</a></div>`.
  Sunucudaki temizleyici (`bcc_sanitize_rich_text_node`) her `div`/`p`'yi
  "içerik + `<br>`" yapıyordu; satır sonunu bloğun ÖNÜNE değil ARKASINA
  koyduğu için ikinci satır birinciye yapışıyordu:
  `<b>Uzun metin</b><i>Uzun metin</i><br><a>…</a><br>`.
- **Yapılan:** Blok etiketleri artık `bcc_sanitize_rich_text_children` içinde
  işleniyor: önceki içerik `<br>` ile bitmiyorsa bloğun önüne `<br>`, blok
  içindeki son `<br>` (tarayıcının boş satır dolgusu) atılıyor, bloktan sonra
  `<br>`; en sondaki tek `<br>` de atılıyor. "`<br>` ile bitiyor mu" kontrolü
  kapanan inline etiketlerin (`</b>`, `</i>`, `</a>`…) içine bakıyor — yoksa
  `<b>X<br></b><div>Y</div>` fazladan boş satır üretirdi. Blok listesi
  `div`/`p`'ye ek olarak yapıştırılan içerik için `li`, `ul`, `ol`, `h1-h6`,
  `blockquote`, `pre`. İzin verilen etiketler ve link/script güvenliği
  değişmedi; saklanan biçim yine "inline + `<br>`", okuyan hiçbir yer
  (grid, detay, Slack, dışa aktarma) değişmedi.
- **Değişen dosyalar:**
  - `src/schema.php:1745` — `bcc_sanitize_rich_text_pass` sondaki dolgu `<br>`'i atıyor
  - `src/schema.php:1748-1778` — `bcc_sanitize_rich_text_children` blok işleme;
    yeni `bcc_rich_text_ends_with_break()`, `bcc_rich_text_strip_trailing_break()`
  - `src/schema.php` `bcc_sanitize_rich_text_node` — eski `div`/`p` dalı silindi
  - `scripts/_verify_rich_text_line_breaks.php` — **YENİ**, 23 kontrol
- **Test:**
  - Yeni betik 23/23; düzeltme `git stash` ile geri alınınca 10 geçti / 13 kaldı
    (betik hatayı gerçekten yakalıyor).
  - `/browse`: Chromium'da yazılan HTML'ler temizleyiciden geçirilip tekrar
    editöre yüklendi, görünen satır sayısı ölçüldü (kullanıcı senaryosu 3 → 3).
    Kaydedilmiş notu açıp sona satır eklemek (4 satır) ve araya satır eklemek
    (5 satır) de doğru.
  - `_verify_cell_text_limits.php` 43/43, `_verify_group_a.php` 81/81.
  - Kirlilik: `users=6 teams=4 bases=5 records=170 notify_sent=2445` önce = sonra.
- **Bilinçli sınır:** metnin EN SONUNDAKİ boş satırlar kaydedilmiyor
  (`A<div>B</div><div><br></div>` → `A<br>B`) — görünmez, eskiden de `trim`
  ediliyordu.

### 5. Uzun metindeki linklerin alt çizgisi kaldırıldı
- **İstek:** Uzun metin içindeki URL'nin altındaki çizgi kalksın.
- **Yapılan:** Link yine mavi (`--bcc-link`) ve tıklanabilir; alt çizgi ne
  normalde ne üzerine gelince var — üzerine gelince yalnızca renk koyulaşıyor
  (`--bcc-link-hover`). Hover kuralına `text-decoration: none` açıkça yazıldı:
  yazılmasa `style.css:9`'daki genel `a:hover { underline }` geri getirirdi.
  Kural üç yeri birlikte kapsıyor: grid hücresi, düzenleme penceresi, arayüz
  (interface) detay alanı.
- **Değişen dosyalar:**
  - `public/assets/style.css:1189-1201` — `text-decoration: underline` /
    `text-underline-offset` / hover `text-decoration-thickness` → `none`
  - `scripts/_verify_richtext_link.php:201-207` — D bölümü eski "altı çizili"
    kararını sabitliyordu (49/51 düştü); yeni karara göre güncellendi
- **Test:** `_verify_richtext_link.php` 51/51 (kirlilik önce = sonra).
  `/browse` fikstüründe hesaplanan `text-decoration-line`: normal `none`,
  hover `none`.

### 6. Çoklu seçim alanında birden fazla seçenek seçilemiyordu
- **Rapor:** "Çoklu seçim"de çoklu seçim yapılamıyor.
- **Ölçülen:** gerçek `grid.js` + `grid-cell-select.js` yüklü statik fikstür
  (`bcc_post` sahte — sunucuya istek YOK), `/browse` ile gerçek tıklamalar:
  "Kırmızı" → `["Kırmızı"]`, sonra "Mavi" → `["Mavi"]`. İkinci tıklama ilkini
  bırakıyor.
- **Kök neden:** Editör tarayıcının yerel `<select multiple>` liste kutusu
  (`grid.js` `buildInput`). Bu kutuda düz tıklama seçimi DEĞİŞTİRİR; çoklu
  seçim yalnızca Ctrl+tıklama ile olur — kullanıcı bunu bilmiyor. Kayıt tarafı
  (`normalize_cell_value` `multiple_select`) sağlam, dizi kabul ediyor.
- **Yapılan:** Liste kutusuna `mousedown` dinleyicisi: sol tıkla bir
  `<option>`'a basılınca tarayıcının varsayılanı engelleniyor, seçenek
  tersine çevriliyor (seç / bırak), odak kutuda tutuluyor (yoksa `blur` hemen
  kaydedip kapatırdı), kaydırma konumu korunuyor, `change` yayılıyor. Kayıt
  eskisi gibi kutudan çıkınca (`blur`) ya da Enter ile. `buildInput` satır
  detay panelinde de kullanıldığı için orası da düzeldi.
- **Değişen dosyalar:**
  - `public/assets/grid.js:448-459` — `multiple_select` dalına `mousedown` dinleyicisi
  - `scripts/_verify_grid_multiselect_toggle.php` — **YENİ**, 12 kontrol
- **Test:**
  - Fikstür: üç seçeneğe tıklama → `["Kırmızı","Mavi","Yeşil"]`, birine tekrar
    tıklama → `["Mavi","Yeşil"]`, kutu açık ve odakta; başka hücreye tıklayınca
    tek kayıt isteği gitti: `value=["Mavi","Yeşil"]`. Detay paneli yolu
    (`BCC_GRID.buildInput` doğrudan): iki tıklama sonrası `A,B,C` seçili,
    `blur` 0. Konsol hatası yok.
  - Yeni betik 12/12; düzeltme `git stash` ile geri alınınca 5/12.
- **Bilinçli sınır:** klavyede ok tuşları tarayıcının yerel davranışında
  kaldı (düz ok tek seçime indirir; Shift/Ctrl+ok ve Boşluk çoklu çalışır).
  Fareyle kullanım düzeltildi, klavyeye dokunulmadı.

### 7. Grid: çoklu seçim ve dosya eki alt alta, satır uzuyor; altı sütun ortalandı
- **İstek:** (a) Çoklu seçimde grid tek seçim gösteriyor → alt alta görünsün.
  (b) Dosya eki birden fazlaysa yan yana → o da alt alta. Birden fazla değer
  alan hücreler satırı otomatik uzatsın. (c) Sayı, onay kutusu, tarih, tekli
  seçim, çoklu seçim, saat sütunları ortalansın.
- **Ölçülen:** `bcc_render_grid_data_row()` sahte alan/kayıt/ek verisiyle PHP
  CLI'dan basıldı (veritabanına yazma YOK), gerçek `style.css` + `grid.js` ile
  `/browse`'ta açıldı. Önce: 3 seçimli hücre "Kırmızı Mavi …" diye kesiliyor,
  3 ek tek satırda yatay kaydırmalı.
- **Kök neden:** `.cell-view` genel kuralı `white-space: nowrap; overflow:
  hidden; text-overflow: ellipsis` (`style.css` `.cell-view`); orta/uzun satır
  modlarında `-webkit-line-clamp`. `.attachment-cell-view` ise `display: flex`
  + `overflow-x: auto` (yatay). Satır yüksekliği `td { height: 32px }` —
  tablo hücresinde bu alt sınırdır, içerik taşmazsa satır büyümez.
- **Yapılan:** Seçiciler `td.grid-cell[data-field-type="…"]` — PHP'nin zaten
  bastığı özniteliğe bağlı, PHP/JS değişmedi; kaydedildikten sonra JS'in
  yeniden çizdiği çipler aynı `.cell-view`'a düştüğü için onlar da uyuyor.
  - Çoklu seçim + dosya eki `.cell-view`: dikey flex, `height: auto`,
    kesme/kırpma/line-clamp kapalı → satır içerik kadar uzuyor. Çipler
    ortalı, sütundan geniş çip `…` ile kesiliyor; ek adları sola dayalı, 110px
    sınırı kaldırıldı (sütuna sığdığı kadar, sonra `…`).
  - Ortalama: `number`, `checkbox`, `date`, `single_select`,
    `multiple_select`, `time` hücreleri `text-align: center`.
  - **Yan etki düzeltmesi:** satırlar artık farklı yükseklikte olabildiği
    için eski `vertical-align: middle` metinleri üstte, onay kutusu / sıra
    numarasını ortada bırakıp dağınık görünüyordu. Veri satırlarının
    hücreleri (`tr[data-record-id] > td`) `vertical-align: top`; onay kutusu
    `margin-top: 8px` ile metin satırına, sıra numarası kutusu
    `min-height: 24px` ile hizalandı. Grup başlığı satırları etkilenmedi.
    Ölçüm (kısa satır modu): metin / onay / tekli / çoklu / sıra dikey
    merkezleri 64 / 64 / 61 / 62 / 63 px.
- **Kapsam dışı (istenmedi):** para birimi, yüzde, puan, otomatik sayı
  sütunları ortalanmadı; başlık hücreleri (`th`) sola dayalı kaldı; kanban ve
  arayüz (interface) kartları etkilenmiyor (`table.grid` kapsamlı).
- **Değişen dosyalar:**
  - `public/assets/style.css:2101-2159` — yeni kurallar (üste hizalama, onay
    kutusu, ortalama, çoklu seçim/ek dikey liste)
  - `scripts/_verify_grid_cell_layout.php` — **YENİ**, 21 kontrol
- **Test:**
  - Fikstür ekran görüntüleri: kısa satır (sütun genişliği ayarlı ve
    ayarsız) ve orta satır modunda 1 / 3 seçim, 1 / 3 ek, boş satır; satır
    üzerine gelince sıra kutucuğu + genişlet düğmesi; çoklu seçim düzenleme
    modu açılıyor.
  - Yeni betik 21/21; `style.css` `git stash` ile geri alınınca 2/21.
  - `_verify_richtext_link.php` 51/51, `_verify_grid_multiselect_toggle.php` 12/12.

### 8. Tüm hücreler dikeyde ortalandı (§7'nin "üste hizala" kararı geri alındı)
- **İstek:** Ekran görüntüsü: çoklu seçim satırı uzayınca "Tek satır metin",
  "Saat", tarihler üstte; "Oluşturan" ortada. Yatay hiza AYNEN kalsın, tüm
  hücreler satır içinde dikeyde ortalansın.
- **Kök neden:** `td` zaten `vertical-align: middle` idi, ama `.cell-view`
  `height: 100%` ile hücreyi boydan kaplıyor ve metin kutunun tepesinde
  duruyordu; kendi içinde `align-items: center` olan görünümler (kullanıcı,
  puan, link) ortada kalıyordu — karışıklık bundan. §7'de bunu
  `vertical-align: top` ile "hepsi üstte" yönünde eşitlemiştim; kullanıcı
  tersini istedi.
- **Yapılan:** §7'nin üç kuralı (`vertical-align: top`, onay kutusu
  `margin-top: 8px`, sıra kutusu `min-height: 24px`) kaldırıldı. Yerine veri
  satırlarında `vertical-align: middle` + `td > .cell-view { height: auto }`
  (içerik kendi yüksekliğinde, hücrenin ortasına oturuyor). `.cell-view`
  artık hücreyi kaplamadığı için üzerine gelme zemini yalnızca yazının
  şeridini boyardı → zemin `td.editable:not(.editing):hover`'a da verildi,
  hücrenin tamamı boyanıyor. Yatay hizalar (§7 ortalamaları dahil)
  değişmedi.
- **Değişen dosyalar:**
  - `public/assets/style.css:2101-2110` — dikey ortalama, `.cell-view`
    yüksekliği, hover zemini
  - `scripts/_verify_grid_cell_layout.php` — C bölümü yeni karara göre (21 → 23)
- **Test:** Fikstüre uzun metin, puan, URL ve kullanıcı sütunları eklendi (14
  sütun). Her hücrenin dikey merkezi − satır merkezi ölçüldü: kısa satır
  (32px), içerikle uzamış satır (76px) ve orta satır modunda (67px) tüm
  sütunlarda **−1…0 px**. Üzerine gelince `td` ve `.cell-view` zemini aynı
  (`rgb(242,242,244)`). Betik 23/23.

### 9. Sütun genişliği: çift tıkla sığdır; boyutlandırma çizgisi ve "satır ekle" düzeltmeleri
- **İstek (3 ekran görüntüsü):**
  (a) Sütun kenarındaki boyutlandırma tutamacına çift tıklayınca sütun, en
  geniş içeriğe göre ayarlansın.
  (b) Boyutlandırma çizgisi (ve imleci) "satır ekle" satırının üzerinde de
  çıkıyor — onun arkasında kalsın.
  (c) Sağa kaydırınca "+ [10] satır ekle" de kayıyor, girdi kutusu sıra
  sütununun altına giriyor — sabit kalsın.
- **Kök nedenler:**
  (a) Özellik yoktu.
  (b) `grid-column-resize.js` `layout()` şerit yüksekliğini
  `table.offsetHeight`'ten alıyordu — tablonun son satırı olan
  `tr.grid-add-row` da dahil.
  (c) Kutu, kaydırılan tablonun `colspan`'lı hücresinde normal akışta. İlk
  denemede `position: sticky` tek başına ÇALIŞMADI (ölçüldü: kaydırma 500 →
  kutu −380px): sütun genişliği ayarlı tabloda `td { overflow: hidden }`
  (`style.css`) hücreyi kaydırma kabı yapıyor, yapışkan kutu hücrenin içine
  hapsoluyor.
- **Yapılan:**
  - (a) `autoFitColumn(key)`: sütun geçici olarak `MIN_WIDTH`'e çekiliyor
    (geniş sütun da daralabilsin), tabloya `is-col-measuring`, başlık + tüm
    veri satırlarındaki hücrelere `is-col-measure` sınıfı; ölçüm CSS'i
    kesme / kırpma / `line-clamp` / `max-width`'i kapatıp `.cell-view`'u
    `max-content` yapıyor. Her hücrede çocukların sağ kenarı + hücrenin sağ
    dolgu/kenarlığı alınıyor, en büyüğü + 2px, `MIN/MAX_WIDTH` sınırında
    uygulanıyor, kaydediliyor (`persist` — sürükleme bitişiyle aynı yol).
    Orta/uzun satır modunda hücreler `-webkit-box` ile çizildiği için
    ölçümde blok görünüme çevriliyor (kısa modda DEĞİL — orada esnek kutu
    `gap`'i gerçek genişliğin parçası; ilk denemede blok ölçüm link ve
    kullanıcı sütununu 4 ve 24px dar bıraktı, düzeltildi). Çoklu seçim ve ek
    hücreleri dikey listede kaldığı için bu dönüşümün dışında.
  - (b) Şerit yüksekliği `addRow ? addRow.offsetTop : table.offsetHeight`.
  - (c) `.grid-add-row-bulk` `position: sticky; left: var(--grid-rownum-w)`;
    değişken `layout()` içinde sıra sütununun gerçek genişliğinden yazılıyor.
    Hücreye `overflow: visible`. "Shift-Enter" ipucu balonu yapışkan kutunun
    İÇİNE taşındı (`grid.php`) — yoksa kaydırınca hücrenin sol başına bağlı
    kalıp ekran dışında açılırdı.
- **Değişen dosyalar:**
  - `public/assets/grid-column-resize.js:210-214` — şerit yüksekliği, `--grid-rownum-w`
  - `public/assets/grid-column-resize.js:269-333` — YENİ `autoFitColumn()`
  - `public/assets/grid-column-resize.js:336-339` — şeride `dblclick`
  - `public/assets/style.css:960-976` — ölçüm modu CSS'i
  - `public/assets/grid-shell.css:375-376` — add-row hücresi `overflow: visible`
  - `public/assets/grid-shell.css:1552-1553` — `.grid-add-row-bulk` yapışkan
  - `public/grid.php:1390-1391` — ipucu `<span>`'i `.grid-add-row-bulk` içine alındı
  - `scripts/_verify_column_resize.php:471-474` — G maddesi eski "şerit =
    tablo yüksekliği" dizesini arıyordu (103/104 düştü), yeni davranışa göre
  - `scripts/_verify_grid_column_autofit.php` — **YENİ**, 17 kontrol
- **Test:** Fikstür: gerçek `bcc_render_grid_data_row` + `grid-column-drag.js`
  + `grid-column-resize.js`, 13 sütun, `.grid-wrap` 900px.
  - (b) şerit `top 0 / height 172px`, add-row `offsetTop 172` (tablo 204px).
  - (c) kaydırma 0 → kutu sol 60px, kaydırma 500 → **60px** (sıra sütununun
    sağ kenarı 60px). Orta modda da aynı (42 / 42).
  - (a) 13 sütunun hepsine çift tıklama, kısa / orta / uzun satır modunda:
    sonrasında başlık etiketi, `.cell-view`, çip, ek adı, link metni,
    kullanıcı adında **kesilen yok**; puan yıldızları tek satırda (orta modda
    ÖNCESİNDE 2 satırdı). 600px'e açılmış tekli seçim sütunu çift tıklamayla
    80px'e indi. İkinci çift tıklama aynı sonucu verdi. Ölçüm sınıfları geride
    kalmadı.
  - Betikler: yeni 17/17 (üç dosya `git stash` ile geri alınınca 4/17),
    `_verify_column_resize` 104/104, `_verify_grid_export` 69/69,
    `_verify_grid_cell_layout` 23/23, `_verify_grid_multiselect_toggle` 12/12,
    `_verify_richtext_link` 51/51. Kirlilik önce = sonra.
- **Bilinmesi gereken:** çift tıklama yalnızca o an sayfada yüklü satırlara
  bakar (gizli / filtrelenmiş satırlar ölçülmez). Çift tıklamanın iki
  `mousedown`'ı eskisi gibi iki kez genişlik kaydı da tetikler (mevcut
  sürükleme davranışı, zararsız).

### 10. "satır ekle" yazısı kutusunda aşağı kayıktı
- **Rapor:** Ekran görüntüsü: "[10] satır ekle" — "satır ekle" biraz aşağıda.
- **Ölçülen (piksel):** fikstürün 3x ekran görüntüsü base64 olarak alınıp
  tuvalde mürekkep satırları sayıldı. Önce: "satır ekle" mürekkebi 35–44px,
  kutu merkezinden **1,56px** aşağıda; yanındaki "10" 34,3–43px — yani yazı
  komşusundan tam 1px aşağıda.
- **Kök neden:** `.grid-add-row-bulk-btn` `height: 1.5rem` (24px, 1px
  kenarlıklı → 22px iç alan) ama `line-height: 1.5rem` (24px): satır kutusu
  iç alandan 2px büyük, yazı 1px aşağı itiliyordu.
- **Yapılan:** Düğme `display: inline-flex; align-items: center;
  box-sizing: border-box; line-height: 1` — yazı iç alanın ortasına
  oturuyor. Yükseklik, dolgu, renkler aynı.
- **Değişen dosyalar:**
  - `public/assets/grid-shell.css` `.grid-add-row-bulk-btn` — 4 bildirim
  - `scripts/_verify_grid_column_autofit.php` — +1 kontrol (17 → 18)
- **Test:** Sonra: "satır ekle" 34–43px, kutu merkezinden 0,56px aşağıda
  (Segoe UI küçük harflerin doğal payı; "10" rakamları da 0,7px) — iki
  yazının alt çizgisi aynı piksel satırında. Betik 18/18.

### 11. "satır ekle" üzerindeki Shift-Enter ipucu balonu kaldırıldı
- **İstek:** Satır ekleme satırının üzerine gelince çıkan "Shift-Enter'a
  basarak herhangi bir yere yeni kayıt da ekleyebilirsiniz" yazısı silinsin.
- **Yapılan:** Balonun `<span>`'i ve yalnızca bu balon için satıra konmuş
  `data-tooltip-host` özniteliği silindi; artık hiçbir öğeye uygulanmayan iki
  `.grid-add-row-hint .gs-kbd-tooltip` kuralı da silindi. Genel
  `.gs-kbd-tooltip` / `[data-tooltip-host]` kuralları DURUYOR — başka yerler
  kullanıyor (ör. `grid-freeze-columns.js` dondurma tutamacı). **Shift+Enter
  kısayolunun kendisi değişmedi** (`grid.js:1214`), yalnızca ipucu gitti.
- **Değişen dosyalar:**
  - `public/grid.php:1387-1390` — `data-tooltip-host` ve ipucu `<span>`'i silindi
  - `public/assets/grid-shell.css:376-379` — iki ölü kural silindi
  - `scripts/_verify_grid_column_autofit.php` — §9'daki "ipucu yapışkan
    kutunun içinde" kontrolü "ipucu kaldırıldı" kontrolüne çevrildi
- **Test:** `php -l` temiz; `_verify_grid_column_autofit` 18/18,
  `_verify_column_resize` 104/104, `_verify_grid_export` 69/69.

### 12. Kayıt detay penceresi: soldaki alan adları değerlerle hizalı değildi
- **Rapor (3 ekran görüntüsü):** Satırı genişletince açılan pencerede
  soldaki alan adları sağdaki değerlerle aynı hizada değil; "Onay kutusu",
  "Oluşturulma zamanı", "Değerlendirme" belirgin şekilde aşağıda.
- **Ölçülen:** `bcc_render_grid_data_row` + `grid.php`'nin detay penceresi
  işaretlemesi (dosyadan kesilip `eval` ile basıldı) + gerçek
  `grid-row-detail.js`; kullanıcının 21 alan tipiyle aynı kayıt. Etiket
  yazısının satır kutusu merkezi − değerin ilk satırının merkezi:
  - Düzenleme modu: kutulu alanlar (sayı, tarih, seçim, saat, URL, e-posta,
    telefon…) **−3…−4px** (etiket yukarıda); onay kutusu **+8,9**; dosya eki
    **+8,3**; salt okunur (oluşturulma/son değişiklik zamanı, oluşturan,
    otomatik numara) **+7,9…+8,1**; değerlendirme **+10,4**.
  - Salt okunur mod (`BCC_CAN_EDIT=false` — commenter/viewer): tüm alanlar
    **+5,9…+10,4**.
- **Kök neden:** `.grid-detail-field-inline .grid-detail-field-label`
  herkese sabit `padding-top: 0.65rem` veriyordu — kutulu girdinin iç dolgusuna
  göre seçilmiş tek sayı. Değer türleri çok farklı yükseklikte başlıyor (kutu,
  düz metin, onay kutusu, yıldız, dosya çipi, liste kutusu); tek sabit hepsine
  uyamaz.
- **Yapılan:** `grid-row-detail.js`:
  - `firstLineCenter(root)`: öğenin görünen ilk "satırını" bulur — gizli alt
    ağaçları atlar; metinse ilk satır kutusunun ortası; `textarea` / çoklu
    `select` ise üst kenar + kenarlık + dolgu + yarım satır (çoklu seçimde
    satır yüksekliği gerçek seçenek yüksekliğinden); tek satırlık
    `input`/`select`, onay kutusu, resim ise kutunun ortası.
  - `alignDetailLabels()`: her satırda etiketin ve değerin ilk satır
    merkezini ölçüp farkı (0,5px'e yuvarlı, negatifse 0) satırın
    `--grid-detail-label-offset` değişkenine yazıyor. Önce hepsi sıfırlanıyor,
    sonra toplu okunuyor, sonra toplu yazılıyor (tek yeniden yerleşim).
  - Kayıt açılınca çağrılıyor; içerik değişince (`MutationObserver` — ör. ek
    eklendi/silindi, ilk satır çipten bırakma alanına geçiyor) ve pencere
    boyutu değişince `requestAnimationFrame` ile yeniden.
  - Değişken ETİKETE değil SATIRA yazılıyor: "Kaydı gönder" önizlemesi
    etiket öğesini `cloneNode` ile kopyalıyor, kopya etkilenmesin.
  - `style.css`: `padding-top: var(--grid-detail-label-offset, 0.65rem)` —
    JS çalışmazsa eski görünüm.
- **Değişen dosyalar:**
  - `public/assets/grid-row-detail.js:349-434` — `firstLineCenter`,
    `alignDetailLabels`, zamanlayıcı, gözlemci, `resize`
  - `public/assets/grid-row-detail.js:664` — `openDetail` sonunda çağrı
  - `public/assets/style.css:452` — etiket üst dolgusu değişkenden
  - `scripts/_verify_detail_label_alignment.php` — **YENİ**, 14 kontrol
- **Test:**
  - Aynı ölçüm, sonra: düzenleme modunda 20 alanın hepsi **−0,1…+0,2px**
    (çoklu seçim önce −1,9 çıktı → seçenek yüksekliği düzeltmesiyle 0);
    salt okunur modda **−0,6…+0,2px** ("Kullanıcı" boş, ölçülecek bir şey
    yok).
  - Bağımsız piksel kontrolü (formülü kullanmadan, 2x ekran görüntüsünde
    mürekkep satırları): tüm alanlarda **±2,6px** içinde. Kalan fark harf
    şekillerinden ("URL" büyük harf / "klkqweqw" `q` kuyruklu gibi); satır
    kutusu hizası 0.
  - Dosya ekinin çipi DOM'dan silinince gözlemci çalıştı: ofset 2px →
    20,5px, fark +0,1px.
  - Ekran görüntüsü: 21 alan göz kontrolü. Konsol hatası yok. `node --check`
    temiz.
  - Yeni betik 14/14; `grid-row-detail.js` `git stash` ile geri alınınca 1/14.
    `_verify_grid_multiselect_toggle` 12/12.
- **Denenmedi:** yazdırma görünümü (orada değerler `.grid-detail-print-value`
  ile basılıyor, etiket kuralına dokunulmadı — ölçülen ofset ekrandaki
  değer içindir, yazdırmada da aynı değişken geçerli kalır).

### 13. Commit öncesi tam regresyon turu
- **Tam paket (78 `_verify_*`):** 74 geçti, 4 sıfır dışı çıktı:
  `_verify_api_error_format`, `_verify_demo_roles`, `_verify_json_in_script`,
  `_verify_login_throttle` (17/3). Dördünün sebebi ölçüldü: veritabanında
  `@bcc.local` demo hesabı **0** — `PROJE-DURUM.md` §7'deki bilinen tuzak.
  Diğer sonuçlarda gizli "KALDI"/eksik oran yok. Kirlilik:
  `users=6 teams=4 bases=5 records=170 notify_sent=2445` önce = sonra.
- **Demo hesaplarına bağlı 8 betik, belgelenmiş akışla** (`seed_demo_users.php`
  → betikler → `--remove`): `_verify_demo_roles`, `_verify_login_throttle`
  20/20, `_verify_rbac`, `_verify_global_search`, `_verify_api_error_format`
  14/14, `_verify_json_in_script` 14/14, `_verify_notification_roles` 22/22
  geçti; `_verify_slack_integration` **1 kontrolde düştü** (aşağıda açık madde).
  Sayaçlar (9 tablo) önce = sonra: `users=6 teams=4 team_members=2 bases=7
  tables_meta=6 fields=47 records=170 cell_values=694 audit_log=15245
  notify_sent=2445 bcc_local=0`.
- **Commit'ler** (`chore/audit-refactor`, dosyalar tek tek eklendi, her
  birinde `git diff --cached` + sır taraması): `ed3c0a9` zengin metin (§4),
  `3a0456d` çoklu seçim (§6), `4161e64` grid/arayüz (§2-§3, §5, §7-§12 —
  `style.css` ortak olduğu için tek commit), ardından belgeler.

## Açık maddeler
- **`_verify_slack_integration.php:284` bayat kontrol (bugünden DEĞİL):**
  "SATIR ekleme Slack bildirimini TETIKLEDI" yeni satırın ANINDA bildirim
  audit'i yazmasını bekliyor; 2026-09-08 `26f0d44` ile anlık yeni kayıt
  bildirimi kaldırıldı (toplu özet, içerik girilince duyuru). Kontrol yalnızca
  demo hesapları varken çalışan bölümde olduğu için 09-14'teki A+B koşularında
  görünmedi. Bugün `record_add.php` / `src/slack.php` değişmedi (diff'te yok).
  Kontrol yeni tasarıma göre güncellenmeli.
- **Kayıt 112250 (tablo 7141) kendiliğinden düzelmez:** satır sonu kayıt
  anında kaybolmuştu, veride yok. Not bir kez açılıp link satırının önüne
  Enter basılıp kaydedilmeli. Bu düzeltmeden önce birden fazla satırla
  kaydedilmiş başka uzun metinler de aynı şekilde birleşmiş olabilir.
- `table_fields.php` (Alanları yönet) sayfasında görünüm gerçek oturumla
  ayrıca bakılmadı; fikstür grid kabuğunu taklit ediyor.

---

## Commit sonrası

### 14. "Kaydı gönder" mailinde değerlendirme puanı görünmüyordu
- **Rapor (ekran görüntüsü, Outlook):** Gönderilen mailde "Değerlendirme"
  sütununda `★★★★★★★★★★` — kaç puan verildiği anlaşılmıyor. İstek: "10
  üzerinden 4" gibi yazı.
- **Kök neden:** Mail gövdesi sunucuda değil, detay penceresinin topladığı
  `preview_fields`'ten kuruluyor (`record_send.php` →
  `bcc_build_send_email_html`). Değerler `grid-row-detail.js`
  `fieldPrintText()` ile alınıyor; yıldız alanında özel dal yoktu,
  `.cell-view`/`textContent` dalına düşüyordu. Her yıldız aynı `★` karakteri —
  dolu/boş ayrımı yalnızca CSS rengi (`.rating-star-filled`), mailde renk
  gidince 10 yıldızın hepsi aynı.
- **Yapılan:** `fieldPrintText()`'e `.rating-view` dalı (`.cell-view`
  dalından ÖNCE — salt okunur görünüm `.cell-view rating-view` taşıyor):
  toplam = `.rating-star` sayısı, puan = `.rating-star-filled` sayısı →
  `"10 üzerinden 4"`; puan yoksa diğer boş alanlar gibi `—`. Aynı fonksiyon
  "Kaydı gönder" önizlemesini ve "Kaydı yazdır"ı da besliyor, ikisi de aynı
  metni gösterecek.
- **Değişen dosyalar:**
  - `public/assets/grid-row-detail.js:908-913` — yıldız dalı
  - `scripts/_verify_record_send_rating_text.php` — **YENİ**, 8 kontrol
- **Test:** Detay fikstürü (8/10 puanlı kayıt), "Kaydı gönder" önizlemesi:
  düzenleme modu `10 üzerinden 8`, salt okunur mod `10 üzerinden 8`, dolu
  yıldızlar kaldırılınca `—`. Konsol hatası yok, `node --check` temiz. Yeni
  betik 8/8 (değişiklik geri alınınca 3/8); `_verify_detail_label_alignment`
  14/14. Gerçek mail GÖNDERİLMEDİ (SMTP'ye dokunulmadı); sunucu değeri
  `htmlspecialchars` ile basıyor, metin olduğu gibi gider.
- **Aynı ölçümde görülen, DÜZELTİLMEYEN:** "Yüzde" alanı düzenleme modunda
  mailde ham sayı (`42342300`), salt okunur modda biçimli (`%42.342.300`)
  gidiyor — girdi kutusunun ham değeri okunuyor. İstenmedi, dokunulmadı.
- **Commit:** `6809264` (§14-§16 birlikte).

### 15. "Kaydı gönder" maili varsayılan olarak tablo düzeninde
- **İstek:** §14 sonrası gönderilen mail alt alta (etiket | değer satırları)
  geldi; önceki gibi tablo olarak gitsin.
- **Kök neden (§14 ile İLGİSİZ):** "Kaydı gönder" penceresindeki "Tablo
  düzenini kullan" anahtarı pencere her açılışta kapalıya sıfırlanıyordu
  (`grid-row-detail.js` `sendUseGridLayoutToggle.checked = false`, özellik ilk
  yazıldığından beri — `42668c7`). Önceki tablo biçimli mail anahtar elle
  açılarak gönderilmişti; bu sefer açılmadığı için `use_grid_layout=0` gitti.
- **Yapılan:** Anahtar varsayılan AÇIK — hem sayfada (`checked`) hem pencere
  her açılışta. Kullanıcı kapatırsa alt alta gönderim aynen çalışıyor
  (`bcc_build_send_email_html` iki dalı da duruyor).
- **Değişen dosyalar:**
  - `public/assets/grid-row-detail.js:1062` — `checked = false` → `true`
  - `public/grid.php:1568` — anahtara `checked`
  - `scripts/_verify_record_send_rating_text.php` — C bölümü (+3, 8 → 11)
- **Test:** Detay fikstürü yeniden basıldı: pencere açılınca anahtar açık;
  `fetch` yakalanarak "Gönder" tıklandı (sunucuya/SMTP'ye istek GİTMEDİ):
  `use_grid_layout=1`, 21 alan, "Değerlendirme" = `10 üzerinden 8`. Aynı
  `preview_fields` ile `record_send.php`'deki `bcc_build_send_email_html`
  (fonksiyon kaynağı ayıklanıp çalıştırıldı) → 21 `<th>` + 21 `<td>`, 2 satır
  (tablo düzeni), puan hücresi `10 üzerinden 8`; HTML tarayıcıda açılıp
  ekran görüntüsüyle bakıldı. Betik 11/11. `php -l`, `node --check` temiz.
- **Commit:** `6809264` (§14-§16 birlikte).

### 16. "Görünümü kopyala" (Excel'e yapıştırma) ve "Excel indir"de yıldız sorunu
- **Rapor (ekran görüntüsü, Excel):** Görünüm kopyalanıp Excel'e
  yapıştırılınca "Değerlendirme" hücresinde `★★★★★★★★★★` — kaç puan olduğu
  anlaşılmıyor. §14'teki mail düzeltmesinin aynısı istendi.
- **Kök neden:** İki ayrı yol, ikisi de yıldız karakteri yazıyordu:
  - Kopyalama (`grid-copy.js` `cellDisplay`) hücrenin görünen metnini
    alıyor; her yıldız aynı `★`, dolu/boş yalnızca CSS rengi.
  - "Excel indir" (`api/view_export_xlsx.php`) `cell_display_text()`
    kullanıyor → `★★★★☆☆☆☆☆☆` (ayırt edilebilir ama yazı değil, yazı tipine
    bağlı).
- **Yapılan:**
  - `grid-copy.js`: `rating` dalı — puan hücrenin `data-value`'sundan, toplam
    `data-options` `max_rating`'den (DOM sınıfından DEĞİL: gridde yıldızın
    üzerine gelince `.rating-star-filled` geçici boyanıyor) →
    `"10 üzerinden 8"`; puansız boş. TSV ve HTML pano çıktısı ikisi de.
  - `grid-paste.js` `coerceForField`: `rating` için `"10 üzerinden 4"` → `4`
    (Türkçe küçük harfe çevirerek; "ÜZERİNDEN" de tutuyor) — Excel'den geri
    yapıştırma kırılmasın. Grid'in kendi HTML panosundan yapıştırma zaten ham
    değeri (`data-bcc-raw`) kullanıyor, etkilenmedi.
  - `src/schema.php`: yeni `bcc_rating_out_of_text($cellRow, $options)`;
    `view_export_xlsx.php` rating için bunu kullanıyor. **`cell_display_text`
    DEĞİŞMEDİ** — grid hücresi, kanban, arayüz, Slack mesajı ve gruplama
    başlıkları yıldız göstermeye devam ediyor.
- **Değişen dosyalar:**
  - `public/assets/grid-copy.js:35-46` — rating dalı
  - `public/assets/grid-paste.js:172-177` — "N üzerinden M" ayrıştırma
  - `src/schema.php:1187-1201` — `bcc_rating_out_of_text()`
  - `public/api/view_export_xlsx.php:62-64` — rating dalı
  - `scripts/_verify_group_c1.php:415-416` — EXPORT kontrolü "★ xlsx içinde"
    kararını sabitliyordu (55/56 düştü); `"7 üzerinden 5"` + yıldız YOK'a
    çevrildi
  - `scripts/_verify_record_send_rating_text.php` — D bölümü (+11, 11 → 22);
    bootstrap betiğin başına alındı
- **Test:**
  - Fikstür (21 alan, 3 kayıt: 8/10, puansız, 3/10) + gerçek `grid-copy.js`;
    `execCommand('copy')` sahte `DataTransfer` ile yakalandı. Kopyalama anında
    ilk kaydın 2. yıldızının üzerine gelinmiş durumdaydı. TSV'de yıldız yok,
    `10 üzerinden 8` ve `10 üzerinden 3` var, puansız hücre boş; HTML
    panoda aynı üç değer.
  - Yapıştırma (`node` ile fonksiyon ayıklandı): `"10 üzerinden 4"` → `4`,
    `"7 ÜZERİNDEN 5"` → `5`, `"4"` → `4`, sayı alanında `"10 üzerinden 4"`
    dokunulmadan kalıyor.
  - Sunucu: `bcc_rating_out_of_text` 8/10, 5/7, null, 0, options null (5),
    12/10 (→ 10) doğru; `cell_display_text` hâlâ `★★★★☆☆☆☆☆☆`.
  - **Gerçek HTTP ile Excel indir:** `_verify_group_c1.php` 56/56 — indirilen
    xlsx'in `sheet1.xml`'inde `7 üzerinden 5`, `★`/`☆` yok. Sayaçlar önce = sonra.
  - `_verify_grid_export` 69/69, `_verify_xlsx_cell_integrity` 11/11,
    `_verify_grid_save_copy_pdf` 56/56, `_verify_grid_clipboard.js` 57/57,
    yeni betik 22/22 (üç dosya geri alınınca 16/22).
- **Not:** Bu ölçümden önce sayaçlar sabahkinden farklıydı (`tables_meta`
  6→5, `records` 170→171, `cell_values` 694→710, `audit_log` 15245→15263,
  `notify_sent` 2445→2447) — kullanıcının uygulamayı gün içinde kullanması
  (mail, kopyalama, tablo işlemleri); testler öncesi/sonrası aynı.

### 17. İkinci commit/push turu
- **Tam paket yeniden** (§14-§16 değişiklikleri dahil): 79 PHP betiği — 75
  geçti, 4'ü demo hesabı yokluğundan (§13 ile aynı); `_verify_grid_clipboard.js`
  57/57. Demo seed → 8 bağımlı betik → `--remove`: 7 geçti, yine yalnızca
  bayat `_verify_slack_integration.php:284` kontrolü. Sayaçlar paket öncesi,
  paket sonrası ve demo temizliği sonrası üçünde de aynı: `users=6 teams=4
  team_members=2 bases=7 tables_meta=5 fields=47 records=171 cell_values=710
  audit_log=15263 notify_sent=2447 bcc_local=0`.
- **Commit'ler:** `6809264` puan yazısı + mail tablo düzeni (§14-§16, 8 dosya),
  ardından bu belgeler. Dosyalar tek tek eklendi; staged diff okundu; izlenen
  diff + takip edilmeyen yeni betik sır taramasından geçti (bulgu yok).

### 18. "Excel indir"de Oluşturan / Son değiştiren (ve zaman) sütunları boş
- **Rapor:** Excel indirince "Son değiştiren", "Oluşturan" gibi kullanıcı
  sütunları görünmüyor.
- **Ölçülen (gerçek HTTP, yeni betik — düzeltmeden ÖNCE):** kendi
  ekibi/kullanıcıları/tablosu olan fikstürden indirilen xlsx'te
  **Oluşturan, Son değiştiren, Oluşturulma zamanı, Son değişiklik zamanı
  dördü de boş**; "Kullanıcı" alan tipi ("Sorumlu") ve otomatik numara DOĞRU
  geliyordu (bunlar `cell_values`'ta duruyor). 7 kontrol kaldı.
- **Kök neden:** `api/view_export_xlsx.php` hücreyi doğrudan
  `$cellsByRecord[kayıt][alan]`'dan alıyordu. Bu dört alan `cell_values`'ta
  tutulmaz, kaydın kendi kolonlarından (`records.created_by`, `updated_by`,
  `created_at`, `updated_at`) hesaplanır; grid ve kanban bunun için
  `bcc_cell_row_for_field()` kullanıyor, dışa aktarma kullanmıyordu →
  `cell_display_text(..., null)` → boş.
- **Yapılan:** Dışa aktarma da `bcc_cell_row_for_field($f['field_type'], $rec,
  $cellsByRecord, $f['id'])` kullanıyor (grid'le aynı çözücü); artık
  kullanılmayan `$cellsForRecord` satırı silindi. "Son değiştiren", hiç
  düzenlenmemiş kayıtta grid'deki gibi oluşturana düşüyor.
- **Değişen dosyalar:**
  - `public/api/view_export_xlsx.php:47-56` — hücre çözümü
  - `scripts/_verify_xlsx_export_computed_fields.php` — **YENİ**, 15 kontrol
    (gerçek HTTP giriş + indirme + xlsx okuma; kendi verisini kurup siliyor,
    9 tablo sayacını kendisi doğruluyor)
- **Test:** yeni betik düzeltmeden önce 8/15, sonra **15/15** (Oluşturan =
  "Ayla Olusturan", Son değiştiren = "Bora Degistiren", düzenlenmemiş kayıtta
  oluşturan, zamanlar `01.09.2026 09:15` / `02.09.2026 17:40`, kullanıcı alanı
  ve numara bozulmadı). `_verify_grid_export` 69/69, `_verify_group_c1` 56/56,
  `_verify_user_field_former_member` 20/20, `_verify_xlsx_cell_integrity`
  11/11, `_verify_record_send_rating_text` 22/22. Sayaçlar önce = sonra.
- **Commit:** `70cbd2b`.
- **Aynı kalıp, DÜZELTİLMEDİ (kapsam dışı, kullanıcıya soruldu):**
  `public/interface.php:263/270/290` (Arayüz sayfası kart ve detay alanları)
  da hücreyi doğrudan `cell_values`'tan alıyor; üstelik
  `bcc_interface_fetch_records()` sorgusu `created_by` / `updated_by`
  kolonlarını hiç çekmiyor. Arayüzde bu dört alan muhtemelen boş görünüyor —
  ölçülmedi.

### 19. "PNG olarak indir" görüntüsü yarıda kesiliyordu
- **Rapor (ekran görüntüsü, Fotoğraflar):** İndirilen PNG'de başlık tam ama
  ilk satır yarıda kesik, sağ taraf da kesik; kelimeler arası anormal geniş
  ("Tek   satır   metin").
- **Ölçülen:** Detay fikstürü (21 alan, gerçek `bcc_render_grid_data_row` +
  `grid-export.css` + `vendor/html2canvas.min.js` + `grid-export-png.js`)
  Chromium'da, `BCC_GRID_EXPORT.captureCanvas('PNG')` ile; canvas piksel
  olarak incelendi ve görüntüler yan yana basıldı.
  - Sonuç SIRAYA değil PENCERE BOYUTUNA bağlı (5 tekrar): sayfa yakınlaştırması
    `--bcc-zoom` **1** iken (1400x900) görüntü kullanıcınınkiyle birebir —
    satır kesik, geniş kelime aralığı; **1.25** iken (1920x960) tam.
  - Bu sabahki (`a6f8ff6`) CSS/JS ile de aynı → bugünkü değişikliklerden
    kaynaklanan bir gerileme DEĞİL.
  - İlk varsayım ("yakınlaştırmalı sayfada bozuluyor") ölçümle YANLIŞ çıktı.
- **Kök neden:** html2canvas görüntüyü sayfanın bir KOPYASINDA yeniden
  kuruyor ve kopya penceresini `windowWidth: max(clientWidth, width + 100)` —
  yani tablo kadar geniş (~2400px) — açıyor. `theme.css`'teki yakınlaştırma
  kuralları (`@media (min-width: 1900px) and (min-height: 900px) { --bcc-zoom:
  1.25 }` …) pencere genişliğine baktığı için kopya, canlı sayfadan farklı bir
  yakınlaştırma alabiliyor; sütun genişlikleri ve canvas boyutu canlı sayfanın
  yakınlaştırmasız CSS pikselinden ölçülüp çizim başka yakınlaştırmada
  yapılınca satırlar taşıyıp kesiliyor, metin ölçüsü kayıyor.
- **Denenen ve BIRAKILAN:** Kopyanın yakınlaştırmasını canlı sayfanınkine
  eşitlemek — 1'de düzeldi ama 1.25 ve 1.45'te BOZDU (ölçüldü). Canvas
  yakınlaştırmasız pikselle kurulduğu için kopyanın her durumda 1'de
  çizilmesi gerekiyor. (Düzeltme öncesi canlı 1.25'te neden tesadüfen doğru
  çıktığı tam açıklanmadı; sonuç ölçümle doğrulandı.)
- **Yapılan:** `onclone` içinde `clonedDoc.documentElement.style.setProperty
  ('--bcc-zoom', '1')` — satır içi stil media kurallarını ezer. Canlı sayfaya
  dokunulmuyor. PDF yolu zaten `@media print { --bcc-zoom: 1 }` kullanıyor.
- **Değişen dosyalar:**
  - `public/assets/grid-export-png.js:118` — kopyada yakınlaştırma 1
  - `scripts/_verify_grid_export.php:249-253` — +2 kontrol (69 → 71; JS geri
    alınınca 70/71)
- **Test:** Düzeltme sonrası dört pencere boyutu / yakınlaştırma (1400x900 → 1,
  1750x850 → 1.1, 1920x960 → 1.25, 2300x1100 → 1.45): canvas boyutu tabloyla
  birebir, dört görüntü de tam ve birbirinin aynı (göz kontrolü). 3 satırlı
  fikstürde 1.25'te tablonun sol ve sağ ucu tam (Telefon numarası son sütun
  görünüyor). `_verify_grid_export` 71/71, `_verify_grid_save_copy_pdf` 56/56;
  sayaçlar önce = sonra. Gerçek uygulama sayfasında denenmedi (fikstür aynı
  CSS/JS'i kullanıyor, sol panel/üst çubuk yok) — kullanıcının Ctrl+Shift+R
  sonrası denemesi gerekiyor.
- **Commit:** `fa1d35a` (§20 ile birlikte).

### 20. PNG/PDF — kullanıcı "düzelmedi" dedi; gerçek uygulamada ölçüldü
- **Rapor:** PDF'te de aynı kesilme (PDF de `captureCanvas` kullanıyor —
  `grid-export-pdf.js:96`). Kullanıcı tahmini: çoklu seçim satırı uzattığı
  için; görüntü "en uzun hücreye göre" ayarlansın.
- **Neden gerçek sayfa:** §19 statik fikstürde doğrulanmıştı; fikstürde sol
  panel / görünüm çekmecesi / kaydırma kabı yok. Bu sefer GERÇEK
  `grid.php` Apache üzerinden, ayrı test verisiyle:
  `scratchpad/realfx.php setup` → yeni kullanıcı `pngtest.<rastgele>@bcc-test.local`,
  kendi ekibi `PNGTEST-<rastgele>`, base, 21 alanlı "DENEME" tablosu; `/browse`
  bu kullanıcıyla giriş yaptı, `BCC_GRID_EXPORT.captureCanvas('PNG')`
  çağrıldı, canvas PNG'ye çevrilip incelendi. Gerçek hesaplara dokunulmadı.
- **Eski (commit'teki) ve yeni `grid-export-png.js` aynı koşullarda**
  (dosya `git stash` ile değiştirildi; `bcc_asset_url` mtime ile yeni sürümü
  zorluyor):

  | Pencere | Yakınlaştırma | Eski | Yeni |
  |---|---|---|---|
  | 1400x800 | 1 | içerik büyük, satır kesik, geniş kelime aralığı | tam |
  | 1920x870 | 1.1 | kesik | tam |
  | 1920x950 | 1.25 | kesik (kullanıcının görüntüsünün aynısı) | tam |

  → Düzeltme gerçek sayfada ÇALIŞIYOR. Kullanıcıda hâlâ bozuk çıkmasının en
  olası sebebi sayfanın düzeltmeden önce açılmış olması (JS bellekte eski);
  **doğrulanmadı**, kullanıcıdan Ctrl+Shift+R sonrası tekrar denemesi istendi.
- **Çoklu seçim tahmini:** Satır uzaması kesilmenin SEBEBİ değil ama etkisini
  büyütüyor (uzun satır yakınlaştırma farkıyla daha çok taşıyor). Canvas
  yüksekliği zaten `table.scrollHeight`'ten, yani en uzun hücreyle büyümüş
  satırlardan hesaplanıyor: 5 seçenekli çoklu seçim × 3 satırla ölçüldü —
  satır 117px, PNG 84 → **383px**, 1920x950 ve 1400x800'de iki görüntü de tam.
  Ek kod gerekmedi.
- **Temizlik:** fikstür iki kez kuruldu/silindi. Sonrası `users=6 teams=4
  team_members=2 bases=7 tables_meta=5 fields=47 records=171 cell_values=710
  audit_log=15275 notify_sent=2448`, önce ile birebir; `pngtest.%` kullanıcı 0,
  `PNGTEST-%` ekip 0.
- **Kod değişikliği YOK** (§19'daki tek satır geçerli). **Commit:** `fa1d35a`.

### 21. Çalışma alanları: "Katılımcılar" kartı kişi sayısına göre uzuyor
- **Rapor (ekran görüntüsü):** Tek katılımcılı çalışma alanında "Katılımcılar"
  kartı çok uzun, alt kısmı boş. Kişi eklendikçe uzasın.
- **Kök neden — bilinçli eski karar:** 2026-09-04 "kırk altıncı adım"
  (`PROJE-DURUM.md`) üç listeye (sol çalışma alanı seçici, katılımcılar, son
  hareketler) ortak SABİT yükseklik bandı vermişti: `height:
  var(--wsx-list-h)` (420px), sütunlar hizalı dursun diye. Katılımcılar kartı
  ise sayfada kendi başına ikinci satırda (`grid-row: 2`); sağdaki "Son
  Hareketler" birinci satırda — yani onu kısaltmak sütun hizasını bozmuyor.
- **Yapılan:** `.wsx-collab-grid` ortak bant kuralından SONRA kendi kuralıyla
  `height: auto; max-height: var(--wsx-list-h)` — kişi sayısı kadar uzuyor,
  420px'i geçince eskisi gibi kendi içinde kayıyor. Sol panel ve son
  hareketler sabit bantta kaldı. "Eşleşen katılımcı yok" satırı
  (`.wsx-collab-empty`, 8+ kişide çıkan aramanın boş sonucu) bant
  `min-height`'inden çıkarıldı — yoksa boş aramada kart yine 420px'e uzardı.
- **Değişen dosyalar:**
  - `public/assets/workspaces.css:14-18` — `.wsx-collab-grid` özel kuralı;
    boş durum kuralı yalnızca `.wsx-act-empty`
  - `scripts/_verify_workspaces_list_bands.php` — E bölümü yeni karara göre
    (eski "iki boş durum tek kuralda" kontrolü 20/22 düşürdü), yeni E2 bölümü
    (+4) → 26/26
- **Test:** Gerçek `workspaces.php`, ayrı test kullanıcısı + 1, 6 ve 20 üyeli
  üç test ekibi (`scratchpad/wsfx.php`), 1920x950:
  1 kişi → kart 168px (ızgara 69px, kaymıyor); 6 kişi → 326px; 20 kişi → ızgara
  üst sınırda (525px = 420 × 1.25 yakınlaştırma), içerik 624px, kayıyor.
  Ekran görüntüleriyle göz kontrolü. `_verify_settings_pages_ui` 139/139,
  `_verify_global_search` çıkış 0. Temizlik: test kullanıcı/ekip 0, sayaçlar
  önce = sonra.
- **Commit:** `119ffc2` (§22 ile birlikte).

### 22. Çalışma alanları: "Base'ler" kartı da içeriği kadar
- **Rapor (ekran görüntüsü):** §21 gibi — 2 base'li çalışma alanında
  "Base'ler" kartının alt yarısı boş.
- **Kök neden:** `.wsx-body` iki sütunlu ızgara; sol ve sağ sütun kapları
  `display: contents` ile ÇÖZÜLÜP kartlar elle hücrelere yerleştiriliyordu
  (Base'ler 1. satır sol, Katılımcılar 2. satır sol, Son Hareketler 1. satır
  sağ). Sağdaki kart §21'deki sabit bantta (420px + başlık ≈ 607px) olduğu
  için 1. satır o yüksekliğe çıkıyor, Base'ler kartı da ızgaranın varsayılan
  `align-items: stretch`'iyle satırı doldurmak için uzuyordu.
- **Yapılan:** Sol sütun gerçek bir sütun oldu: `.wsx-body-main { display:
  flex; flex-direction: column; gap }`, `.wsx-body { align-items: start }`;
  `display: contents` ve kartlara elle verilen `grid-row` / `grid-column`
  atamaları silindi. Base'ler içeriği kadar, Katılımcılar hemen altında; sağ
  kart kendi boyunda. 1280px altında tek sütuna düşme aynı (DOM sırası:
  Base'ler, Katılımcılar, Son Hareketler — eskisiyle aynı). Kartların kendi
  `margin-bottom`'una dokunulmadı; kartlar arası mesafe eskisiyle aynı.
- **Değişen dosyalar:**
  - `public/assets/workspaces.css:468-487` — gövde düzeni
  - `scripts/_verify_workspaces_list_bands.php` — yeni E3 bölümü (+4) → 30/30
- **Test:** Gerçek `workspaces.php`, ayrı test kullanıcısı + 2 ve 8 base'li iki
  test ekibi (`scratchpad/wsfx2.php`), 1920x950: 2 base → kart 282px (base
  ızgarasının altında yalnızca 28px iç boşluk), 8 base → 859px; Katılımcılar
  kartı her ikisinde Base'ler'in 46px altında; sağ kart 607px ve üst kenarı
  Base'ler ile hizalı. 1200x900'de sağ kart ana sütunun altına, aynı genişlikte
  iniyor. Ekran görüntüsüyle göz kontrolü. `_verify_settings_pages_ui`
  139/139, `_verify_global_search` ve `_verify_rbac` çıkış 0. Temizlik: test
  kullanıcı/ekip 0, sayaçlar önce = sonra.
- **Commit:** `119ffc2`.

### 23. "İmlecin yanındaki şey ne?" — sütun dondurma tutamacının kırpılan ipucu balonu (kod değişikliği YOK)
- **Soru (ekran görüntüsü, tablo 2992):** İlk sütunun sağ kenarında imleç "el"
  işaretine dönüyor, solunda başlığın hemen üstünde soluk yatay bir çizgi var.
- **Teşhis:** El imleci + mavi dikey çizgi = **sütun dondurma tutamacı**
  (`grid-freeze-columns.js`, `.grid-freeze-handle`, `cursor: grab`) —
  sürükleyerek soldan kaç sütunun sabit kalacağı ayarlanıyor. Tutamaca
  `data-tooltip-host` ile "Dondurulan sütun sayısını ayarlamak için
  sürükleyin" ipucu bağlı; `.gs-kbd-tooltip` `bottom: calc(100% + 0.4rem)` ile
  başlığın ÜSTÜNE açılıyor ve `.grid-wrap`'ın taşma kırpmasına takılıyor.
- **Ölçülen (gerçek `grid.php`, ayrı test verisi, 1920x950, tutamaca hover):**
  balon `display: flex` (açık), kutusu y=192–222px, tablo kabının üst kenarı
  y=230px → **görünen yükseklik 0**. Kullanıcının sayfasında kap biraz daha
  yukarıdan başladığı için balonun yalnızca alt kenarı/gölgesi ince çizgi
  olarak görünüyor. Yani ipucu var ama hiç okunamıyor.
- **Karar kullanıcıya soruldu** (balonu alta taşı / kaldır). Test verisi
  silindi, sayaçlar önce = sonra.

### 24. Üçüncü commit/push turu
- **Tam paket** (§18-§22 dahil): 80 PHP betiği — 76 geçti, 4'ü demo hesabı
  yokluğundan; `_verify_grid_clipboard.js` 57/57. Demo seed → 8 bağımlı betik
  → `--remove`: 7 geçti, yine yalnızca bayat
  `_verify_slack_integration.php:284`. Sayaçlar paket öncesi / sonrası / demo
  temizliği sonrası aynı: `users=6 teams=4 team_members=4 bases=7
  tables_meta=5 fields=47 records=171 cell_values=710 audit_log=15281
  notify_sent=2448 bcc_local=0` (`team_members` 3→4 ve `audit_log` artışı
  kullanıcının gün içindeki kendi işlemi — testlerden önce ölçüldü).
- **Commit'ler:** `70cbd2b` Excel hesaplanan alanlar (§18), `fa1d35a` PNG/PDF
  kesilmesi (§19-§20), `119ffc2` çalışma alanı kartları (§21-§22), ardından
  bu belgeler. Dosyalar tek tek eklendi, her commit'te staged diff okundu ve
  sır taraması yapıldı (tek eşleşme test betiğinin rastgele ürettiği giriş
  şifresi değişkeni — gerçek sır değil).
- **Açık:** §23 dondurma tutamacı ipucu balonu — kullanıcı kararı bekleniyor.

### 25. Dondurma tutamacının ipucu balonu kaldırıldı (§23 kararı: "Kaldır")
- **Yapılan:** `grid-freeze-columns.js` tutamaca artık `.gs-kbd-tooltip`
  balonu ve `data-tooltip-host` eklemiyor. Tutamacın kendisi (el imleci, mavi
  çizgi, sürükleme) aynen duruyor. Aynı gerekçeyle sütun boyutlandırma
  şeridinde balon zaten hiç üretilmiyordu (`_verify_column_resize` G maddesi:
  "grid-wrap dışına taşardı"). Genel `.gs-kbd-tooltip` / `[data-tooltip-host]`
  CSS kuralları duruyor — başka yerlerde (sekme menüsü vb.) kullanılıyor.
- **Değişen dosyalar:**
  - `public/assets/grid-freeze-columns.js:28-31` — balon ve öznitelik silindi (5 satır)
  - `scripts/_verify_column_resize.php:504-506` — "dondurma tutamacının balonu
    KORUNDU" kontrolü "balon üretilmiyor"a çevrildi (104/104)
- **Test:** Gerçek `grid.php`, ayrı test verisi, 1920x950, tutamaca hover:
  tutamaç var, imleç `grab`, mavi çizgi görünür (`opacity 1`), balon 0,
  `data-tooltip-host` yok, konsol hatası yok; ekran görüntüsünde başlığın
  üstündeki ince çizgi yok. `node --check` temiz; `_verify_column_resize`
  104/104, `_verify_grid_export` 71/71, `_verify_grid_column_autofit` 18/18.
  Test verisi silindi, sayaçlar önce = sonra.
- **Commit:** `6009e41`.

### 26. Arayüz (interface) araması yalnızca kalın başlıkta
- **İstek (ekran görüntüsü, `interface.php?base_id=2126`):** Sol listedeki
  "Ara..." kutusu notlarda değil, yalnızca kalın puntolu başlıklarda arasın.
- **Kök neden:** Arama sunucuda `bcc_interface_fetch_records()`
  (`src/schema.php`) içinde; `cv.field_id IN (birincil, özet)` ile hem kalın
  başlığı (tablonun ilk alanı) hem de altında görünen not satırını
  (`bcc_interface_summary_field()` — ilk `long_text` alan) `LIKE %...%` ile
  arıyordu. Liste `interface.js` → `api/interface_records.php?q=`.
- **Yapılan:** Arama yalnızca birincil alanda: `cv.field_id = ?`. Özet alanı
  parametresi yalnızca arama için kullanıldığından fonksiyon imzasından
  çıkarıldı (`$tableId, $primaryFieldId, $searchTerm`), üç çağıran
  güncellendi (`interface.php`, `api/interface_records.php`,
  `api/interface_search.php`). Başlık içinde "içerir" eşleşmesi, `%`/`_`
  kaçışı, filtre/sıralama/gruplama ile birleşimi aynı. Liste kartındaki not
  önizlemesi (`$summaryField`) görünmeye devam ediyor — yalnızca aramadan
  çıktı.
- **Değişen dosyalar:**
  - `src/schema.php:3568-3593` — `bcc_interface_fetch_records`
  - `public/interface.php:56-61`, `public/api/interface_records.php:33-36`,
    `public/api/interface_search.php:17-20` — çağrılar
  - `scripts/_verify_interface_search_primary_only.php` — **YENİ**, 14 kontrol
    (gerçek HTTP; iki uç nokta; kendi ekip/kullanıcı/tablosunu kurup siliyor)
- **Test:** yeni betik 14/14; dört dosya `git stash` ile geri alınınca 4/14 —
  eski kod "fatura" (yalnızca notlarda) aramasında iki kaydı, "ALKAN"da notunda
  "Alkan" geçen KRALSPORT'u da getiriyordu. `php -l` dört dosyada temiz.
  Tarayıcıda denenmedi (liste doğrudan doğrulanan uç noktayı çağırıyor).
- **Ekran görüntüsünde görülen, DOKUNULMAYAN:** listede
  "Kralsport&#039;tan" — not önizlemesinde HTML varlığı çift kaçışlı basılıyor
  (ayrı hata, istenmedi).
- **Commit'lenmedi** — kullanıcı henüz denemedi; bu turda yalnızca §25
  commit'lendi.

### 27. Dördüncü commit/push turu
- **Tam paket** (§25 + §26 çalışma ağacında): 81 PHP betiği — 77 geçti, 4'ü
  demo hesabı yokluğundan; `_verify_grid_clipboard.js` 57/57. Demo seed → 8
  bağımlı betik → `--remove`: 7 geçti, yine yalnızca bayat
  `_verify_slack_integration.php:284`. Sayaçlar üç ölçümde aynı: `users=6
  teams=4 team_members=4 bases=7 tables_meta=5 fields=48 records=171
  cell_values=711 audit_log=15295 notify_sent=2450`
  (`fields` 47→48, `cell_values` 710→711, `notify_sent` 2448→2450 kullanıcının
  gün içindeki kendi işlemi — paket öncesi ölçüldü).
- **Commit'ler:** `6009e41` dondurma tutamacı balonu (§25), ardından bu
  belgeler. §26 (arayüz araması) bilerek commit'lenmedi.
